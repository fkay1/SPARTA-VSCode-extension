{
  "fix": [
    {
      "family": "fix",
      "style": "ablate",
      "description": "Perform ablation once every Nevery steps on a set of grid cell corner\r points to induce new implicit surface elements in those grid cells.\r This command is also used as an argument to the\r \"read_isurf\"_read_isurf.html command so that the grid corner point\r values it reads from a file can be assigned to and stored by each grid\r cell.\r \r Here are simulation snapshots of 2d and 3d implicit surface models\r through which particles flow.  Click on any image for a larger image.\r The 1st and 3rd images are the initial states of the porous media.\r The 2nd and 4th images are snapshots midway through an ablation\r simulation.  In the 2d case, the colorings are by processor for\r sub-domains each owns.  Particles flow from left to right.  The\r implicit triangles for the 3d case were created via Marching Cubes\r (discussed on the \"read_isurf\"_read_isurf.html command doc page) from\r a tomographic image of a sample of NASA FiberForm (TM) material, used\r as a heat shield material on spacecraft.  Particles flow from top to\r bottom.\r \r :c,image(JPG/porous2d_initial_small.png,JPG/porous2d_initial.png),image(JPG/porous2d_ablated_small.png,JPG/porous2d_ablated.png),image(JPG/porous3d_initial_small.png,JPG/porous3d_initial.png),image(JPG/porous3d_ablated_small.png,JPG/porous3d_ablated.png)\r \r The specified {group-ID} must be the name of a grid cell group, as\r defined by the \"group grid\"_group.html command, which contains a set\r of grid cells, all of which are the same size, and which comprise a\r contiguous 3d array.  It must be the same as group-ID used with the\r \"read_isurf\"_read_isurf.html command, which specifies its {Nx} by {Ny}\r by {Nz} extent.  See the \"read_isurf\"_read_isurf.html command for more\r details.  This command reads the initial values for grid cell corner\r points, which are stored by this fix.\r \r The specfied {Nevery} determines how often an ablation operation is\r performed.  If {Nevery} = 0, ablation is never performed.  The grid\r cell corner point values and the surface elements they induce will\r remain static for the duration of subsequent simulations.\r \r The specified {scale} is a pre-factor on the specified {source} of\r ablation strength.  It converts the per grid cell numeric quantities\r produced by the {source} (which may have associated units) to a\r unitless decrement value for the grid cell corner points, which range\r from 0 to 255 inclusive.  A value of 255 represents solid material and\r a value of 0 is void (flow volume for particles).  Values in between\r represent partially ablated material.\r \r The {source} can be specified as a per grid cell quantity calculated\r by a compute, fix, or variable.  For example, \"compute\r isurf/grid\"_compute_isurf_grid.html can tally the number of collisions\r of particles with the surfaces in each grid cell or the amount of\r energy transferred to the surface by the collisions.  Or \"compute\r react/isurf/grid\"_compute_isurf_grid.html can tally the number of\r reactions that remove a species from the surface.\r \r An example of a fix which be used as a {source} is \"fix\r ave/grid\"_fix_ave_grid.html which could use either of those per grid\r cell computes as input.  It could thus accumulate and time average the\r same quantities over many timesteps.  In that case the {scale} factor\r should account for applying a time-averaged quantity at an interval of\r {N} steps.  \r \r Finally, a grid-style variable can be be used as a {source}.  This\r could perform a calculation on other per grid cell quantities.  For\r example, it could add and subtract columns from the compute or fix\r just mentioned to tally adsorption versus desorption reactions and\r thus infer net mass removed from the surface.\r \r For debugging purposes, the {source} can also be specified as {random}\r with an additional integer {maxrandom} value also specified.  In this\r case, the {scale} factor should be floating point value between 0.0\r and 1.0.  Each time ablation is performed, two random numbers are\r generated for each grid cell.  The first is a random value between 0.0\r and 1.0.  The second is a random integer between 1 and maxrandom.  If\r the first random # < {scale}, then the second random integer is the\r decrement value for the cell.  Thus {scale} is effectively the\r fraction of grid cells whose corner point values are decremented.\r \r For basic testing of new ablation capabilities or geometries, the\r {source} can be specified as {uniform}. Any cell which contains part\r of the gas and the surface is decremented by {maxrandom}.\r \r See the explanation for the optional {mindist} and {multiple} keywords\r below.\r \r :line\r \r Here is an example of commands that will couple ablation to surface\r reaction statistics to modulate ablation of a set of implicit\r surfaces.  These lines are taken from the\r examples/ablation/in.ablation.3d.reactions input script:\r \r surf_collide\t    1 diffuse 300.0 1.0\r surf_react\t      2 prob air.surf :pre\r \r compute             10 react/isurf/grid all 2\r fix                 10 ave/grid all 1 100 100 c_10\\[*\\]\r dump                10 grid all 100 tmp.grid id c_10\\[1\\] :pre\r \r global              surfs implicit\r fix                 ablate ablate all 100 2.0 c_10\\[1\\]   # could be f_10\r read_isurf          all 20 20 20 binary.21x21x21 99.5 ablate :pre\r \r surf_modify         all collide 1 react 2 :pre\r \r The order of these commands matter, so here is the explanation.\r \r The \"surf_modify\"_surf_modify.html command must come after the\r \"read_isurf\"_read_isurf.html command, because surfaces must exist\r before assigning collision and reaction models to them.  The \"fix\r ablate\"_fix_ablate.html command must come before the\r \"read_isurf\"_read_isurf.html command, since it uses the ID of the \"fix\r ablate\"_fix_ablate command as an argument to create implicit surfaces.\r The \"fix ablate\"_fix_ablate.html command takes a compute or fix as an\r argument, in this case the ID of the \"compute\r react/isurf/grid\"_compute_react_isurf_grid.html command.  This is to\r specify what calculation drives the ablation.  In this case, it is the\r \"compute react/isurf/grid\"_compute_react_isurf_grid.html command (or\r could be the \"fix ave/grid\"_fix_ave_grid.html command) which tallies\r counts of surface reactions for implicit triangles in each grid cell.\r The \"compute react/isurf/grid\"_compute react/isurf/grid command\r requires the ID of a surface reaction model, so that it knows the list\r of possible reactions to tally.  In this case the reaction is set by\r the \"surf_react\"_surf_react.html command, which must therefore comes\r near the beginning of this list of commands.\r \r :line\r \r As explained on the \"read_isurf\"_read_isurf.html doc page, the\r marching cubes (3d) or marching squares (2d) algorithm is used to\r convert a set of grid corner point values to a set of implicit\r triangles in each grid cell which represent the current surface of\r porous material which is undergoing dynamic ablation.  This uses a\r threshold value, defined by the \"read_isurf\"_read_isurf.html command,\r to set the boundary between solid material and void.\r \r The ablation operation decrements the corner point values of each grid\r cell containing porous material.  The marching cubes or squares\r algorithm is re-invoked on the new corner point values to create a new\r set of implicit surfaces, which effectively recess due to the\r decrement produced by the ablative {source} factor.\r \r The manner in which the per-grid source decrement value is applied to\r the grid corner points is as follows.  Note that each grid cell has 4\r (2d) or 8 (3d) corner point values.  Except at the boundary of the 2d\r of 3d array of grid cells containing porous materials, each corner\r point is similarly shared by 4 (2d) or 8 (3d) grid cells.\r \r Within each grid cell, the decrement value is subtracted from the\r smallest corner point value.  Except that a corner point value cannot\r become smaller than 0.0.  If this would occur, only a portion of the\r decrement is used to set the corner point to 0.0; the remainder is\r applid to the next smallest corner point value.  And so forth on\r successive corner points until all of the decrement is used.\r \r The amount of decrement applied to each corner point is next shared\r between all the grid cells (4 or 8) sharing each corner point value.\r The sum of those decrements is subtracted from the corner point,\r except that it's final value is set no smaller than 0.0.  All the\r copies of each corner point value are now identical.\r \r :line\r \r One issue with the marching cubes or squares algorithm is that it can\r produce very tiny triangles (3d) or line segments (2d) when grid\r corner point values are equal to or very close to the threshold value.\r \r To avoid this problem, the default behavior of this fix is to use an\r \"epsilon method\" to adjust a grid corner point value.  If the corner\r point has a value X where threshold-epsilon < X < threshold+epsilon,\r then it is reset to a value = threshold-epsilon.  As explained above,\r the threshold value is defined by the \"read_isurf\"_read_isurf.html\r command.  Epsilon is set within the code to be 1.0e-4.  Note that this\r is on the scale of corner point values which can range from 0 to 255.\r \r An alternate method for avoiding tiny triangles or line segments is to\r use the {mindist} keyword.  For 3d models, its {fraction} value sets\r the minimum fractional distance between any vertex of a triangle\r generated by the marching cubes algorithm and any of the 8 corner\r points of the grid cell.  For 2d models, it sets the minimum\r fractional distance between any end point of a line segment generated\r by the marching squares algorithm and any of the 4 corner points of\r the grid cell.  Fractional means relative to the grid \r cell edge length.  I.e. if the grid cell size is 2.0 and {fraction} is \r 0.1, then the fractional distance is 0.2.\r \r The specified {fraction} value must be a number >= 0.0 and < 0.5.  If\r the value is less than 1.0e-4, then it is treated as if the value were\r 0.0 (the default), and the epsilon method described above is used.\r \r For values of fraction >= 1.0e-4, the \"isosurface stuffing\" method\r proposed by Labelle and Shewchuk \"(Labelle07)\"_#Labelle07 is used.\r The idea is as follows:\r \r If a generated triangle vertex or line segment end point could be\r geometrically closer to a grid corner point than {fraction}, the\r vertex location is adjusted to ensure the vertex/end-point will always\r be at least a distance {fraction} from the corner point and remains\r between the same inside and outside grid corner points.  The grid\r point values themselves are not changed.  There are two cases to\r consider.\r \r (1) The vertex is too close to a inside grid corner point. In this\r case, the vertex location is shifted towards the outside corner point\r such that the relative distance from the vertex to the inside corner\r point is at least {mindist}\r \r (2) Conversely, if the vertex is too close to the outside grid corner\r point, the vetex location is shifted towards the inside corner point\r such that the relative distance from the vertex to the outside corner\r point is at least {mindist}\r \r :line\r \r :c,image(JPG/multipoint_decrement_small.png)\r \r The {multiple} option allows a multipoint decrement \"(Hong24)\"_#Hong24 to be used. \r In a cell, three types of corners are identified: inside, outside or\r interface.  An [inside] point is a point inside the surface (its\r values is greater than the specified threshold).  An [outside] point\r is a point that is outside the surface and all corner points adjacent\r to that corner point within the cell are also outside the surface.  An\r [interface] is a point outside the surface but one of its neighboring\r points is inside. Given a cell decrement, the multipoint decrement\r distributes that cell decrement evenly to each interface point. In the\r case the interface point becomes negative, the negative value is\r evenly distributed to each of the adjoining inside points. The\r multipoint decrement may also be used with the multivalues.\r \r :c,image(JPG/multi_single_ablation_small.png)\r \r The multipoint decrement is conceptually different than the single\r point decrement.  A single point decrement visits each corner point\r one at a time and reduces the minimum corner point value found.  The\r multipoint decrement reduces all interface points. A distributed\r decrement is advantageous when one is interested in preserving cell\r level features. In the above example, a single cell is ablated\r (indicated by a red arrow). With a single point decrement, only one\r corner point is updated and only the left side of the affected cell\r ablates. With the multipoint decrement, two corner points are reduced\r and the entire surface contained within the ablated cell recedes.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global scalar and a global vector of length 2.\r The global scalar is the current sum of unique corner point values\r across the entire grid (not counting duplicate values).  This sum\r assumes that corner point values are 0.0 on the boundary of the 2d or\r 3d array of grid cells containing implicit surface elements.\r \r The 2 vector values are the (1) sum of decrement values for each grid\r cell in the most recent ablation operation, and (2) the # of particles\r deleted during the most recent ablation operation that ended up\r \"inside\" the newly ablated surface.  The latter quantity should be 0.\r A non-zero value indicates a corner case in the marching cubes or\r marching squares algorithm the developers still need to address.\r \r These values can be accessed by any command that uses global values\r from a fix as input.  See \"Section 6.4\"_Section_howto.html#howto_4 for\r an overview of SPARTA output options.\r \r The scalar and vector values are unitless.",
      "docFile": "fix_ablate.txt"
    },
    {
      "family": "fix",
      "style": "adapt",
      "description": "This command performs on-the-fly adapatation of grid cells as a\r simulation runs, either by refinement or coarsening or both.  Grid\r adaptation can also be performed before or between simulations by\r using the \"adapt_grid\"_adapt_grid.html command.\r \r Refinement means splitting one child cell into multiple new child\r cells; the original child cell becomes a parent cell.  Coarsening\r means combining all the child cells of a parent cell, so that the\r child cells are deleted and the parent cell becomes a single new child\r cell.  See \"Section howto 4.8\"_Section_howto.html#howto_8 for a\r description of the hierarchical grid used by SPARTA and a defintion of\r child and parent cells.\r \r Grid adaptation can be useful for adjusting the grid cell sizes to the\r current particle density distribution, or mean-free-path of particles,\r or to other simulation attributes such as the presence of surface\r elements.  A well-adapted grid can improve accuracy of the simulation\r and/or reduce a simulation's computational cost.\r \r Adaptation is performed by this command once every {Nfreq} timesteps.\r \r All of the command arguments which appear after {Nfreq}, which\r determine how adapation is done for both refinement and coarsening,\r are exactly the same as for the \"adapt_grid\"_adapt_grid.html command.\r \r This includes a group-ID parameter which can be used to limit\r adaptation to a subset of current grid cells.  See the\r \"adapt_grid\"_adapt_grid.html command doc page for details.\r \r The one exception is that the {iterate} keyword cannot be used with\r the fix adapt command.  Only a single iteration of the action1 and\r action2 parameters (described on the \"adapt_grid\"_adapt_grid.html doc\r page) can be performed each time grid adaptation is performed.\r \r Note that if you want the grid partitioning (and their particles) to\r be rebalanced across processors after grid adaptation, you can use the\r \"fix_balance\"_fix_balance.html command.  If you set the {Nfreq}\r setting for this command and the \"fix_balance\"_fix_balance.html\r command to the same value, then both operations will occur on the same\r timesteps.  However, the fix balance command needs to appear in the\r input script after the fix adapt command, if you want balancing to\r occur after adaptation (whether the Nfreq values are the same or not).\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global scalar which is a flag for whether any grid\r cells were adapted on the last timestep it was invoked.  The value of\r the flag is 1 if any cells were refined or coarsened, else it is 0.\r \r This fix also computes a global vector of length 2.  The first value\r is the number of cells which were refined.  The second is the number\r which were coarsened.  Both on the last timestep the fix was invoked.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_adapt.txt"
    },
    {
      "family": "fix",
      "style": "ambipolar",
      "description": "Enable the ambipolar approximation to be used in a simulation.  The\r ambipolar approximation is a computationally efficient way to model\r low-density plasmas which contain positively-charged ions and\r negatively-charged electrons.  In this model, electrons are not free\r particles which move independently.  This would require a simulation\r with a very small timestep due to electon's small mass and high speed\r (1000x that of an ion or neutral particle).\r \r Instead each ambipolar electron is assumed to stay \"close\" to its\r parent ion, so that the plasma gas appears macroscopically neutral.\r Each pair of particles thus moves together through the simulation\r domain, as if they were a single particle, which is how they are\r stored within SPARTA.  This means a normal timestep can be used.\r \r An overview of how to run simulations with the ambipolar approximation\r is given in the \"Section 6.11\"_Section_howto.html#howto_11.  This\r includes gas-phase collisions and chemistry as well as surface\r chemistry when particles collide with surface elements or the global\r boundary of the simulation box.  The section also lists all the\r commands that can be used in an input script to invoke various options\r associated with the ambipolar approximation.  All of them depend on\r this fix ambipolar command being defined.\r \r This command defines {especies} which is the species ID associated\r with the ambipolar electrons.  It also specifies one or more species\r IDs as {ion1}, {ion2}, etc for ambipolar ions.  SPARTA checks that the\r especies has a negative charge (as read in by the\r \"species\"_species.html command), and the ions have positive charges.\r An error is flagged if that is not the case.\r \r Internally, this fix defines two custom particle attributes.  The\r first is named \"ionambi\" and is an integer vector (one integer per\r particle).  It stores a value of 1 for ambipolar ions, or 0 otherwise.\r The second is named \"velambi\" and is a floating-point array (3 values\r per particle).  It stores the velocity of the ambipolar electron\r associated with the ambipolar ion if it exists, or zeroes otherwise.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r However, the values of the two custom particle attributes defined by\r this fix are written to the restart file.  Namely the integer value\r \"ionambi\" and floating-point velocity values \"velambi\" for each\r particle.  As explained on the \"read_restart\"_read_restart.html doc\r page these values will be re-assigned to particles when a restart file\r is read.  If a new fix ambipolar command is specified in the restart\r script before the first \"run\"_run.html command is used, then the\r ambipolar model will continue to be used in the continued run.\r \r No global or per-particle or per-grid quantities are stored by this\r fix for access by various output commands.\r \r However, the two custom per-particle attributes defined by this fix\r can be accessed by the \"dump particle\"_dump.html command, as p_ionambi\r and p_velambi.  That means those per-particle values can be written to\r particle dump files.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_ambipolar.txt"
    },
    {
      "family": "fix",
      "style": "ave/grid",
      "description": "Use one or more per-grid vectors as inputs every few timesteps, and\r average by grid cell over longer timescales, applying appropriate\r normalization factors.  The resulting per grid cell averages can be\r used by other output commands such as the \"dump grid\"_dump.html\r command.  Only grid cells in the grid group specified by {group-ID}\r are included in the averaging.  See the \"group grid\"_group.html\r command for info on how grid cells can be assigned to grid\r groups.\r \r Each input value can be the result of a \"compute\"_compute.html or\r \"fix\"_fix.html or \"grid-style variable\"_variable.html.  The compute or\r fix must produce a per-grid vector or array, not a global or\r per-particle or per-surf quantity.  If you wish to time-average global\r quantities from a compute, fix, or variable, then see the \"fix\r ave/time\"_fix_ave_time.html command.  To time-average per-surf\r quantities, see the \"fix ave/surf\"_fix_ave_surf.html command.\r \r Each per-grid value of each input vector is averaged independently.\r \r \"Computes\"_compute.html that produce per-grid vectors or arrays are\r those which have the word {grid} in their style name.  See the doc\r pages for individual \"fixes\"_fix.html to determine which ones produce\r per-grid vectors or arrays. \r \r Note that for values from a compute or fix or custom attribute, the\r bracketed index can be specified using a wildcard asterisk with the\r index to effectively specify multiple values.  This takes the form \"*\"\r or \"*n\" or \"n*\" or \"m*n\".  If N = the size of the vector (for {mode} =\r scalar) or the number of columns in the array (for {mode} = vector),\r then an asterisk with no numeric values means all indices from 1 to N.\r A leading asterisk means all indices from 1 to n (inclusive).  A\r trailing asterisk means all indices from n to N (inclusive).  A middle\r asterisk means all indices from m to n (inclusive).\r \r Using a wildcard is the same as if the individual columns of the array\r had been listed one by one.  E.g. these 2 fix ave/grid commands are\r equivalent, since the \"compute grid\"_compute_grid.html command creates\r a per-grid array with 3 columns:\r \r compute myGrid all all u v w\r fix 1 ave/grid all 10 20 1000 c_myGrid\\[*\\]\r fix 1 ave/grid all 10 20 1000 c_myGrid\\[1\\] c_myGrid\\[2\\] c_myGrid\\[3\\] :pre\r \r :line\r \r The {Nevery}, {Nrepeat}, and {Nfreq} arguments specify on what\r timesteps the input values will be used in order to contribute to the\r average.  The final averaged quantities are generated on timesteps\r that are a multiple of {Nfreq}.  The average is over {Nrepeat}\r quantities, computed in the preceding portion of the simulation every\r {Nevery} timesteps.  {Nfreq} must be a multiple of {Nevery} and\r {Nevery} must be non-zero even if {Nrepeat} is 1.  Also, the timesteps\r contributing to the average value cannot overlap, i.e. Nfreq >\r (Nrepeat-1)*Nevery is required.\r \r For example, if Nevery=2, Nrepeat=6, and Nfreq=100, then values on\r timesteps 90,92,94,96,98,100 will be used to compute the final average\r on timestep 100.  Similarly for timesteps 190,192,194,196,198,200 on\r timestep 200, etc.\r \r :line\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  If no bracketed term is\r appended, the compute must calculate a per-grid vector.  If\r {c_ID\\[N\\]} is used, the compute must calculate a per-grud array with\r M columns and N must be in the range from 1-M, which will use the Nth\r column of the M-column per-grid array.  See the discussion above for\r how N can be specified with a wildcard asterisk to effectively specify\r multiple values.\r \r Users can also write code for their own compute styles and \"add them\r to SPARTA\"_Section_modify.html.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  If no bracketed term is\r appended, the fix must calculates a per-grid vector.  If {f_ID\\[N\\]}\r is used, the fix must calculate a per-grid array with M columns and N\r must be in the range from 1-M, which will use the Nth column of the\r M-column per-grid array.  See the discussion above for how N can be\r specified with a wildcard asterisk to effectively specify multiple\r values.\r \r Note that some fixes only produce their values on certain timesteps,\r which must be compatible with {Nevery}, else an error will result.\r Users can also write code for their own fix styles and \"add them to\r SPARTA\"_Section_modify.html.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  Only grid-style\r variables can be referenced.  See the \"variable\"_variable.html command\r for details.  Note that grid-style variables define a formula which\r can reference \"stats_style\"_stats_style.html keywords, or they can\r invoke other computes, fixes, or variables when they are evaluated, so\r this is a very general means of specifying quantities to time average.\r \r If a value begins with \"g_\", the name of a custom per-grid vector or\r array must follow.  Custom attributes can store either a single or\r multiple values per grid cell.  See \"Section\r 6.17\"_Section_howto.html#howto_17 for more discussion of custom\r attributes and command that define them.  For example, the\r \"read_grid\"_read_grid.html and \"surf_react\r implicit\"_surf_react_implicit.html commands can define per-grid\r attributes.  (The surf/react implicit command has not yet been\r released in public SPARTA).\r \r If {g_name} is used as a value, the custom attribute must be a vector.\r If {g_name\\[N\\]} is used, the custom attribute must be an array, and N\r must be in the range from 1-M for an M-column array.  See the\r discussion above for how N can be specified with a wildcard asterisk\r to effectively specify multiple values.\r \r :line\r \r For averaging of a value that comes from a compute or fix,\r normalization is performed as follows.  Note that no normalization is\r performed on a value produced by a grid-style variable.\r \r If the compute or fix is summing over particles in a grid cell to\r calculate a per-grid quantity (e.g. energy or temperature), this takes\r the form of a numerator divided by a denominator.  For example, see\r the formulas discussed on the \"compute grid\"_compute_grid.html doc\r page, where the denominator is 1 (for keyword n), or the number of\r particles (ke, mass, temp), or the sum of particle masses (u, usq,\r etc).  When this command averages over a series of timesteps, the\r numerator and denominator are summed separately.  This means the\r numerator/denominator division only takes place when this fix produces\r output, every Nfreq timesteps.\r \r For example, say the Nfreq output is over 2 timesteps, and the value\r produced by \"compute grid mass\"_compute_grid.html is being averaged.\r Say a grid cell has 10 particles on the 1st timestep with a numerator\r value of 10.0, and 100 particles on the 2nd timestep with a numerator\r value of 50.0.  The output of this fix will be (10+50) / (10+100) =\r 0.54, not ((10/10) + (50/100)) / 2 = 0.75.\r \r :line\r \r Additional optional keywords also affect the operation of this fix. \r \r The {ave} keyword determines what happens to the accumulation of\r statistics every {Nfreq} timesteps.\r \r If the {ave} setting is {one}, then the values produced on timesteps\r that are multiples of Nfreq are independent of each other.\r Normalization as described above is performed, and all tallies are\r zeroed before accumulating over the next {Nfreq} steps.\r \r If the {ave} setting is {running}, then tallies are never zeroed.\r Thus the output at any {Nfreq} timestep is normalized over all\r previously accumulated samples since the fix was defined.  The tallies\r can only be zeroed by deleting the fix via the unfix command, or by\r re-defining the fix, or by re-specifying it.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces a per-grid vector or array which can be accessed by\r various output commands.  A vector is produced if only a single\r quantity is averaged by this fix.  If two or more quantities are\r averaged, then an array of values is produced, where the number of\r columns is the number of quantities averaged.  The per-grid values can\r only be accessed on timesteps that are multiples of {Nfreq} since that\r is when averaging is performed.\r \r This fix performs averaging for all child grid cells in the\r simulation, which includes unsplit, split, and sub cells.  \"Section\r 4.8\"_Section_howto.html#howto_ of the manual gives details of how\r SPARTA defines child, unsplit, split, and sub cells.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_ave_grid.txt"
    },
    {
      "family": "fix",
      "style": "ave/histo",
      "description": "Use one or more values as inputs every few timesteps to create a\r single histogram.  The histogram can then be averaged over longer\r timescales.  The resulting histogram can be used by other \"output\r commands\"_Section_howto.html#howto_15, and can also be written to a\r file.  The fix ave/histo/weight command has identical syntax to fix\r ave/histo, except that exactly two values must be specified.  See\r details below.\r \r A histogram is simply a count of the number of values that fall within\r a histogram bin.  {Nbins} are defined, with even spacing between {lo}\r and {hi}.  Values that fall outside the lo/hi bounds can be treated in\r different ways; see the discussion of the {beyond} keyword below.\r \r Each input value can be a particle attribute (position, velocity), or\r can be the result of a \"compute\"_compute.html or \"fix\"_fix.html that\r produces global or per-particle or per-grid quantities, or the\r evaluation of an equal-style or particle-style or grid-style\r \"variable\"_variable.html.  The set of input values can be either all\r global, all per-particle, or all per-grid quantities.  Inputs of\r different kinds (e.g. global and per-particle) cannot be mixed.\r Particle attributes are per-particle vector values.  See the doc page\r for individual \"compute\" and \"fix\" commands to see what kinds of\r quantities they generate.\r \r The input values must either be all scalars or all vectors (or\r arrays), depending on the setting of the {mode} keyword.\r \r Note that the output of this command is a single histogram for all\r input values combined together, not one histogram per input value.\r See below for details on the format of the output of this fix.\r \r If {mode} = scalar, then the input values must be scalars, or vectors\r with a bracketed term appended, indicating the Ith value of the vector\r is used.\r \r If {mode} = vector, then the input values must be vectors, or arrays\r with a bracketed term appended, indicating the Ith column of the array\r is used. \r \r Note that for values from a compute or fix, the bracketed index I can\r be specified using a wildcard asterisk with the index to effectively\r specify multiple values.  This takes the form \"*\" or \"*n\" or \"n*\" or\r \"m*n\".  If N = the size of the vector (for {mode} = scalar) or the\r number of columns in the array (for {mode} = vector), then an asterisk\r with no numeric values means all indices from 1 to N.  A leading\r asterisk means all indices from 1 to n (inclusive).  A trailing\r asterisk means all indices from n to N (inclusive).  A middle asterisk\r means all indices from m to n (inclusive).\r \r Using a wildcard is the same as if the individual elements of the\r vector or columns of the array had been listed one by one.  E.g. these\r 2 fix ave/histo commands are equivalent, since the \"compute\r grid\"_compute_com_chunk.html command creates a per-grid array with 3\r columns:\r \r compute myGrid grid all all u v w\r fix 1 ave/histo 100 1 100 c_myGrid file tmp1.grid mode vector\r fix 2 ave/histo 100 1 100 c_myGrid\\[1\\] c_myGrid\\[2\\] c_myGrid\\[3\\] file tmp2.grid mode vector :pre\r \r If the fix ave/histo/weight command is used, exactly two values must\r be specified.  If the values are vectors, they must be the same\r length.  The first value (a scalar or vector) is what is histogrammed\r into bins, in the same manner the fix ave/histo command operates.  The\r second value (a scalar or vector) is used as a \"weight\".  This means\r that instead of each value tallying a \"1\" to its bin, the\r corresponding weight is tallied.  E.g. the Nth entry in the first\r vector tallies the Nth entry (weight) in the second vector.\r \r :line\r \r The {Nevery}, {Nrepeat}, and {Nfreq} arguments specify on what\r timesteps the input values will be used in order to contribute to the\r histogram.  The final histogram is generated on timesteps that are\r multiple of {Nfreq}.  It is averaged over {Nrepeat} histograms,\r computed in the preceding portion of the simulation every {Nevery}\r timesteps.  {Nfreq} must be a multiple of {Nevery} and {Nevery} must\r be non-zero even if {Nrepeat} is 1.  Also, the timesteps\r contributing to the histogram value cannot overlap, \r i.e. Nrepeat*Nevery can not exceed Nfreq. \r \r For example, if Nevery=2, Nrepeat=6, and Nfreq=100, then input values\r on timesteps 90,92,94,96,98,100 will be used to compute the final\r histogram on timestep 100.  Similarly for timesteps\r 190,192,194,196,198,200 on timestep 200, etc.  If Nrepeat=1 and Nfreq\r = 100, then no time averaging of the histogram is done; a histogram is\r simply generated on timesteps 100,200,etc.\r \r :line\r \r The particle attribute values (x,y,z,vx,vy,vz) are self-explanatory.\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  If {mode} = scalar, then if\r no bracketed term is appended, the global scalar calculated by the\r compute is used.  If a bracketed term is appended, the Ith element of\r the global vector calculated by the compute is used.  If {mode} =\r vector, then if no bracketed term is appended, the global or per-atom\r or local vector calculated by the compute is used.  If a bracketed\r term is appended, the Ith column of the global or per-particle or\r per-grid array calculated by the compute is used.  See the discussion\r above for how I can be specified with a wildcard asterisk to\r effectively specify multiple values.\r \r Note that there is a \"compute reduce\"_compute_reduce.html command\r which can sum per-particle or per-grid or per-surf quantities into a\r global scalar or vector which can thus be accessed by fix ave/histo.\r Users can also write code for their own compute styles and \"add them\r to SPARTA\"_Section_modify.html.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  If {mode} = scalar, then if\r no bracketed term is appended, the global scalar calculated by the fix\r is used.  If a bracketed term is appended, the Ith element of the\r global vector calculated by the fix is used.  If {mode} = vector, then\r if no bracketed term is appended, the global or per-atom or local\r vector calculated by the fix is used.  If a bracketed term is\r appended, the Ith column of the global or per-particle or per-grid\r array calculated by the fix is used.  See the discussion above for how\r I can be specified with a wildcard asterisk to effectively specify\r multiple values.\r \r Note that some fixes only produce their values on certain timesteps,\r which must be compatible with {Nevery}, else an error will result.\r Users can also write code for their own fix styles and \"add them to\r SPARTA\"_Section_modify.html.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  If {mode} = scalar, then\r only equal-style variables can be used.  If {mode} = vector, then only\r particle-style or grid-style variables can be used, which produce\r per-particle per-grid vectors respectively.  See the\r \"variable\"_variable.html command for details.\r \r Note that variables of style {equal}, {particle}, and {grid} define a\r formula which can reference individual particle properties or stats\r output keywords, or they can invoke other computes, fixes, or\r variables when they are evaluated, so this is a very general means of\r specifying quantities to histogram.\r \r :line\r \r Additional optional keywords also affect the operation of this fix.\r \r If the {mode} keyword is set to {scalar}, then all input values must\r be global scalars, or elements of global vectors.  If the {mode}\r keyword is set to {vector}, then all input values must be global or\r per-particle or per-grid vectors, or columns of global or per-particle\r or per-grid arrays.\r \r The {file} keyword allows a filename to be specified.  Every {Nfreq}\r steps, one histogram is written to the file.  This includes a leading\r line that contains the timestep, number of bins, the total count of\r values contributing to the histogram, the count of values that were\r not histogrammed (see the {beyond} keyword), the minimum value\r encountered, and the maximum value encountered.  The min/max values\r include values that were not histogrammed.  Following the leading\r line, one line per bin is written into the file.  Each line contains\r the bin #, the coordinate for the center of the bin (between {lo} and\r {hi}), the count of values in the bin, and the normalized count.  The\r normalized count is the bin count divided by the total count (not\r including values not histogrammed), so that the normalized values sum\r to 1.0 across all bins.\r \r The {region}, {mix}, and {group} keywords limit which particles\r or grid cells are included in the histogramming.\r \r The {region} keyword only applies to per-particle histogramming.  Only\r particles in the specified {region-ID} are included in the histogram.\r See the \"region\"_region.html command for details of how geometric\r regions are defined.\r \r The {mix} keyword only applies to per-particle histogramming.  Only\r particles whose species are in the specified {mixture-ID} are included\r in the histogram, which allows for only a subset of species to be\r included.  See the \"mixture\"_mixture.html command for details of how\r mixtures are defined.\r \r The {group} keyword only applies to per-grid cell histogramming.  Only\r grid cells in the grid group specified by {group-ID} are included in\r the histogram.  See the \"grid group\"_group.html command for details of\r how grid groups are defined.\r \r The {ave} keyword determines how the histogram produced every {Nfreq}\r steps are averaged with histograms produced on previous steps that\r were multiples of {Nfreq}, before they are accessed by another output\r command or written to a file.\r \r If the {ave} setting is {one}, then the histograms produced on\r timesteps that are multiples of {Nfreq} are independent of each other;\r they are output as-is without further averaging.\r \r If the {ave} setting is {running}, then the histograms produced on\r timesteps that are multiples of {Nfreq} are summed and averaged in a\r cumulative sense before being output.  Each bin value in the histogram\r is thus the average of the bin value produced on that timestep with\r all preceding values for the same bin.  This running average begins\r when the fix is defined; it can only be restarted by deleting the fix\r via the \"unfix\"_unfix.html command, or by re-defining the fix by\r re-specifying it.\r \r If the {ave} setting is {window}, then the histograms produced on\r timesteps that are multiples of {Nfreq} are summed within a moving\r \"window\" of time, so that the last M histograms are used to produce\r the output.  E.g. if M = 3 and Nfreq = 1000, then the output on step\r 10000 will be the combined histogram of the individual histograms on\r steps 8000,9000,10000.  Outputs on early steps will be sums over less\r than M histograms if they are not available.\r \r The {start} keyword specifies what timestep histogramming will begin\r on.  The default is step 0.  Often input values can be 0.0 at time 0,\r so setting {start} to a larger value can avoid including a 0.0 in\r a running or windowed histogram.\r \r The {beyond} keyword determines how input values that fall outside the\r {lo} to {hi} bounds are treated.  Values such that {lo} <= value <=\r {hi} are assigned to one bin.  Values on a bin boundary are assigned\r to the lower of the 2 bins.  If {beyond} is set to {ignore} then\r values < {lo} and values > {hi} are ignored, i.e. they are not binned.\r If {beyond} is set to {end} then values < {lo} are counted in the\r first bin and values > {hi} are counted in the last bin.  If {beyond}\r is set to {extend} then two extra bins are created, so that there are\r Nbins+2 total bins.  Values < {lo} are counted in the first bin and\r values > {hi} are counted in the last bin (Nbins+1).  Values between\r {lo} and {hi} (inclusive) are counted in bins 2 thru Nbins+1.  The\r \"coordinate\" stored and printed for these two extra bins is {lo} and\r {hi}.\r \r The {overwrite} keyword will continuously overwrite the output file\r with the latest output, so that it only contains one timestep worth of\r output.  This option can only be used with the {ave running} setting.\r \r The {title1} and {title2} and {title3} keywords allow specification of\r the strings that will be printed as the first 3 lines of the output\r file, assuming the {file} keyword was used.  SPARTA uses default\r values for each of these, so they do not need to be specified.\r \r By default, these header lines are as follows:\r \r # Histogram for fix ID\r # TimeStep Number-of-bins Total-counts Missing-counts Min-value Max-value\r # Bin Coord Count Count/Total :pre\r \r In the first line, ID is replaced with the fix-ID.  The second line\r describes the six values that are printed at the first of each section\r of output.  The third describes the 4 values printed for each bin in\r the histogram.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces a global vector and global array which can be\r accessed by various output commands.  The values can only be accessed\r on timesteps that are multiples of {Nfreq} since that is when a\r histogram is generated.  The global vector has 4 values:\r \r 1 = total counts in the histogram\r 2 = values that were not histogrammed (see {beyond} keyword)\r 3 = min value of all input values, including ones not histogrammed\r 4 = max value of all input values, including ones not histogrammed :ul\r \r The global array has # of rows = Nbins and # of columns = 3.  The\r first column has the bin coordinate, the 2nd column has the count of\r values in that histogram bin, and the 3rd column has the bin count\r divided by the total count (not including missing counts), so that the\r values in the 3rd column sum to 1.0.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_ave_histo.txt"
    },
    {
      "family": "fix",
      "style": "ave/histo/weight",
      "docFile": ""
    },
    {
      "family": "fix",
      "style": "ave/surf",
      "description": "Use one or more per-surf vectors as inputs every few timesteps, and\r average them surface element by surface element by over longer\r timescales, applying appropriate normalization factors. The resulting\r per-surf averages can be used by other output commands such as the\r \"dump surf\"_dump.html command.  Only surface elements in the surface\r group specified by {group-ID} are included in the averaging.  See the\r \"group surf\"_group.html command for info on how surface elements can\r be assigned to surface groups.\r \r Each input value can be the result of a \"compute\"_compute.html or\r \"fix\"_fix.html or \"surf-style variable\"_variable.html or a custom\r per-surf attribute..  The compute or fix must produce a per-surf\r vector or array, not a global or per-particle or per-grid quantity.\r If you wish to time-average global quantities from a compute or fix\r then see the \"fix ave/time\"_fix_ave_time.html command.  To\r time-average per-grid quantities, see the \"fix\r ave/grid\"_fix_ave_surf.html command.\r \r Each per-surf value of each input vector is averaged independently.\r \r \"Computes\"_compute.html that produce per-surf vectors or arrays are\r those which have the word {surf} in their style name.  See the doc\r pages for individual \"fixes\"_fix.html to determine which ones produce\r per-surf vectors or arrays.\r \r Note that for values from a compute or fix or custom attribute, the\r bracketed index can be specified using a wildcard asterisk with the\r index to effectively specify multiple values.  This takes the form \"*\"\r or \"*n\" or \"n*\" or \"m*n\".  If N = the size of the vector (for {mode} =\r scalar) or the number of columns in the array (for {mode} = vector),\r then an asterisk with no numeric values means all indices from 1 to N.\r A leading asterisk means all indices from 1 to n (inclusive).  A\r trailing asterisk means all indices from n to N (inclusive).  A middle\r asterisk means all indices from m to n (inclusive).\r \r Using a wildcard is the same as if the individual columns of the array\r had been listed one by one.  E.g. these 2 fix ave/surf commands are\r equivalent, since the \"compute surf\"_compute_grid.html command creates\r a per-surf array with 4 columns:\r \r compute mySurf all all n fx fy fz\r fix 1 ave/surf all 10 20 1000 c_mySurf\\[*\\]\r fix 1 ave/surf all 10 20 1000 c_mySurf\\[1\\] c_mySurf\\[2\\] &\r                               c_mySurf\\[3\\] c_mySurf\\[4\\] :pre\r \r :line\r \r The {Nevery}, {Nrepeat}, and {Nfreq} arguments specify on what\r timesteps the input values will be used in order to contribute to the\r average.  The final averaged quantities are generated on timesteps\r that are a multiple of {Nfreq}.  The average is over {Nrepeat}\r quantities, computed in the preceding portion of the simulation every\r {Nevery} timesteps.  {Nfreq} must be a multiple of {Nevery} and\r {Nevery} must be non-zero even if {Nrepeat} is 1.  Also, the timesteps\r contributing to the average value cannot overlap, i.e. Nfreq >\r (Nrepeat-1)*Nevery is required.\r \r For example, if Nevery=2, Nrepeat=6, and Nfreq=100, then values on\r timesteps 90,92,94,96,98,100 will be used to compute the final average\r on timestep 100.  Similarly for timesteps 190,192,194,196,198,200 on\r timestep 200, etc.\r \r :line\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  If no bracketed term is\r appended, the compute must calculate a per-surf vector.  If\r {c_ID\\[N\\]} is used, the compute must calculate a per-surf array with\r M columns and N must be in the range from 1-M, which will use the Nth\r column of the M-column per-surf array.  See the discussion above for\r how N can be specified with a wildcard asterisk to effectively specify\r multiple values.\r \r Users can also write code for their own compute styles and \"add them\r to SPARTA\"_Section_modify.html.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  If no bracketed term is\r appended, the fix must calculates a per-surf vector.  If {f_ID\\[N\\]}\r is used, the fix must calculate a per-surf array with M columns and N\r must be in the range from 1-M, which will use the Nth column of the\r M-column per-surf array.  See the discussion above for how N can be\r specified with a wildcard asterisk to effectively specify multiple\r values.\r \r Note that some fixes only produce their values on certain timesteps,\r which must be compatible with {Nevery}, else an error will result.\r Users can also write code for their own fix styles and \"add them to\r SPARTA\"_Section_modify.html.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  Only surf-style\r variables can be referenced.  See the \"variable\"_variable.html command\r for details.  Note that surf-style variables define a formula which\r can reference \"stats_style\"_stats_style.html keywords, or they can\r invoke other computes, fixes, or variables when they are evaluated, so\r this is a very general means of specifying quantities to time average.\r \r If a value begins with \"s_\", the name of a custom per-surf vector or\r array must follow.  Custom attributes can store either a single or\r multiple values per surface element.  See \"Section\r 6.17\"_Section_howto.html#howto_17 for more discussion of custom\r attributes and command that define them.  For example, the\r \"read_surf\"_read_surf.html, \"fix surf/temp\"_fix_surf_temp.html, and\r \"surf_react adsorb\"_surf_react_adsorb.html commands can define\r per-surf attributes.\r \r If {s_name} is used as a value, the custom attribute must be a vector.\r If {s_name\\[N\\]} is used, the custom attribute must be an array, and N\r must be in the range from 1-M for an M-column array.  See the\r discussion above for how N can be specified with a wildcard asterisk\r to effectively specify multiple values.\r \r :line\r \r For averaging of a value that comes from a compute or fix,\r normalization is performed as follows.  Note that no normalization is\r performed on a value produced by a surf-style variable.\r \r If the compute or fix is summing over particles to calculate a\r per-surf quantity (e.g. pressure or energy flux), this takes the form\r of a numerator divided by a denominator.  For example, see the\r formulas discussed on the \"compute surf\"_compute_surf.html doc page,\r where the denominator is 1 (for keyword n), area times dt (timestep)\r for the other quantities (press, shx, ke, etc).  When this command\r averages over a series of timesteps, the numerator and denominator are\r summed separately.  This means the numerator/denominator division only\r takes place when this fix produces output, every Nfreq timesteps.\r \r :line\r \r Additional optional keywords also affect the operation of this fix. \r \r The {ave} keyword determines what happens to the accumulation of\r statistics every {Nfreq} timesteps.\r \r If the {ave} setting is {one}, then the values produced on timesteps\r that are multiples of Nfreq are independent of each other.\r Normalization as described above is performed, and all tallies are\r zeroed before accumulating over the next {Nfreq} steps.\r \r If the {ave} setting is {running}, then tallies are never zeroed.\r Thus the output at any {Nfreq} timestep is normalized over all\r previously accumulated samples since the fix was defined.  The tallies\r can only be zeroed by deleting the fix via the unfix command, or by\r re-defining the fix, or by re-specifying it.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces a per-surf vector or array which can be accessed by\r various output commands.  A vector is produced if only a single\r quantity is averaged by this fix.  If two or more quantities are\r averaged, then an array of values is produced, where the number of\r columns is the number of quantities averaged.  The per-surf values can\r only be accessed on timesteps that are multiples of {Nfreq} since that\r is when averaging is performed.\r \r Surface elements not in the specified {group-ID} will output zeroes\r for all their values.",
      "docFile": "fix_ave_surf.txt"
    },
    {
      "family": "fix",
      "style": "ave/time",
      "description": "Use one or more global values as inputs every few timesteps, and\r average them over longer timescales.  The resulting averages can be\r used by other output commands such as \"stats_style\r custom\"_stats_style.html, and can also be written to a file.  Note\r that if no time averaging is done, this command can be used as a\r convenient way to simply output one or more global values to a file.\r \r Each listed value can be the result of a \"compute\"_compute.html or\r \"fix\"_fix.html or the evaluation of an equal-style\r \"variable\"_variable.html.  In each case, the compute, fix, or variable\r must produce a global quantity, not a per-grid or per-surf quantity.\r If you wish to time-average those quantities, see the \"fix\r ave/grid\"_fix_ave_grid.html and \"fix ave/surf\"_fix_ave_surf.html\r commands.\r \r \"Computes\"_compute.html that produce global quantities are those which\r do not have the word {particle} or {grid} or {surf} in their style\r name.  Only a few \"fixes\"_fix.html produce global quantities.  See the\r doc pages for individual fixes for info on which ones produce such\r values.  \"Variables\"_variable.html of style {equal} are the only ones\r that can be used with this fix.  Variables of style {particle} cannot\r be used, since they produce per-particle values.\r \r The input values must either be all scalars or all vectors (or\r arrays), depending on the setting of the {mode} keyword.  In both\r cases, the averaging is performed independently on each input value.\r I.e. each input scalar is averaged independently and each element of\r each input vector (or array) is averaged independently.\r \r If {mode} = scalar, then the input values must be scalars, or vectors\r with a bracketed term appended, indicating the Ith value of the vector\r is used.\r \r If {mode} = vector, then the input values must be vectors, or arrays\r with a bracketed term appended, indicating the Ith column of the array\r is used.  All vectors must be the same length, which is the length of\r the vector or number of rows in the array.\r \r Note that for values from a compute or fix, the bracketed index I can\r be specified using a wildcard asterisk with the index to effectively\r specify multiple values.  This takes the form \"*\" or \"*n\" or \"n*\" or\r \"m*n\".  If N = the size of the vector (for {mode} = scalar) or the\r number of columns in the array (for {mode} = vector), then an asterisk\r with no numeric values means all indices from 1 to N.  A leading\r asterisk means all indices from 1 to n (inclusive).  A trailing\r asterisk means all indices from n to N (inclusive).  A middle asterisk\r means all indices from m to n (inclusive).\r \r Using a wildcard is the same as if the individual elements of the\r vector or columns of the array had been listed one by one.  E.g. these\r 2 fix ave/time commands are equivalent, since the \"compute\r count\"_compute_count.html command creates, in this case, a global\r vector with 3 values.\r \r compute 1 count Ar He O\r fix 1 ave/time 100 1 100 c_1 file tmp.count\r fix 1 ave/time 100 1 100 c_1\\[1\\] c_1\\[2\\] c_1\\[3\\] file tmp.count :pre\r \r :line\r \r The {Nevery}, {Nrepeat}, and {Nfreq} arguments specify on what\r timesteps the input values will be used in order to contribute to the\r average.  The final averaged quantities are generated on timesteps\r that are a mlutiple of {Nfreq}.  The average is over {Nrepeat}\r quantities, computed in the preceding portion of the simulation every\r {Nevery} timesteps.  {Nfreq} must be a multiple of {Nevery} and\r {Nevery} must be non-zero even if {Nrepeat} is 1.  Also, the timesteps\r contributing to the average value cannot overlap, i.e. Nfreq >\r (Nrepeat-1)*Nevery is required.\r \r For example, if Nevery=2, Nrepeat=6, and Nfreq=100, then values on\r timesteps 90,92,94,96,98,100 will be used to compute the final average\r on timestep 100.  Similarly for timesteps 190,192,194,196,198,200 on\r timestep 200, etc.  If Nrepeat=1 and Nfreq = 100, then no time\r averaging is done; values are simply generated on timesteps\r 100,200,etc.\r \r :line\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  If {mode} = scalar, then if\r no bracketed term is appended, the global scalar calculated by the\r compute is used.  If a bracketed term is appended, the Ith element of\r the global vector calculated by the compute is used.  If {mode} =\r vector, then if no bracketed term is appended, the global vector\r calculated by the compute is used.  If a bracketed term is appended,\r the Ith column of the global array calculated by the compute is used.\r See the discussion above for how I can be specified with a wildcard\r asterisk to effectively specify multiple values.\r \r Note that there is a \"compute reduce\"_compute_reduce.html command\r which can sum per-particle or per-grid or per-surf quantities into a\r global scalar or vector which can thus be accessed by fix ave/time.\r Also Note that users can also write code for their own compute styles\r and \"add them to SPARTA\"_Section_modify.html; their output can then be\r processed by this fix.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  If {mode} = scalar, then if\r no bracketed term is appended, the global scalar calculated by the fix\r is used.  If a bracketed term is appended, the Ith element of the\r global vector calculated by the fix is used.  If {mode} = vector, then\r if no bracketed term is appended, the global vector calculated by the\r fix is used.  If a bracketed term is appended, the Ith column of the\r global array calculated by the fix is used.  See the discussion above\r for how I can be specified with a wildcard asterisk to effectively\r specify multiple values.\r \r Note that some fixes only produce their values on certain timesteps,\r which must be compatible with {Nevery}, else an error will result.\r Users can also write code for their own fix styles and \"add them to\r SPARTA\"_Section_modify.html.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  Variables can only be\r used as input for {mode} = scalar.  Only equal-style variables can be\r referenced.  See the \"variable\"_variable.html command for details.\r Note that variables of style {equal} define a formula which can\r reference \"stats_style\"_stats_style.html keywords, or they can invoke\r other computes, fixes, or variables when they are evaluated, so this\r is a very general means of specifying quantities to time average.\r \r :line\r \r Additional optional keywords also affect the operation of this fix.\r \r If the {mode} keyword is set to {scalar}, then all input values must\r be global scalars, or elements of global vectors.  If the {mode}\r keyword is set to {vector}, then all input values must be global\r vectors, or columns of global arrays.  They can also be global arrays,\r which are converted into a series of global vectors (one per column),\r as explained above.\r \r The {ave} keyword determines how the values produced every {Nfreq}\r steps are averaged with values produced on previous steps that were\r multiples of {Nfreq}, before they are accessed by another output\r command or written to a file.\r \r If the {ave} setting is {one}, then the values produced on timesteps\r that are multiples of {Nfreq} are independent of each other; they are\r output as-is without further averaging.\r \r If the {ave} setting is {running}, then the values produced on\r timesteps that are multiples of {Nfreq} are summed and averaged in a\r cummulative sense before being output.  Each output value is thus the\r average of the value produced on that timestep with all preceding\r values.  This running average begins when the fix is defined; it can\r only be restarted by deleting the fix via the \"unfix\"_unfix.html\r command, or by re-defining the fix by re-specifying it.\r \r If the {ave} setting is {window}, then the values produced on\r timesteps that are multiples of {Nfreq} are summed and averaged within\r a moving \"window\" of time, so that the last M values are used to\r produce the output.  E.g. if M = 3 and Nfreq = 1000, then the output\r on step 10000 will be the average of the individual values on steps\r 8000,9000,10000.  Outputs on early steps will average over less than M\r values if they are not available.\r \r The {start} keyword specifies what timestep averaging will begin on.\r The default is step 0.  Often input values can be 0.0 at time 0, so\r setting {start} to a larger value can avoid including a 0.0 in a\r running or windowed average.\r \r The {off} keyword can be used to flag any of the input values.  If a\r value is flagged, it will not be time averaged.  Instead the most\r recent input value will always be stored and output.  This is useful\r if one of more of the inputs produced by a compute or fix or variable\r are effectively constant or are simply current values.  E.g. they are\r being written to a file with other time-averaged values for purposes\r of creating well-formatted output.\r \r The {file} keyword allows a filename to be specified.  Every {Nfreq}\r steps, one quantity or vector of quantities is written to the file for\r each input value specified in the fix ave/time command.  For {mode} =\r scalar, this means a single line is written each time output is\r performed.  Thus the file ends up to be a series of lines, i.e. one\r column of numbers for each input value.  For {mode} = vector, an array\r of numbers is written each time output is performed.  The number of\r rows is the length of the input vectors, and the number of columns is\r the number of values.  Thus the file ends up to be a series of these\r array sections.\r \r The {title1} and {title2} and {title3} keywords allow specification of\r the strings that will be printed as the first 2 or 3 lines of the\r output file, assuming the {file} keyword was used.  SPARTA uses\r default values for each of these, so they do not need to be specified.\r \r By default, these header lines are as follows for {mode} = scalar:\r \r # Time-averaged data for fix ID\r # TimeStep value1 value2 ... :pre\r \r In the first line, ID is replaced with the fix-ID.  In the second line\r the values are replaced with the appropriate fields from the fix\r ave/time command.  There is no third line in the header of the file,\r so the {title3} setting is ignored when {mode} = scalar.\r \r By default, these header lines are as follows for {mode} = vector:\r \r # Time-averaged data for fix ID\r # TimeStep Number-of-rows\r # Row value1 value2 ... :pre\r \r In the first line, ID is replaced with the fix-ID.  The second line\r describes the two values that are printed at the first of each section\r of output.  In the third line the values are replaced with the\r appropriate fields from the fix ave/time command.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces a global scalar or global vector or global array\r which can be accessed by various output commands.  The values can only\r be accessed on timesteps that are multiples of {Nfreq} since that is\r when averaging is performed.\r \r A scalar is produced if only a single input value is averaged and\r {mode} = scalar.  A vector is produced if multiple input values are\r averaged for {mode} = scalar, or a single input value for {mode} =\r vector.  In the first case, the length of the vector is the number of\r inputs.  In the second case, the length of the vector is the same as\r the length of the input vector.  An array is produced if multiple\r input values are averaged and {mode} = vector.  The global array has #\r of rows = length of the input vectors and # of columns = number of\r inputs.",
      "docFile": "fix_ave_time.txt"
    },
    {
      "family": "fix",
      "style": "balance",
      "description": "This command dynamically adjusts the assignment of grid cells and\r their particles to processors as a simulation runs, to attempt to\r balance the computational cost (load) evenly across processors.  The\r load balancing is \"dynamic\" in the sense that rebalancing is performed\r periodically during the simulation. To perform \"static\" balancing,\r before or between runs, see the \"balance_grid\"_balance_grid.html\r command.\r \r This command is useful to use during simulations where the spatial\r distribution of particles varies with time, leading to load imbalance.\r \r After grid cells have been assigned, they are migrated to new owning\r processors, along with any particles they own or other per-cell\r attributes stored by fixes.  The internal data structures within\r SPARTA for grid cells and particles are re-initialized with the new\r decomposition.\r \r The details of how child cells are assigned to processors by the\r various options of this command are described below.  The cells\r assigned to each processor will either be \"clumped\" or \"dispersed\".\r \r The {rcb} keyword will produce clumped assignments of child cells to\r each processor.  This means each processor's cells will be\r geometrically compact.  The {random} and {proc} keywords will produce\r dispersed assignments of child cells to each processor.\r \r IMPORTANT NOTE: See \"Section 6.8\"_Section_howto.html#howto_8 of the\r manual for an explanation of clumped and dispersed grid cell\r assignments and their relative performance trade-offs.\r \r :line\r \r Rebalancing is attempted by this command once every {Nfreq} timesteps,\r but only if the current imbalance factor exceeds the specified\r {thresh}.  This factor is defined as the maximum number of particles\r owned by any processor, divided by the average number of particles per\r processor.  Thus an imbalance factor of 1.0 is perfect balance.  For\r 10000 particles running on 10 processors, if the most heavily loaded\r processor has 1200 particles, then the factor is 1.2, meaning there is\r a 20% imbalance.  The {thresh} setting must be >= 1.0.\r \r IMPORTANT NOTE: This command attempts to minimize the imbalance\r factor, as defined above.  But computational cost is not strictly\r proportional to particle count, depending on the\r \"collision\"_collide.html and \"chemistry\"_react.html models being used.\r Also, changing the assignment of grid cells and particles to\r processors may lead to additional communication overheads, e.g.  when\r migrating particles between processors.  Thus you should benchmark the\r run times of your simulation to judge how often balancing should be\r performed, and how aggressively to set the {thresh} value.\r \r :line\r \r The {random} keyword means that each grid cell will be assigned\r randomly to one of the processors.  In this case every processor will\r typically not be assigned exactly the same number of grid cells.\r \r The {proc} keyword means that each processor will choose a random\r processor to assign its first grid cell to.  It will then loop over\r its grid cells and assign each to consecutive processors, wrapping\r around the collection of processors if necessary.  In this case every\r processor will typically not be assigned exactly the same number of\r grid cells.\r \r The {rcb} keyword uses a recurvise coordinate bisectioning (RCB)\r algorithm to assign spatially-compact clumps of grid cells to\r processors.  Each grid cell has a \"weight\" in this algorithm so that\r each processor is assigned an equal total weight of grid cells, as\r nearly as possible.\r \r If the {weight} argument is specified as {cell}, then the weight for\r each grid cell is 1.0, so that each processor will end up with an\r equal number of grid cells.\r \r If the {weight} argument is specified as {part}, than the weight for\r each grid cell is the number of particles it currently owns, so that\r each processor will end up with an equal number of particles.\r \r If the {weight} argument is specified as {time}, then timers are used\r to estimate the cost of each grid cell.  The cost from the timers is\r given on a per processor basis, and then assigned to grid cells by\r weighting by the relative number of particles in the grid cells. If no\r timing data has yet been collected at the point in a script where this\r command is issued, a {cell} style weight will be used instead of\r {time}.  A small warmup run (for example 100 timesteps) can be used\r before the balance command so that timer data is available. The number\r of timesteps {Nfreq} between balancing steps also needs to be large\r enough to give reliable timings. The timers used for balancing tally\r time from the move, sort, collide, and modify portions of each timestep.\r \r IMPORTANT NOTE: The \"adapt_grid\"_adapt_grid.html command zeros out\r timing data, so the weight {time} option is not available immediatly\r after this command.\r \r IMPORTANT NOTE: The coarsening option in \"fix_adapt\"_fix_adapt.html may\r shift cells to different processors, which makes the accumulated\r timing data for the weight {time} option less accurate when load\r balancing is performed immediately after this command.\r \r Here is an example of an RCB partitioning for 24 processors, of a 2d\r hierarchical grid with 5 levels, refined around a tilted ellipsoidal\r surface object (outlined in pink).  This is for a {weight cell}\r setting, yielding an equal number of grid cells per processor.  Each\r processor is assigned a different color of grid cells.  (Note that\r less colors than processors were used, so the disjoint yellow cells\r actually belong to three different processors).  This is an example of\r a clumped distribution where each processor's assigned cells can be\r compactly bounded by a rectangle.  Click for a larger version of the\r image.\r \r :c,image(JPG/partition_small.jpg,JPG/partition.jpg)\r \r :line\r \r The optional keywords {axes} and {flip} only apply to the {rcb}\r style.  Otherwise they are ignored.\r \r The {axes} keyword allows limiting the partitioning created by the RCB\r algorithm to a subset of dimensions.  The default is to allow cuts in\r all dimension, e.g. x,y,z for 3d simulations.  The dims value is a\r string with 1, 2, or 3 characters.  The characters must be one of \"x\",\r \"y\", or \"z\".  They can be in any order and must be unique.  For\r example, in 3d, a dims = xz would only partition the 3d grid only in\r the x and z dimensions.\r \r The {flip} keyword is useful for debugging.  If it is set to {yes}\r then each time an RCB partitioning is done, the coordinates of grid\r cells will (internally only) undergo a sign flip to insure that the\r new owner of each grid cell is a different processor than the previous\r owner, at least when more than a few processors are used.  This will\r insure all particle and grid data moves to new processors, fully\r exercising the rebalancing code.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global scalar which is the imbalance factor after\r the most recent rebalance.  It also computes a global vector of length\r 3 with additional information about the most recent rebalancing and\r the cummulative count of rebalancings.  The 3 values in the vector are\r as follows:\r \r 1 = max particle count on any processor after last rebalance\r 2 = imbalance factor before the last rebalance was performed\r 3 = cummulative count of rebalances since the fix was specified :ul\r \r As explained above, the imbalance factor is the ratio of the maximum\r number of particles on any processor to the average number of\r particles per processor. For the {rcb} style's {time} option, the\r imbalance factor after the most recent rebalance cannot be computed\r and 0.0 is returned for the global scalar value.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_balance.txt"
    },
    {
      "family": "fix",
      "style": "custom",
      "description": "Reset one or more custom attributes for particles, grid cells, or\r surface elements.  This operation is performed once every {Nfreq}\r timesteps.\r \r The syntax for {actions} and their arguments are the same as for the\r \"custom\"_custom.html command with two differences.\r \r First, only the {set}, {file}, and {file/coarse} actions are supported\r for this fix.  The {create} and {remove} actions are not, since they\r do not make sense to perform multiple times during a run.\r \r Second, the {file} and {file/coarse} actions take a filename as an\r argument.  The filename must contain a wildcard \"*\" character which\r will be replaced by the timestep.  This is to allow a sequence of\r files to be read during the simulation.  The filename wildcard \"*\"\r cannot be used with the \"custom\"_custom.html command.\r \r All of the actions supported by this command reset the values of\r custom attribute(s) which must already exist.  See the\r \"custom\"_custom.html and \"read_surf\"_read_surf.html and\r \"read_grid\"_read_grid.html commands for different ways to define and\r initialize custom attributes.\r \r Note that the resetting of attributes by this fix takes place at the\r end of timesteps determined by {Nfreq}.  If custom attributes are\r output by a \"dump\"_dump.html command on the same timestep, that takes\r place after this fix has performed the reset.  This means that if this\r fix and a dump command both reset/output every 1000 steps, then a dump\r file snapshot for step 3000 could include custom attributes which were\r reset at the end of step 3000.  But the new attribute values will not\r influence particles or grid cells or surface elements until the\r following step 3001 and beyond.\r \r Also note that this command DOES NOT reset attribute values at the\r beginning of any run, even if an input script contains multiple\r \"run\"_run.html commands.  For example, if {Nfreq} = 1000 and the input\r script performs a series of 10000-step runs, then no resetting of\r attributes takes place (due to this command) on timestep 0, before the\r first run.  Likewise, between runs, this command DOES reset attribute\r values at the end of the last step (10000, 20000, etc) of each run,\r but DOES NOT reset them again before the first step of the next run.\r \r IMPORTANT NOTE: If needed, resetting of custom values can be performed\r before the first run or between runs using the \"custom\"_custom.html\r command.\r \r IMPORTANT NOTE: If {Nfreq} triggers the reading of a file on the last\r step of the last run in the input script, then the\r wildcard-substituted filename (explained below) must exist.  This is\r the case, even though the new values will have no effect in the\r current input script, since there are no subsequent timestep\r operations which will use them.  Note, however that if a restart file\r is written on the last timestep, or following the last run, the\r updated custom values will be written to the restart file.\r \r :line",
      "docFile": "fix_custom.txt"
    },
    {
      "family": "fix",
      "style": "dt/reset",
      "description": "Calculate a new global timestep for the simulation based on per grid\r cell timesteps calculated by a compute or fix.  The new global\r timestep can be output by the \"stats_style\"_stats_style.html command.\r Or it can be used to overwrite the current global timestep for a\r variable time simulation.  See this\r \"section\"_Section_howto.html#howto_17 of the manual for more\r information on variable timestep simulations.\r \r The {Nfreq} argument specifies how often the global timestep is calculated.\r \r The {step} argument specifies a compute which calculates a per grid\r cell timestep.  Or it specifies a fix which time averages a per grid\r cell timestep.  Currently the only compute that calculates a per grid\r cell timestep is \"compute dt/grid\"_compute_dt_grid.html.  The \"fix\r ave/grid\"_fix_ave_grid.html command could perform a time average of\r the compute.\r \r This is done by specifying the {step} argument like this:\r \r c_ID = compute with ID that calculates a per grid cell timestep as a vector output\r c_ID\\[m\\] = compute with ID that calculates a timestep as its Mth column of array output\r f_ID\\[m\\] = fix with ID that calculates a time-averaged timestep as a vector output\r f_ID\\[m\\] = fix with ID that calculates a time-averaged timestep as its Mth column of array output :ul\r \r IMPORTANT NOTE: If the ID of a \"fix ave/grid\"_fix_ave_grid.html\r command is used as the {step} argument, it only produces output on\r timesteps that are multiples of its {Nfreq} argument.  Thus this fix\r can only be invoked on those timesteps.\r \r Note that some of the per-cell timesteps may be zero for several reasons.  First,\r data used to calculate the timestep, such as mean collision time, temperature, or particle speed, may be zero.\r Also, some cells may not contain particles, either due to their type or to local flow conditions.\r For example, split cells (in which sub cells store the particles) and cells interior to surface\r objects do not store particles.  See \"Section 6.8\"_Section_howto.html#howto_8 of the manual for\r details of how SPARTA defines child, unsplit, split, and sub cells.\r \r From the per-cell timesteps, 3 values are extracted by this fix.  They\r are the minimum positive timestep (DTmin) for all cells, the maximum positive timestep\r (DTmax) for all cells, and the average positive timestep (DTave) over all\r cells.  Cells with a timestep value of zero are not included in the mininum,\r maximum, and average timestep calculations.\r \r A new global timestep is than calculated by this formula, using\r the specified {weight} argument:\r \r DTnew = (1-weight)*DTmin + weight*DTave :pre\r \r If the {resetflag} argument is specified as 1, then the global\r timestep for the simulation, initially specified by the\r \"timestep\"_timestep.html command, is overwritten with the new DTnew\r value.  If {resetflag} is 0, then the global timestep is not changed.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global scalar which is the new global timestep\r (DTnew above) after the most recent timestep re-calculation.  This\r value is accessible to other commands whether or not the global\r timestep is overwritten with the new value.\r \r It also computes a global vector of length 3 with these values:\r \r 1 = DTmin\r 2 = DTmax\r 3 = DTave :ul",
      "docFile": "fix_dt_reset.txt"
    },
    {
      "family": "fix",
      "style": "emit/face",
      "description": "Emit particles from one or more faces of the simulation box,\r continuously during a simulation.  If invoked every timestep, this fix\r creates a continuous influx of particles thru the face(s).\r \r The properties of the added particles are determined by the mixture\r with ID {mix-ID}.  This sets the number and species of added\r particles, as well as their streaming velocity, thermal temperature,\r and internal energy modes.  The details are explained below.\r \r One or more faces of the simulation box can be specified via the\r {face1}, {face2}, etc arguments.  The 6 possible faces can be\r specified as {xlo}, {xhi}, {ylo}, {yhi}, {zlo}, or {zhi}.  Specifying\r {all} is the same as specifying all 6 individual faces.\r \r On each insertion timestep, each grid cell with one or more of its\r faces touching a specified boundary {face} performs the following\r computations to add particles.  The particles are added at the\r beginning of the SPARTA timestep.\r \r The molecular flux across a grid cell face per unit time is given by\r equation 4.22 of \"(Bird94)\"_#Bird94.  The number of particles {M} to\r insert on a particular grid cell face is based on this flux and\r additional global, flow, and cell face properties:\r \r global property: {fnum} ratio as specified by the \"global\"_global.html\" command\r flow properties: number density, streaming velocity, and thermal temperature\r cell face properties: area of face and its orientation relative to the streaming velocity :ul\r \r The flow properties are defined for the specified mixture via the\r \"mixture\"_mixture.html command.\r \r If {M} has a fractional value, e.g. 12.5, then 12 particles are added,\r and a 13th depending on the value of a random number.  Each particle\r is added at a random location on the grid cell face.  The particle\r species is chosen randomly in accord with the {frac} settings of the\r collection of species in the mixture, as set by the\r \"mixture\"_mixture.html command.\r \r IMPORTANT NOTE: The preceeding calculation is actually done using face\r areas associated with {weighted} cell volumes.  Grid cells can be\r weighted using the \"global weight\"_global.html command.\r \r The velocity of the particle is set to the sum of the streaming\r velocity and a thermal velocity sampled from the thermal temperature.\r The internal energy modes of the particle are determined by the {trot}\r and {tvib} settings of the mixture and the {rotate} and {vibrate}\r options of the \"collide_modify\"_collide_modify.html command.  Note\r that if the \"collide\"_collide.html command has not been specified\r (free molecular flow), then no rotational or vibrational energy will\r be assigned to created particles.\r \r If the final particle velocity is not directed \"into\" the grid cell,\r then the velocity sampling procedure is repeated until it is.  This\r insures that all added particles enter the simulation domain, as\r desired.\r \r The first timestep that added particles are advected, they move for a\r random fraction of the timestep.  This insures a continuous flow field\r of particles entering the simulation box.\r \r :line\r \r The {n} keyword can alter how many particles are added, which can be\r useful for debugging purposes.  If {Np} is set to 0, then the number\r of added particles is a function of {fnum}, {nrho}, and other mixture\r settings, as described above.  If {Np} is set to a value > 0, then the\r {fnum} and {nrho} settings are ignored, and exactly {Np} particles are\r added on each insertion timestep.  This is done by dividing {Np} by\r the total number of grid cells that are adjacent to the specified box\r faces and adding an equal number of particles per grid cell.\r \r The {nevery} keyword determines how often particles are added.  If\r {Nstep} > 1, this may give a non-continuous, clumpy distribution in\r the inlet flow field.\r \r The {perspecies} keyword determines how the species of each added\r particle is randomly determined.  This has an effect on the\r statistical properties of added particles.\r \r If {perspecies} is set to {yes}, then a target insertion number {M} in\r a grid cell is calculated for each species, which is a function of the\r relative number fraction of the species, as set by the \"mixture\r nfrac\"_mixture.html command.  If {M} has a fractional value,\r e.g. 12.5, then 12 particles of that species will always be added, and\r a 13th depending on the value of a random number.\r \r If {perspecies} is set to {no}, then a single target insertion number\r {M} in a grid cell is calculated for all the species.  Each time a\r particle is added, a random number is used to choose the species of\r the particle, based on the relative number fractions of all the\r species in the mixture.  As before, if {M} has a fractional value,\r e.g. 12.5, then 12 particles will always be added, and a 13th\r depending on the value of a random number.\r \r Here is a simple example that illustrates the difference between the\r two options.  Assume a mixture with 2 species, each with a relative\r number fraction of 0.5.  Assume a particular grid cell adds 10\r particles from that mixture.  If {perspecies} is set to {yes}, then\r exactly 5 particles of each species will be added on every timestep\r insertions take place.  If {perspecies} is set to {no}, then exactly\r 10 particles will be added every time and on average there will be 5\r particles of each of the two species.  But on one timestep it might be\r 6 of the first and 4 of the second.  On another timestep it might be 3\r of the first and 7 of the second.\r \r If the {region} keyword is used, then a particle will only added if\r its position is within the specified {region-ID}.  This can be used to\r only allow particle insertion on a subset of the boundary face.  Note\r that the {side} option for the \"region\"_region.html command can be\r used to define whether the inside or outside of the geometric region\r is considered to be \"in\" the region.\r \r IMPORTANT NOTE: If the {region} and {n} keywords are used together,\r less than N particles may be added on an insertion timestep.  This is\r because grid cells will be candidates for particle insertion, unless\r they are entirely outside the bounding box that encloses the region.\r Particles those grid cells attempt to add are included in the count\r for N, even if some or all of the particle insertions are rejected due\r to not being inside the region.\r \r The {modulate} keyword can be used to multiple the count of particles\r inserted at each timestep by a prefactor.  The prefactor is calcalated\r at each insertion timestep by invoking the equal-style variable whose\r name is speficied as {v_name}.  A value of 1.0 will not modulate the\r count of inserted particles (on that timestep).  A value of 0.5 (or\r 2.0) will insert half (or twice) the unmodulated count of particles.\r Note that the formula used for an equal-style variable can include the\r current timestep, so this is a simple way to insert a time-varying\r flow of particles.\r \r The {subsonic} keyword uses the method of Fang and Liou\r \"(Fang02)\"_#Fang02 to determine the number of particles to insert in\r each grid cell on the emitting face(s).  They used the method of\r characteristics to calculate the mean properties of the incoming\r molecular flux, so that the prescribed pressure condition is achieved.\r These properties are then applied to calculate the molecular flux\r across a grid cell face per unit time, as given by equation 4.22 of\r \"(Bird94)\"_#Bird94.\r \r This keyword allows specification of both the pressure and temperature\r at the boundary or just the pressure (by specifying the temperature as\r NULL).  If specified, the temperature must be > 0.0.  Currently,\r instantaneous values for the density, temperature, and stream velocity\r of particles in the cells adjacent to the boundary face(s) are\r computed and used to determine the properties of inserted particles on\r each timestep.\r \r IMPORTANT NOTE: Caution must be exercised when using the subsonic\r boundary condition without specifying an inlet temperature. In this\r case the code tries to estimate the temperature of the flow from the\r properties of the particles in the domain. If the domain contains few\r particles per cell it may lead to spurious results.  This boundary\r condition is meant more for an outlet than an inlet boundary\r condition, and performs well in cases where the cells are adequately\r populated.\r  \r IMPORTANT NOTE: When using this keyword, you should also use an\r appropriate boundary collision or chemistry model via the\r \"boundary\"_boundary.htmo or \"bound_modify\"_bound_modify.html or\r \"surf_collide\"_surf_collide.html or \"surf_react\"_surf_react.html\r commands, so that particles hitting the surface disappear as if they\r were exiting the simulation domain.  That is necessary to produce the\r correct subsonic conditions that the particle insertions due to this\r command are trying to achieve.\r \r The {twopass} keyword does not require a value.  If used, the\r insertion procedure will loop over the insertion grid cells twice, the\r same as the KOKKOS package version of this fix does, so that it can\r reallocate memory efficiently, e.g. on a GPU.  If this keyword is used\r the non-KOKKOS and KOKKOS version will generate exactly the same set\r of particles, which makes debugging easier.  If the keyword is not\r used, the non-KOKKOS and KOKKOS runs will use random numbers\r differently and thus generate different particles, though they will be\r statistically similar.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global vector of length 2 which can be accessed by\r various output commands.  The first element of the vector is the total\r number of particles added on the most recent insertion step.  The\r second element is the cummulative total number added since the\r beginning of the run.  The 2nd value is initialized to zero each time\r a run is performed.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_emit_face.txt"
    },
    {
      "family": "fix",
      "style": "emit/face/file",
      "description": "Emit particles from a face of the simulation box, continuously during\r a simulation.  The particles are added using properties of the\r specified mixture and values read from an input file that can override\r the default global values for those properties.  In particular, the\r input file can be used to create an influx of particles that varies\r spatially over the surface of the {face}.  This can be useful, for\r example, to model an object inserted into a plume flow where the flow\r has spatially varying properties.  If invoked every timestep, this fix\r creates a continuous influx of particles thru the face.\r \r The properties of the added particles are determined by the mixture\r with ID {mix-ID} and the input file.  Together they set the number and\r species of added particles, as well as their streaming velocity,\r thermal temperature, and internal energy modes.  Settings for a\r subsonic pressure boundary condition is also allowed.  The details are\r explained below.\r \r Only one face of the simulation box can be specified via the {face}\r argument.  The 6 possible faces are {xlo}, {xhi}, {ylo}, {yhi}, {zlo},\r or {zhi}.  This command can be used multiple times to add particles on\r multiple faces.\r \r On each insertion timestep, each grid cell with a face touching the\r specified boundary {face} performs the following computations to add\r particles.  The particles are added at the beginning of the SPARTA\r timestep.\r \r The molecular flux across a grid cell face per unit time is given by\r equation 4.22 of \"(Bird94)\"_#Bird94.  The number of particles {M} to\r add on a particular grid cell face is based on this flux and\r additional global, flow, and cell face properties:\r \r global property: {fnum} ratio as specified by the \"global\"_global.html\" command\r flow properties: number density, streaming velocity, and thermal temperature\r cell face properties: area of face and its orientation relative to the streaming velocity :ul\r \r The flow properties are defined for the specified mixture via the\r \"mixture\"_mixture.html command.  Any or all them can be overridden by\r values in the input data file, which affect individual grid cells as\r described below.\r \r If {M} has a fractional value, e.g. 12.5, then 12 particles are\r added, and a 13th depending on the value of a random number.  Each\r particle is added at a random location on the grid cell face.  The\r particle species is chosen randomly in accord with the {frac} settings\r of the collection of species in the mixture, as set by the\r \"mixture\"_mixture.html command.  These can also be overridden by\r spatially varying number fraction values in the input data file, as\r described below.\r \r The velocity of the particle is set to the sum of the streaming\r velocity and a thermal velocity sampled from the thermal temperature.\r The internal energy modes of the particle are determined by the {trot}\r and {tvib} settings and the {rotate} and {vibrate} options of the\r \"collide_modify\"_collide_modify.html command.  Note that if the\r \"collide\"_collide.html command has not been specified (free molecular\r flow), then no rotational or vibrational energy will be assigned to\r created particles.\r \r If the final particle velocity is not directed \"into\" the grid cell,\r then the velocity sampling procedure is repeated until it is.  This\r insures that all added particles enter the simulation domain, as\r desired.\r \r The first timestep that added particles are advected, they move for a\r random fraction of the timestep.  This insures a continuous flow field\r of particles entering the simulation box.\r \r :line\r \r For 3d simulations, the input data file defines a 2d [mesh of points]\r which conceptually overlays some portion or all of the specified face\r of the simulation box.  For a 2d simulation, a 1d [mesh of points] is\r defined.  The set of mesh points is topologically regular, but can\r have uniform or non-uniform spacing in each of its two or one\r dimensions (for 3d or 2d problems).  One or more values can be defined\r at every mesh point, which override any of the mixture settings\r defined by the \"mixture\"_mixture.html command.  These are the flow\r properties discussed above (number density, streaming velocity, and\r thermal temperature), as well as the number fraction of any species in\r the mixture.  Any value not defined in the input data file defaults to\r the mixture value.\r \r IMPORTANT NOTE: It is critical to understand that the input data file\r defines [mesh points] on the face of the simulation box.  It does not\r define [mesh cells], e.g. 2d squares or rectangles, each with flow\r properties.\r \r For 3d simulations, 2d mesh points are defined in the file using I,J\r indices.  (The 1d mesh points for 2d simulations are described below).\r I and J map to any of the simulation box faces in this manner.  A\r simulation box face has two varying dimensions (e.g. ylo face = x and\r z dimensions).  The I index in the file corresponds to the \"lowest\" of\r these dimensions, where x < y < z.  The J index in the file\r corresponds to the higher.  Thus for face ylo, I = x and J = z.  A low\r I or J value corresponds to a low x or z value, regardless of whether\r the mapping is to the ylo or yhi face.  1d mesh points for a 2d\r simulation are defined in an analogous manner, e.g. for face xlo, I =\r y.\r \r For a 3d simulation, interpolation from values on the 2d mesh points\r to any grid cell face that is on the corresponding simulation box face\r is done in the following manner.  There are 3 cases to consider.\r \r (a) For a grid cell face that is entirely inside the area defined by\r the file mesh points, the centroid (center point) of the grid cell\r face is surrounded geometrically by 4 mesh points.  The 4 values\r defined on those 4 points are averaged in a weighted manner using\r bilinear interpolation (described below) to determine the value for\r the grid cell face.  This value is then used for the calculation\r described above for {M} = the number of particles to add on the cell\r face as well as the properties of the added particles.\r \r (b) For a grid cell face that is entirely outside the area defined by\r the file mesh points, no particles are added in that grid cell.\r \r (c) For a grid cell face that partially overlaps the area defined by\r the file mesh points, the extent of the overlap is computed.  The\r centroid (center point) of the overlap area is surrounded\r geometrically by 4 mesh points.  The values for those 4 points are\r used as in (a) above to determine properties of particles added in\r that grid cell.  Note that the area of insertion, used to calculate\r {M}, is the overlap area, which is smaller than the grid cell face\r area.  Also, particles are only added within the overlap area of the\r grid cell face.\r \r For a 2d simulation, the 3 cases are similar, except for (a) and (c)\r the centroid is the midpoint of a line segment, the centroid is\r surrounded by 2 mesh points, and linear interpolation (described\r below) is performed to determine the value for the grid face.\r \r :line\r \r The format of the input data file is a series of one or more sections,\r defined as follows (without the parenthesized comments).  Note that\r one file can contain many sections, each with a different set of\r tabulated values.  The sections can be a mix of 2d and 3d formats.\r SPARTA reads the file section by section, skipping sections with\r non-matching boundary IDs, until it finds one that matches the\r specified boundary-ID.  The lines that follow must be in this order:\r \r # plume ABC info           (one or more comment or blank lines) :pre\r \r PLUME_ABC                  (boundary-ID is first word on line)\r NIJ 4 10                   (mesh size: Ni by Nj points)\r NV 3                       (Nv = number of values per mesh point)\r VALUES nrho temp Ar        (list of Nv values per mesh point)\r IMESH 0.0 0.3 0.9 1.0      (mesh point coordinates in I direction)\r JMESH ...                  (mesh point coordinates in J direction)\r                            (blank)\r 1 1 1.0 300.0 0.5          (I, J, value1, value2, ...)\r 1 2 1.02 310.0 0.5           \r ...\r 4 10 3.0 400.0 0.7 :pre\r \r This format is for a 3d simulation.  For a 2d simulation, there are 3\r changes:\r \r \"NIJ 4 10\" is replaced by \"NI 6\"\r JMESH line is not included\r \"I,J,value1,...\" is replaced by \"I,value1,...\" :pre\r \r A section begins with a non-blank line whose first character is not a\r \"#\".  Blank lines or lines starting with \"#\" can be used as comments\r between sections.  The first line begins with a boundary-ID which\r identifies the section.  The line can contain additional text, but the\r initial text must match the boundary-ID specified in the fix\r emit/face/file command.  Otherwise the section is skipped.\r \r The VALUES line lists Nv keywords.  The list of possible keywords is\r as follows, along with the meaning of the numeric value specified for\r the mesh point:\r \r nrho = number density\r vx,vy,vz = 3 components of streaming velocity\r temp = thermal temperature\r trot = rotational temperature\r tvib = vibrational temperature\r press = pressure for subsonic boundary condition\r species = number fraction of any species in the mixture :ul\r \r The IMESH and JMESH lines must list values that are monotonically\r increasing.\r \r Following a blank line, the next N = Ni x Nj lines (or N = Ni lines\r for a 2d simulation) list the tabulated values.  The format of each\r line is I,J followed by Nv values.  The N lines can be in any order,\r but all unique I,J (or I for 2d) indices must be listed.\r \r Note that if number fractions are specified for one or more species in\r the mixture, then they override number fraction values for the mixture\r itself, as set by the \"mixture\"_mixture.html command.  However, for\r each grid cell, the rule that the number fraction of all species in\r the mixture must sum to 1.0 is enforced, just as it is for the\r mixture.  This means that number fractions of species not specified in\r the file or in the mixture may be reset (for that grid cell) to insure\r the sum = 1.0, as explained on the \"mixture\"_mixture.html command doc\r page.  If this cannot be done, an error will be generated.\r \r If the {press} keyword is used, this means a subsonic pressure\r boundary condition is used for the face, similar to how the {subsonic}\r keyword is used for the \"fix emit/face\"_fix_emit_face.html command.\r If just the {press} keyword is specified, but not the {temp} keyword,\r then it is similar to the \"subsonic press NULL\" setting for the \"fix\r emit/face\"_fix_emit_face.html command.  If both keywords are used it\r is similar to the \"subsonic press temp\" setting for the \"fix\r emit/face\"_fix_emit_face.html command.  The difference with this\r command is that both the {press} and {temp} values can be vary\r spatially across the box face, like the other keyword values.\r \r The subsonic pressure boundary condition is uses the method of Fang\r and Liou \"(Fang02)\"_#Fang02 to determine the number of particles to\r insert in each grid cell on the emitting face(s).  They used the\r method of characteristics to calculate the mean properties of the\r incoming molecular flux, so that the prescribed pressure condition is\r achieved.  These properties are then applied to calculate the\r molecular flux across a grid cell face per unit time, as given by\r equation 4.22 of \"(Bird94)\"_#Bird94.\r \r As explained above the input data file can specify both the pressure\r and temperature at the boundary or just the pressure.  If specified,\r the temperature must be > 0.0.  Currently, instantaneous values for\r the density, temperature, and stream velocity of particles in the\r cells adjacent to the boundary face(s) are computed and used to\r determine the properties of inserted particles on each timestep.\r \r IMPORTANT NOTE: Caution must be exercised when using the subsonic\r boundary condition without specifying an inlet temperature. In this\r case the code tries to estimate the temperature of the flow from the\r properties of the particles in the domain. If the domain contains few\r particles per cell it may lead to spurious results.  This boundary\r condition is meant more for an outlet than an inlet boundary\r condition, and performs well in cases where the cells are adequately\r populated.\r \r IMPORTANT NOTE: When using a subsonic prsesure boundary condition, you\r should also use an appropriate boundary collision or chemistry model\r via the \"boundary\"_boundary.htmo or \"bound_modify\"_bound_modify.html\r or \"surf_collide\"_surf_collide.html or \"surf_react\"_surf_react.html\r commands, so that particles hitting the surface disappear as if they\r were exiting the simulation domain.  That is necessary to produce the\r correct subsonic conditions that the particle insertions due to this\r command are trying to achieve.\r \r :line\r \r For 3d simulations, bilinear interpolation from the 2d mesh point\r values specified in the file is performed using this equation to\r calculate the value at the centroid point (i,j) in the grid cell face:\r \r f(i,j) = 1/area * (f(i1,j1)*(i2-i)*(j2-j) + f(i2,j1)*(i-i1)*(j2-j) +\r                    f(i2,j2)*(i-i1)*(j-j1) + f(i1,j2)*(i2-i)*(j-j1)) :pre\r \r where the 4 surrounding mesh points are (i1,j1), (i2,j1), (i2,j2), and\r (i1,j2).  The 4 f() values on the right-hand side are the values\r defined at the mesh points.  The sum is normalized by the area of the\r overlap between the grid cell face and the file mesh.\r \r For 2d simulations, linear interpolation from the 1d mesh point values\r specified in the file is performed using this equation to calculate\r the value at the centroid point (i) in the grid cell line:\r \r f(i) = 1/length * (f(i1)*(i2-i) + f(i2)*(i-i1)\r      = f(i1) + (i - i1)/(i2 - i1) * (f(i2) - f(i1)) :pre\r \r where the 2 surrounding mesh points are (i1) and (i2).  The 2 f()\r values on the right-hand side are the values defined at the mesh\r points.  The sum is normalized by the length of the overlap between\r the grid cell line and file mesh.\r \r :line\r \r The {frac} keyword can alter how many particles are added, which\r can be useful for debugging purposes.  If {frac} is set to 1.0 (the\r default) then the number of particles added is the sum of the {M}\r values computed for each grid cell that overlaps with the mesh defined\r in the file, as described above.  If {frac} < 1.0 then {M} is scaled\r by frac to determine the number of particles added in each grid\r cell.  Thus a simulation with less particles can easily be run to test\r if it is setup correctly.\r \r The {nevery} keyword determines how often particles are added.  If\r {Nstep} > 1, this may give a non-continuous, clumpy distribution in\r the inlet flow field.\r \r The {perspecies} keyword determines how the species of each added\r particle is randomly determined.  This has an effect on the\r statistical properties of added particles.\r \r If {perspecies} is set to {yes}, then a target insertion number {M} in\r a grid cell is calculated for each species, which is a function of the\r relative number fraction of the species, as set by the \"mixture\r nfrac\"_mixture.html command.  If {M} has a fractional value,\r e.g. 12.5, then 12 particles of that species will always be added,\r and a 13th depending on the value of a random number.\r \r If {perspecies} is set to {no}, then a single target insertion number\r {M} in a grid cell is calculated for all the species.  Each time a\r particle is added, a random number is used to choose the species of\r the particle, based on the relative number fractions of all the\r species in the mixture.  As before, if {M} has a fractional value,\r e.g. 12.5, then 12 particles will always be added, and a 13th\r depending on the value of a random number.\r \r Here is a simple example that illustrates the difference between the\r two options.  Assume a mixture with 2 species, each with a relative\r number fraction of 0.5.  Assume a particular grid cell adds 10\r particles from that mixture.  If {perspecies} is set to {yes}, then\r exactly 5 particles of each species will be added on every timestep\r insertions take place.  If {perspecies} is set to {no}, then exactly\r 10 particles will be added every time and on average there will be 5\r particles of each of the two species.  But on one timestep it might be\r 6 of the first and 4 of the second.  On another timestep it might be 3\r of the first and 7 of the second.\r \r If the {region} keyword is used, then a particle will only added if\r its position is within the specified {region-ID}.  This can be used to\r only allow particle insertion on a subset of the boundary face.  Note\r that the {side} option for the \"region\"_region.html command can be\r used to define whether the inside or outside of the geometric region\r is considered to be \"in\" the region.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global vector of length 2 which can be accessed by\r various output commands.  The first element of the vector is the total\r number of particles added on the most recent insertion step.  The\r second element is the cummulative total number added since the\r beginning of the run.  The 2nd value is initialized to zero each time\r a run is performed.",
      "docFile": "fix_emit_face_file.txt"
    },
    {
      "family": "fix",
      "style": "emit/surf",
      "description": "Emit particles from a group of surface elements, continuously during a\r simulation.  If invoked every timestep, this fix creates a continuous\r outflux of particles from the surface elements in the group.  This\r command can only be used with explicit surfaces, not implicit.  See\r \"Section Howto 6.13\"_Section_howto.html#howto_13 for a discussion of\r explicit and implicit surface elements.\r \r The properties of the added particles are determined by the mixture\r with ID {mix-ID}.  This sets the number and species of added\r particles, as well as their streaming velocity, thermal temperature,\r and internal energy modes.  The details are explained below.\r \r Which surface elements emit particles is specified by the {group-ID}\r for a surface group, which defines a set of surface elements.  The\r \"group surf\"_group.html is used to define surface groups.\r \r On each insertion timestep, each grid cell that overlaps with one or\r more emitting surface elements performs the following computations to\r add particles for each grid cell/surface element pairing.  The\r particles are added at the beginning of the SPARTA timestep.\r \r The molecular flux emitted from a surface element per unit time is\r given by equation 4.22 of \"(Bird94)\"_#Bird94.  The number of particles\r {M} to insert on the portion of a surface element that is contained\r within a grid cell is based on this flux and additional global, flow,\r and surface element properties:\r \r global property: {fnum} ratio as specified by the \"global\"_global.html\" command\r flow properties: number density, streaming velocity, and thermal temperature\r surface element properties: portion of surface element area that overlaps with the grid cell and its orientation relative to the streaming velocity :ul\r \r The flow properties are defined for the specified mixture via the\r \"mixture\"_mixture.html command.\r \r If {M} has a fractional value, e.g. 12.5, then 12 particles are added,\r and a 13th depending on the value of a random number.  Each particle\r is added at a random location within the portion of the surface\r element that overlaps with the grid cell.  The particle species is\r chosen randomly in accord with the {frac} settings of the collection\r of species in the mixture, as set by the \"mixture\"_mixture.html\r command.\r \r IMPORTANT NOTE: The preceeding calculation is actually done using\r surface element areas associated with {weighted} cell volumes.  Grid\r cells can be weighted using the \"global weight\"_global.html command.\r \r The velocity of the particle is set to the sum of the streaming\r velocity and a thermal velocity sampled from the thermal temperature.\r The internal energy modes of the particle are determined by the {trot}\r and {tvib} settings of the mixture and the {rotate} and {vibrate}\r options of the \"collide_modify\"_collide_modify.html command.  Note\r that if the \"collide\"_collide.html command has not been specified\r (free molecular flow), then no rotational or vibrational energy will\r be assigned to created particles.  See the discussion of the {normal}\r keyword below for a way to change the velocity assignment to be\r oriented in the direction normal to the surface element, rather than\r in the direction of the streaming velocity.\r \r If the final particle velocity is not directed \"out of\" the surface\r element, then the velocity sampling procedure is repeated until it is.\r This insures that all added particles emit from the surface element,\r as desired.\r \r The first timestep that added particles are advected, they move for a\r random fraction of the timestep.  This insures a continuous flow field\r of particles emitting from each surface element.\r \r :line\r \r The {n} keyword can alter how many particles are added, which can be\r useful for debugging purposes.  If {Np} is set to 0, then the number\r of added particles is a function of {fnum}, {nrho}, and other mixture\r settings, as described above.\r \r If {Np} is set to a value > 0, then the {fnum} and {nrho} settings are\r ignored, and roughly {Np} particles are added on each insertion\r timestep.  For each grid cell/surface element pair, its target number\r of emitted particles is set to its fraction of the total emission area\r (for all grid cell/surface element pairs), multiplied by {Np}.  If\r that results in a fractional value, then an extra particle is emitted\r depending on the value of a random number, as explained above.\r \r The {Np} value can be also be specified as an equal-style\r \"variable\"_variable.html.  If the value is a variable, it should be\r specified as v_name, where name is the variable name.  In this case,\r the variable will be evaluated on each emission timestep, and its\r value used as {Np} on that step to determine the target number of\r emitted particles for each grid cell/surface element pair, the same as\r described in the preceeding paragraph.\r \r Equal-style variables can specify formulas with various mathematical\r functions, and include \"stats_style\"_status_style.html command\r keywords for the simulation box parameters and timestep and elapsed\r time.  Thus it is easy to specify a time-dependent value of {Np}.\r \r The {normal} keyword can be used to alter how velocities are set for\r added particles.  If {normal} is set to {no}, then a particle's\r velocity is set as described above, using the mixture's streaming\r velocity superposed with a thermal velocity sampled from the\r temperature of the mixture.  Note that the same streaming velocity is\r used for all emitting surface elements, regardless of their\r orientation with respect to the streaming velocity.  If {normal} is\r set to {yes}, then each surface element is assigned its own\r \"streaming\" velocity in the following manner.  The streaming velocity\r points in the direction of the outward normal of the surface element,\r and its magnitude is set to the magnitude of the mixture's streaming\r velocity.  A velocity is then assigned to the particle in the same\r manner as before.  It is assigned the outward streaming velocity\r superposed with a thermal velocity sampled from the temperature of the\r mixture.  The effect is that particles effectively stream outward from\r each emitting surface element.\r \r The {nevery} keyword determines how often particles are added.  If\r {Nstep} > 1, this may give a non-continuous, clumpy distribution in\r the inlet flow field.\r \r The {perspecies} keyword determines how the species of each added\r particle is randomly determined.  This has an effect on the\r statistical properties of added particles.\r \r If {perspecies} is set to {yes}, then a target insertion number {M}\r for a grid cell/surface element pair is calculated for each species,\r which is a function of the relative number fraction of the species, as\r set by the \"mixture nfrac\"_mixture.html command.  If {M} has a\r fractional value, e.g. 12.5, then 12 particles of that species will\r always be added, and a 13th depending on the value of a random number.\r \r If {perspecies} is set to {no}, then a single target insertion number\r {M} for a grid cell/surface element pair is calculated for all the\r species.  Each time a particle is added, a random number is used to\r choose the species of the particle, based on the relative number\r fractions of all the species in the mixture.  As before, if {M} has a\r fractional value, e.g. 12.5, then 12 particles will always be added,\r and a 13th depending on the value of a random number.\r \r Here is a simple example that illustrates the difference between the\r two options.  Assume a mixture with 2 species, each with a relative\r number fraction of 0.5.  Assume a particular grid cell/surface element\r pair adds 10 particles from that mixture.  If {perspecies} is set to\r {yes}, then exactly 5 particles of each species will be added on every\r timestep insertions take place.  If {perspecies} is set to {no}, then\r exactly 10 particles will be added every time and on average there\r will be 5 particles of each of the two species.  But on one timestep\r it might be 6 of the first and 4 of the second.  On another timestep\r it might be 3 of the first and 7 of the second.\r \r If the {region} keyword is used, then a particle will only added if\r its position is within the specified {region-ID}.  This can be used to\r only allow particle insertion on a subset of the collective area of\r the specified group of surface elements.  Note that the {side} option\r for the \"region\"_region.html command can be used to define whether the\r inside or outside of the geometric region is considered to be \"in\" the\r region.\r \r IMPORTANT NOTE: If the {region} and {n} keywords are used together,\r less than N particles may be added on an insertion timestep.  This is\r because grid cell/suface element pairs will be candidates for particle\r insertion, unless the grid cell is entirely outside the bounding box\r that encloses the region.  Particles those grid cell/surface element\r pairs will attempt to add are included in the count for N, even if\r some or all of the particle insertions are rejected due to not being\r inside the region.\r \r The {subsonic} keyword uses the method of Fang and Liou\r \"(Fang02)\"_#Fang02 to determine the number of particles to insert in\r each grid cell on the emitting face(s).  They used the method of\r characteristics to calculate the mean properties of the incoming\r molecular flux, so that the prescribed pressure condition is achieved.\r These properties are then applied to calculate the molecular flux\r across a grid cell face per unit time, as given by equation 4.22 of\r \"(Bird94)\"_#Bird94.\r \r This keyword allows specification of both the pressure and temperature\r at the surface or just the pressure (by specifying the temperature as\r NULL).  If specified, the temperature must be > 0.0.  Currently,\r instantaneous values for the density, temperature, and stream velocity\r of particles in the cells containing the surface elements are computed\r and used to determine the properties of inserted particles on each\r timestep.\r \r IMPORTANT NOTE: Caution must be exercised when using the subsonic\r boundary condition without specifying an inlet temperature. In this\r case the code tries to estimate the temperature of the flow from the\r properties of the particles in the domain. If the domain contains few\r particles per cell it may lead to spurious results.  This boundary\r condition is meant more for an outlet than an inlet boundary\r condition, and performs well in cases where the cells are adequately\r populated.\r \r IMPORTANT NOTE: When using this keyword, you should also use an\r appropriate surface collision or chemistry model via the\r \"surf_collide\"_surf_collide.html or \"surf_react\"_surf_react.html\r commands, so that particles hitting the surface disappear as if they\r were exiting the simulation domain.  That is necessary to produce the\r correct subsonic conditions that the particle insertions due to this\r command are trying to achieve.\r \r :line\r \r The {custom} keyword can be used to tailor the emission of particles\r from individual surface elements.  This is done by using custom\r per-surf vectors or arrays defined by other commands.\r \r For example, the \"read_surf\"_read_surf.html command can read per-surf\r attributes included in a surface data file.  The \"custom\"_custom.html\r and \"fix custom\"_fix_custom.html commands allow for definition of\r custom per-surf vectors or arrays and their initialization by use of\r \"surf-style variables\"_variable.html or by reading the values from a\r file.  Note that the use of \"fix custom\" allows for the resetting of\r custom attributes once every N timesteps, which allows for\r time-dependence in per-surface emission properties.  The \"fix\r surf/temp\"_fix_surf_temp.html command resets per-surf temperatures\r based on gas collisions with each surface element.\r \r See \"Section Howto 6.17\"_Section_howto.html#howto_17 for a discussion\r of custom per-surf attributes.\r \r IMPORTANT NOTE: The {custom} keyword cannot be used together with\r either the {n} or {subsonic} keywords.\r \r The {attribute} value of the {custom} keyword can be any of the\r following:\r \r nrho = number density (# per volume or area units) = per-surf vector\r vstream = 3-component streaming velocity (velocity units) = per-surf array with 3 columns\r speed = length of streaming velocity vector in normal direction (velocity units) = per-surf vector\r temp = temperature (temperature units) = per-surf vector\r fractions = species fractions (unitless) = per-surf array :ul\r \r The {s_name} value of the {custom} keyword is the name of the custom\r per-surf vector or array.  It must store floating-point values and be\r a vector or array, as indicated in the list above.\r \r When the fix emit/surf command calculates the number of particles (and\r their attributes) to be emitted from each surface element, by default\r it uses the mixture properties of the specified {mix-ID} for number\r density, streaming velocity, temperature, and relative species\r fractions.  The same values are used for all surface elements.  If the\r {custom} keyword is used for one or more of these properties, the\r values of the associated custom per-surf vector or array override the\r default mixture properties.\r \r The {custom} attribute {nrho} sets a number density used to calculate\r the count of particles emitted from each surface element.  The nrho\r values should be in per-volume units = per-length^3 for 3d models\r or axisymmetric models.  The values should be in per-area units =\r per-length^2 for 2d models.\r \r The {custom} attribute {vstream} can only be used if the {normal}\r keyword is set to {no}, which is the default.  In this case it must\r refer to a 3-column per-surf custom array which stores the 3 streaming\r velocity components for each surface element.  If the {normal} keyword\r is set to {yes}, then the {custom} atrribute {speed} should be used\r instead.  It must refer to a custom per-surf vector which stores the\r \"speed\" of the emission in the direction normal to each surface\r element.  I.e. it is the scalar length of the streaming velocity\r vector, as described above for the {normal} keyword.\r \r The {custom} attribute {temp} sets a temperature for each surface\r element.  This temperature is used as the thermal, rotational,\r vibrational temeperature for each inserted particle which means it\r affects their thermal velocity components as well as their rotational\r and vibrational energies.\r \r The {custom} attribute {fractions} must refer to a per-surf custom\r array with N columns, where N is the number of species in the mixture.\r For each surface element, the N values will be used to set the\r relative fractions of emitted particles for that element, using the\r logic for the {perspecies yes/no} keyword described above.\r \r For each surface element, the N per-species fractional values must sum\r to 1.0.  However, one or more of the numeric values can be < zero, say\r M of them.  In this case, each of the M values will be reset to (1 -\r sum)/M, where sum is the sum of the N-M values which are >= zero.\r \r Note that the ordering of species within the N columns of the custom\r per-surf array, is the same as the order of species within the mix-ID\r mixture.  This is determined by the \"mixture\"_mixture.html command.\r It is the order the gas species names were listed when the mixture\r command was specified (one or more times).\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global vector of length 2 which can be accessed by\r various output commands.  The first element of the vector is the total\r number of particles added on the most recent insertion step.  The\r second element is the cummulative total number added since the\r beginning of the run.  The 2nd value is initialized to zero each time\r a run is performed.",
      "docFile": "fix_emit_surf.txt"
    },
    {
      "family": "fix",
      "style": "field/grid",
      "description": "Specify the formulas used to calculate the acceleration effect of an\r external field on particle motion.  The ID of this fix can be used by\r the \"global field grid\"_global.html command which applies the field\r when particles are advected during a simulation run.  This is done by\r invoking a method in this fix, which evaluates the specified\r grid-style variables.\r \r Each of the {axvar}, {ayvar}, and {azvar} arguments is the name of a\r grid-style variable.  The variables should compute the x,y,z\r components of acceleration applied at the center point of each grid\r cell in the simulation.  Any of the three variables can be specified\r as NULL, which means there is no acceleration in that dimension.\r \r Each timestep when a particle is advected the acceleration vector (a)\r for the grid cell it is in acts as a perturbation on straight-line\r motion which affects both the end-of-timestep position (x) and\r velocity (v) vectors of the particle:\r \r xnew = x + dt*v + 0.5*a*dt^2\r vnew = v + dt*a :pre\r \r Note that the formulas encoded by the {axvar}, {ayvar}, and {azvar}\r variables should produce values that are in units of acceleration\r (distance/time^2, see the \"units\"_units.html command), not force.  And\r they should not include the timestep (dt) value in the formulas above.\r That is applied by SPARTA during advection.\r \r See the \"variable\"_variable.html doc page for a description of the\r formula syntax allowed for grid-style variables.  They can include\r references to the grid vectors xc, yc, and zc for the grid cell center\r point.  Using these values in a formula can enable a\r spatially-dependent field.  The formulas can also include the current\r timestep and timestep size (dt) to enable a time-dependent field.\r \r NOTE still need to figure this out: And they can include properties of\r the particle, such as its mass or magnetic moment.\r \r :line\r \r Note that the \"global field\"_global.html command provides three\r alternatives for specifying an external field:\r \r global field constant ...     # field is constant in space and time\r global field particle ...     # field is applied on a per particle basis\r global field grid ...         # field is applied on a per grid cell basis :pre\r \r This fix is only used for per-grid fields.  It should only be used for\r fields which vary spatially or in time; otherwise use the constant\r option which will be much more efficient.  The use of per-grid\r variables allows the field to vary spatially as a function of the grid\r cell center point. It also allows the field to vary in time by having\r the variables use the current timestep.  \r \r NOTE still need to figure out how to do this: The field can also\r depend on particle attributes, such as its mass and magnetic moment\r (for a B field).\r \r Note that use of the \"global field grid\"_global.html command with this\r fix will evaluate the specified grid-style variables as often as\r requested.  For a field that has no time-ependence, you can specificy\r it only be evaluated once at the beginning of a run.  For a field that\r is time-dependent you can choose how often to recompute the field,\r depending on how fast it varies.\r \r The \"fix field/particle\"_fix_field_particle.html command is an\r alternative which will typically run much slower, but be more\r accurate.  When used with the \"global field particle\"_global.html\r command, the particle-style variables it uses are invoked every\r timestep using current particle positions.  And the field calculation\r is done for each grid particle, not for each grid cell.  The trade-off\r is that the fields it calculates for each particle is more accurate,\r but the simulation will typically run several times slower than it\r would with this fix.\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix stores a per-grid cell array of values which can be accessed\r by various output commands on any timestep, e.g. by the \"dump\r grid\"_dump.html command.  The values are those produced by evaluating\r the grid-style variables.  The number of rows in the array is the\r number of grid cells this processor owns.  The number of columns in\r the array is the number of non-NULL variables specified.",
      "docFile": "fix_field_grid.txt"
    },
    {
      "family": "fix",
      "style": "field/particle",
      "description": "Specify the formulas used to calculate the acceleration effect of an\r external field on particle motion.  The ID of this fix can be used by\r the \"global field particle\"_global.html command which applies the\r field when particles are advected during a simulation run.  This is\r done by invoking a method in this fix every timestep, which evaluates\r the specified particle-style variables.\r \r Each of the {axvar}, {ayvar}, and {azvar} arguments is the name of a\r particle-style variable.  The variables should compute the x,y,z\r components of acceleration applied to each particle by the field.  Any\r of the three variables can be specified as NULL, which means there is\r no acceleration in that dimension.\r \r Each timestep when a particle is advected the acceleration vector (a)\r acts as a perturbation on straight-line motion which affects both the\r end-of-timestep position (x) and velocity (v) vectors of the particle:\r \r xnew = x + dt*v + 0.5*a*dt^2\r vnew = v + dt*a :pre\r \r Note that the formulas encoded by the {axvar}, {ayvar}, and {azvar}\r variables should produce values that are in units of acceleration\r (distance/time^2, see the \"units\"_units.html command), not force.  And\r they should not include the timestep (dt) value in the formulas above.\r That is applied by SPARTA during advection.\r \r See the \"variable\"_variable.html doc page for a description of the\r formula syntax allowed for particle-style variables.  They can include\r the particle position, thus enabling a spatially-dependent field.\r They can include the current timestep and timestep size (dt) to enable\r a time-dependent field.  And they can include properties of the\r particle, such as its mass or magnetic moment.\r \r :line\r \r Note that the \"global field\"_global.html command provides three\r alternatives for specifying an external field:\r \r global field constant ...     # field is constant in space and time\r global field particle ...     # field is applied on a per particle basis\r global field grid ...         # field is applied on a per grid cell basis :pre\r \r This fix is only used for per-particle fields.  It should only be used\r for fields which vary spatially or in time; otherwise use the constant\r option which will be much more efficient.  The use of per-particle\r variables allows the field to vary spatially as a function of particle\r position.  It also allows the field to vary in time by having the\r variables use the current timestep.  The field can also depend on\r particle attributes, such as its mass and magnetic moment (for a B\r field).\r \r Note that use of the \"global field particle\"_global.html command with\r this fix will evaluate the specified particle-style variables every\r timestep on all particles.  Thus a simulation will typically run\r several times slower than it would without the external field.\r \r The \"fix field/grid\"_fix_field_grid.html command is an alternative\r which should run faster but be more approximate.  When used with the\r \"global field grid\"_global.html command, the grid-style variables it\r uses are only invoked once as a pre-calculation (for static fields) or\r once every N timesteps (for time-varying fields).  And the field\r calculation is done for each grid cell, not for each particle.  The\r trade-off is that the fields it calculates are based on the grid cell\r center pint, and thus are not as accurate as the calucations performed\r by this fix.\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix stores a per-particle array of values which can be accessed\r by various output commands on any timestep, e.g. by the \"dump\r particle\"_dump.html command.  The values are those produced by\r evaluating the particle-style variables.  The number of rows in the\r array is the number of particles this processor owns.  The number of\r columns in the array is the number of non-NULL variables specified.",
      "docFile": "fix_field_particle.txt"
    },
    {
      "family": "fix",
      "style": "grid/check",
      "description": "Check if particles are inside the grid cell they are supposed to be,\r based on their current coordinates.  This is useful as a debugging\r check to insure that no particles have been assigned to the incorrect\r grid cell during the particle move stage of the SPARTA timestepping\r algorithm.\r \r The check is performed once every {N} timesteps.  Particles not inside\r the correct grid cell are counted and the value of the count can be\r monitored (see below).  A value of 0 is \"correct\", meaning that no\r particle was found outside its assigned grid cell.\r \r If the {outside} keyword is set to {yes}, then a check for particles\r inside explicit or implicit surfaces is also performed.  If a particle\r is in a grid cell with surface elements and the particle is \"inside\"\r the surfaces, then the error count is incremented.\r \r If the outflag setting is {error}, SPARTA will print an error and stop\r if it finds a particle in an incorrect grid cell or inside the surface\r elements.  For {warn}, it will print a warning message and continue.\r For {silent}, it will print no message, but the count of such\r occurrences can be monitored as described below, e.g. by outputting\r the value with the \"stats\"_stats.html command.\r \r IMPORTANT NOTE: Use of {outside yes} can be expensive if the check is\r performed frequently (e.g. every step).\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix computes a global scalar which can be accessed by various\r output commands.  The scalar is the count of how many particles were\r not in the correct grid cell.  The count is cummulative over all the\r timesteps the check was performed since the start of the run.  It is\r initialized to zero each time a run is performed.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_grid_check.txt"
    },
    {
      "family": "fix",
      "style": "halt",
      "description": "Check a condition every N steps during a simulation run. N must be\r >=1. If the condition is met, exit the run.\r \r The specified {attribute} can be one of the options listed above,\r namely {tlimit}, or an \"equal-style variable\"_variable.html referenced\r as {v_name}, where \"name\" is the name of a variable that has been\r defined previously in the input script.\r \r The {tlimit} attribute queries the elapsed CPU time (in seconds) since\r the current run began, and sets {attribute} to that value. The clock\r starts at the beginning of the current run (not when the fix command\r is specified), so that any setup time for the run is not included in\r the elapsed time. The timer invocation and syncing across all\r processors (via MPI_Allreduce) is performed (typically) only a small\r number of times and the elapsed times are used to predict when the\r end-of-the-run will be.  This can be useful when performing benchmark\r calculations for a desired length of time with minimal overhead.\r \r Equal-style variables evaluate to a numeric value. See the\r \"variable\"_variable.html command for a description. They calculate\r formulas which can involve mathematical operations, particle\r properties, grid properties, surface properties, global values\r calculated by a \"compute\"_compute.html or \"fix\"_fix.html, or\r references to other \"variables\"_varible.html. Thus they are a very\r general means of computing some attribute of the current system.  For\r example, the following two versions of a fix halt command will both\r stop the run after an hour of walltime:\r \r fix 1 halt 10 tlimit > 3600.0 :pre\r \r variable cpu equal cpu\r fix 1 halt 10 v_cpu > 3600.0 :pre\r \r The commands above apply only to the time spent in the current run\r command. If multiple run commands are used in the same input script,\r one can also stop the run after a predetermined amount of {total}\r walltime:\r \r variable wall equal wall\r fix 1 halt 10 v_wall > 3600.0 :pre\r \r Similarly one can stop the run after a predetermined amount of\r simulation time, which is useful when using a \"variable\r timestep\"_fix_dt_reset.html:\r \r variable time equal time\r fix 1 halt 10 v_time > 1.0e-3 :pre\r \r One can also stop the run if a file exists. The example below\r checks for a file named \"EXIT\" in the current directory:\r \r variable file equal is_file(EXIT)\r fix 1 halt 10 v_file > 0 :pre\r \r The choice of operators listed above are the usual comparison\r operators. The XOR operation (exclusive or) is also included as \"|^\".\r In this context, XOR means that if either the attribute or avalue is\r 0.0 and the other is non-zero, then the result is \"true\". Otherwise it\r is \"false\".\r \r The specified {avalue} must be a numeric value.\r \r The optional {error} keyword determines how the current run is halted.\r If its value is {hard}, then SPARTA will stop with an error message.\r \r If its value is {soft}, SPARTA will exit the current run, but continue\r to execute subsequent commands in the input script. However,\r additional \"run\"_run.html commands will be skipped. For example, this\r allows a script to output the current state of the system, e.g. via a\r \"write_grid\"_write_grid.html or \"write_restart\"_write_restart.html\r command.\r \r If its value is {continue}, the behavior is the same as for soft,\r except subsequent run commands are executed. This allows your script\r to remedy the condition that triggered the halt, if necessary.  Note\r that you may wish use the \"unfix\"_unfix.html command on the fix halt\r ID, so that the same condition is not immediately triggered in a\r subsequent run.\r \r :line\r \r The optional {message} keyword determines whether a message is printed\r to the screen and logfile when the halt condition is triggered. If\r {message} is set to {yes}, a one line message with the values that\r triggered the halt is printed. If {message} is set to {no}, no message\r is printed; the run simply exits. The latter may be desirable for\r post-processing tools that extract statistical information from log\r files.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces no output.\r \r :line",
      "docFile": "fix_halt.txt"
    },
    {
      "family": "fix",
      "style": "move/surf",
      "description": "This command performs on-the-fly movement of all the surface elements\r in the specfied group via one of several styles.  See the \"group\r surf\"_group.html command for info on how surface elements can be\r assigned to surface groups.  Surface element moves can also be\r performed before or between simulations by using the\r \"move_surf\"_move_surf.html command.\r \r Moving surfaces during a simulation run can be useful if you want to\r to track transient changes in a flow while some attribute of the\r surface elements change, e.g. the separation between two spheres.\r \r All of the command arguments which appear after {Nlarge}, which\r determine how surface elements move, are exactly the same as for the\r \"move_surf\"_move_surf.html command, starting with its {style}\r argument.  This includes optional keywords it defines.  See its doc\r page for details.\r \r {Nevery} specifies how often surface elements are moved incrementally\r along the path towards their final position.  The current timestep\r must be a multiple of {Nevery}.\r \r {Nlarge} must be a multiple of {Nevery} and specifies how long it will\r take the surface elements to move to their final position.\r \r Thus if {Nlarge} = 100*{Nevery}, each surface elements will move 1/100 of\r its total distance every {Nevery} steps.\r \r The same rules that the \"move_surf\"_move_surf.html command follows for\r particle deletion after surface elements move, are followed by this\r command as well.  The criteria are applied after every incremental\r move.  This is to prevent particles from ending up inside surface\r objects.\r \r Likewise, the {connect} option of the \"move_surf\"_move_surf.html\r command should be used in the same manner by this command if you\r need to insure that moving only some elements of an object\r do not result in a non-watertight surface grid.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.  No global or per-particle or per-grid quantities\r are stored by this fix for access by various output commands.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_move_surf.txt"
    },
    {
      "family": "fix",
      "style": "print",
      "description": "Print a text string every N steps during a simulation run.  This can\r be used for diagnostic purposes or as a debugging tool to monitor some\r quantity during a run.  The text string must be a single argument, so\r it should be enclosed in quotes if it is more than one word.  If it\r contains variables it must be enclosed in quotes to insure they are\r not evaluated when the input script line is read, but will instead be\r evaluated each time the string is printed.\r \r See the \"variable\"_variable.html command for a description of {equal}\r style variables which are the most useful ones to use with the fix\r print command, since they are evaluated afresh each timestep that the\r fix print line is output.  Equal-style variables calculate formulas\r involving mathematical operations, statistical properties, global\r values calculated by a \"compute\"_compute.html or \"fix\"_fix.html, or\r references to other \"variables\"_variable.html.\r \r If the {file} or {append} keyword is used, a filename is specified to\r which the output generated by this fix will be written.  If {file} is\r used, then the filename is overwritten if it already exists.  If\r {append} is used, then the filename is appended to if it already\r exists, or created if it does not exist.\r \r If the {screen} keyword is used, output by this fix to the screen and\r logfile can be turned on or off as desired.\r \r The {title} keyword allow specification of the string that will be\r printed as the first line of the output file, assuming the {file}\r keyword was used.  By default, the title line is as follows:\r \r # Fix print output for fix ID :pre\r \r where ID is replaced with the fix-ID.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.  No global or per-particle or per-grid quantities\r are stored by this fix for access by various output commands.",
      "docFile": "fix_print.txt"
    },
    {
      "family": "fix",
      "style": "surf/temp",
      "description": "Compute a new temperature for each surface element in the group once\r every Nevery steps.  This command uses a compute or fix ID which\r should compute the flux of total energy onto each surface element,\r e.g. {etot} from the \"compute surf\"_compute_surf.html command.  Note\r that SPARTA does not check that the specified compute/fix calculates\r an energy flux.\r \r This fix creates a custom per-surf floating point vector with the\r specified name of {custom-name}.  It can be accessed by other commands\r which use the temperature of surface elements.  An example is the\r \"surf_collide diffuse\"_surf_collide.html command, as shown above.\r \r The per-surface element temperatures computed by this fix can be\r output via the \"dump surf\"_dump.html command, using its {s_name}\r syntax.  See the examples/adjust_temp directory for input scripts that\r use this fix.\r \r The specified {group-ID} must be the name of a surface element group,\r as defined by the \"group surf\"_group.html command, which contains a\r set of surface elements.\r \r The specfied {Nevery} determines how often the surface temperatures\r are re-computed.\r \r The {source} can be specified as a per-surf quantity calculated by a\r compute, such as the \"compute surf\"_compute_surf.html command.  Or it\r can be specified a per-surf quantity calculated by a fix, e.g. one\r which performs time-averaging of per-surf quantities, such as \"fix\r ave/surf\"_fix_ave_surf.html.\r \r If the specified {source} has no bracketed term appended, the compute\r or fix must calculate a per-surf vector.  If {c_ID\\[N\\]} or\r {f_ID\\[N\\]} is used, then N must be in the range from 1-M, which will\r use the Nth column of the M-column per-surf array calculated by the\r compute or fix.\r \r The temperature of each surface element is calculated from the\r Stefan-Boltzmann law for a gray-body as follows:\r \r q_wall = sigma * emisurf * Tsurf^4 :pre\r \r where q_wall is the heat flux to the surface (provided by the compute\r or fix), sigma is the Stefan-Boltzmann constant appropriate to the\r \"units\"_units.html being used, {emisurf} is the surface emissivity,\r and {Tsurf} is the resulting surface temperature.\r \r The specified emissivity {emisurf} is a unitless coefficient > 0.0 and\r <= 1.0, which determines the emissivity of the surface.  An emissivity\r coefficient of 1.0 means the surface is a black-body that radiates all\r the energy it receives.\r \r The specified {Tinit} value is used to set the initial temperature of\r every surface element in the system.  New temperature values for only\r the surface elements in the {surf-ID} group will be reset every\r {Nevery} timesteps by the formula above.  Thus temperature values for\r surfaces not in the {surf-ID} group will always be {Tinit}.\r \r Note that commands which use these temperature values can determine\r which surface element values they access by their own {surf-ID} group.\r E.g. the \"surf_collide diffuse\"_surf_collide.html command is assigned\r to a group of surface elements via the \"surf_modify\"_surf_modify.html\r command.  It its Tsurf value is set to the custom vector defined by\r this fix, then you likely want the two surface groups to be\r consistent.  Note that it also possible to define multiple\r \"surf_collide diffuse\"_surf_collide.html and multiple fix surf/temp\r commands, each pair of which use a different surface group and\r different custom per-surf vector name.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r However, the values of the custom particle attribute defined by this\r fix is written to the restart file.  Namely the floating-point vector\r of temperature values for each surface with the name assigned by this\r command.  As explained on the \"read_restart\"_read_restart.html doc\r page these values will be re-assigned to surface when a restart file\r is read.  If a new fix surf/temp command is specified in the restart\r script as well as a surface collision model which uses the custom\r attribute updated by this fix, then the per-surf temperatures and\r updating process will continue to be used in the continued run.\r \r No global or per-surf quantities are stored by this fix for access by\r various output commands.\r \r However, the custom per-surf attribute defined by this fix can be\r accessed by the \"dump surf\"_dump.html command, as s_name.  That means\r those per-surf values can be written to surface dump files.",
      "docFile": "fix_surf_temp.txt"
    },
    {
      "family": "fix",
      "style": "temp/global/rescale",
      "description": "Reset the temperature of all the particles in the entire simulation by\r explicitly rescaling their velocities.  This is a simple\r thermostatting operation to keep the temperature of the gas near the\r desired target temperature.  This can be useful if an external driving\r force is adding energy to the system.  Or if you wish the heat or cool\r the temperature of the system over time.\r \r The rescaling is applied to only the translational degrees of freedom\r for the particles.  Their rotational or vibrational degrees of freedom\r are not altered.\r \r Rescaling is performed every N timesteps. The target temperature is a\r ramped value between the Tstart and Tstop temperatures at the\r beginning and end of the run.\r \r From the current global temperature and the current target\r temperature, a velocity scale factor is calculated.  The amount of\r rescaling that is applied is adjusted by the {fraction} parameter\r which is a value from 0.0 to 1.0.  difference between the actual and\r desired temperature.  If {fraction} = 1.0, the temperature is reset to\r exactly the desired value.  If {fraction} = 0.5, the temperature is\r reset to a value halfway between the current global and target\r temperatures.\r \r The rescaling factor is applied to each of the components of the\r translational velocity for every particle in the simulation.\r \r Note that this fix performs thermostatting using the same formula for\r temperature as calculated by the \"compute temp\"_compute_temp.html\r command.  It does not currently subtract out a net streaming velocity\r to measure a thermal temperature since it assumes the net center of\r mass velocity for the entire system is zero.  An option for this may\r be added in the future.  See the \"fix\r temp/rescale\"_fix_temp_rescale.html doc page for a command that\r thermostats the thermal temperature on a per-grid-cell basis.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces no output.\r \r This fix can ramp its target temperature over multiple runs, using the\r start and stop keywords of the run command. See the run command for\r details of how to do this.\r \r :line",
      "docFile": "fix_temp_global_rescale.txt"
    },
    {
      "family": "fix",
      "style": "temp/rescale",
      "description": "Reset the thermal temperature of all the particles within each grid\r cell by explicitly rescaling their thermal velocities.  This is a\r simple thermostatting operation to keep the thermal temperature of the\r gas near the desired target temperature. This can be useful if an\r external driving force is adding energy to the system.  Or if you wish\r the thermal temperature of the system to heat or cool over time.\r \r The rescaling is applied to only the translational degrees of freedom\r for the particles.  Their rotational or vibrational degrees of freedom\r are not altered.\r \r Rescaling is performed every N timesteps. The target temperature\r (Ttarget) is a ramped value between the Tstart and Tstop temperatures\r at the beginning and end of the run.\r \r This fix performs thermostatting on a per grid cell basis.  For each\r grid cell, the center-of-mass velocity and thermal temperature of the\r particles in the grid cell is computed.  The thermal temperature is\r defined as the kinetic temperature after any center-of-mass motion\r (e.g. a flow velocity) is subtracted from the collection of particles\r in the grid cell.  These are the same calculations as are performed by\r the \"compute thermal/grid\"_compute_thermal_grid.html command.  See its\r doc page for the equations.  See the \"fix\r temp/global/rescale\"_fix_temp_globalrescale.html doc page for a\r command that thermostats the temperature of the global system.\r \r How the rescaling of particle velocities is done depends on the value\r of the {ave} keyword.\r \r For {ave} with a value {no} (the default), the thermal temperature\r (Tthermal) of each cell is used to compute a velocity scale factor for\r that cell, which is\r \r vscale = sqrt(Ttarget/Tthermal) :pre\r \r The vscale factor is applied to each of the components of the thermal\r velocity for each particle in the grid cell.  Only cells with 2 or\r more particles have their particle velocities rescaled.\r \r For {ave} with a value {yes}, the thermal temperatures of all the\r cells are averaged.  The average thermal temperature is simply the sum\r of cell thermal temperatures divided by the number of cells.  Cells\r with less than 2 particles or whose thermal temperature = 0.0\r contribute a thermal temperaure = Ttarget to the average.  The average\r thermal temperature (Tthermal_ave) for all cells is used to compute a\r velocity scale factor for all cells, which is\r \r vscale = sqrt(Ttarget/Tthermal_ave) :pre\r \r This single vscale factor is applied to each of the components of the\r thermal velocity for each particle in all the grid cells, including\r the particles in single-particle cells.\r \r After rescaling, for either {ave} = {no} or {yes}, if the thermal\r temperature were re-computed for any grid cell with more than one\r particle, it would be exactly the target temperature.\r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r This fix produces no output.\r \r This fix can ramp its target temperature over multiple runs, using the\r start and stop keywords of the run command. See the run command for\r details of how to do this.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "fix_temp_rescale.txt"
    },
    {
      "family": "fix",
      "style": "vibmode",
      "description": "Enable multiple vibrational energy levels, defined on a per-species\r basis, to be used in a simulation.  This fix is meant to be used with\r the \"collide_modify vibrate discrete\"_collide_modify.html setting\r which means that the vibrational energy of each (non-monoatomic)\r particle is discretized across one or more energy modes, each with its\r own characteristic vibrational temperature.  This fix allocates\r per-particle storage for the mode indices and also has code to\r populate the multiple levels appropriately when particles are created.\r Collisions between pairs of particles will then transfer energy\r between the different modes of the two particles.\r \r An overview of how to run simulations with multiple vibrational energy\r modes is given in the \"Section 4.12\"_Section_howto.html#howto_12.\r This includes use of the \"species\"_species.html command with its\r {vibfile} option, and the use of the \"collide_modify vibrate\r discrete\"_collide_modify.html command.  The section also lists all the\r commands that can be used in an input script to invoke various options\r associated with the vibrational energy modes.  All of them depend on\r this fix vibmode command being defined.\r \r Internally, this fix defines a custom particle attribute named\r \"vibmode\".  It is an integer array with N values per particle.  N is\r the maximum number of energy modes for any species defined in the\r simulation.  The number of energy modes is half the vibrational\r degrees of freedom defined for each species.  See the \"species\"\r command for how the degrees of freedom and associated vibrational\r temperatures and other properties are defined for each mode for each\r species.\r \r Each of the N values is an integer count for the \r \r :line\r \r [Restart, output info:]\r \r No information about this fix is written to \"binary restart\r files\"_restart.html.\r \r However, the values of the custom particle attribute defined by this\r fix are written to the restart file.  Namely the integer values stored\r in \"vibmode\" for each particle.  As explained on the\r \"read_restart\"_read_restart.html doc page these values can be\r re-assigned to particles when a restart file is read, if a new fix\r vibmode command is specified in the restart script before the first\r \"run\"_run.html command is used.\r \r No global or per-particle or per-grid quantities are stored by this\r fix for access by various output commands.\r \r However, the custom particle attributes defined by this fix can be\r accessed by the \"dump particle\"_dump.html command, as p_vibmode.  That\r means those per-particle values can be written to particle dump files.",
      "docFile": "fix_vibmode.txt"
    }
  ],
  "compute": [
    {
      "family": "compute",
      "style": "boundary",
      "description": "Define a computation that calculates one or more values for each\r boundary (i.e. face) of the simulation box, based on the particles\r that cross or collide with the boundary.  The values are summed for\r each group of species in the specified mixture.  See the\r \"mixture\"_mixture.html command for how a set of species can be\r partitioned into groups.\r \r Note that depending on the settings for the \"boundary\"_boundary.html\r command, when a particle collides with a boundary, it can exit the\r simulation box (outflow), re-enter from the other side (periodic),\r reflect specularly from the boundary, or interact with it as if it\r were a surface.  In the surface case, the incident particle may bounce\r off (possibly as a different species), be captured by the boundary\r (vanish), or a 2nd particle can also be emitted.  The formulas below\r account for all these possible scenarios.  As an example, the pressure\r exerted on an outflow boundary versus a specularly reflecting boundary\r is different, since in the former case there is no net momentum flux\r back into the simulation box by reflected particles.\r \r Also note that all values for a boundary collision are tallied based\r on the species group of the incident particle.  Quantities associated\r with outgoing particles are part of the same tally, even if they are\r in different species groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"stats_style\"_stats_style.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/time\"_fix_ave_time.html command.  It does its averaging as if the\r particles striking the boundary at each sampling timestep were\r combined together into one large set to compute the formulas below.\r The answer is then divided by the number of sampling timesteps if it\r is not otherwise normalized by the number of particles.  Note that in\r general this is a different normalization than taking the values\r produced by the formulas below for a single timestep, summing them\r over the sampling timesteps, and then dividing by the number of\r sampling steps.  However for the current values listed below, the two\r normalization methods are the same.\r \r NOTE: If particle weighting is enabled via the \"global\r weight\"_global.html command, then all of the values below are scaled\r by the weight assigned to the grid cell in which the particle\r collision with the boundary occurs.  The only exception is the the {n}\r value, which is NOT scaled by the weight; it is a simple count of\r particle crossings or collisions with the boundary.\r \r :line\r \r The {n} value counts the number of particles in the group crossing or\r colliding with the boundary.\r \r The {nwt} value counts the number of particles in the group crossing\r or colliding with the boundary and weights the count by the weight\r assigned to the grid cell in which the particle collision with the\r boundary occurs.  The {nwt} quantity will only be different than {n}\r if particle weighting is enabled via the \"global weight\"_global.html\r command.\r \r The {nflux} value calculates the number flux imparted to the boundary by\r particles in the group.  This is computed as\r \r Nflux = N / (A * dt / fnum) :pre\r \r where N is the number of all contributing particles, normalized by\r A = the area of the surface element, dt = the timestep, and fnum = the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command.\r \r The {mflux} value calculates the mass flux imparted to the boundary by\r particles in the group.  This is computed as\r \r Mflux = Sum_i (mass_i) / (A * dt / fnum) :pre\r \r where the sum is over all contributing particle masses, normalized by\r the area of the surface element, dt and fnum as defined before.\r \r The {press} value calculates the pressure {P} exerted on the boundary\r in the normal direction by particles in the group, such that outward\r pressure is positive.  This is computed as\r \r p_delta = mass * (V_post - V_pre)\r P = Sum_i (p_delta_i dot N) / (A * dt / fnum) :pre\r \r where A, dt, fnum are defined as before.  P_delta is the change in\r momentum of a particle, whose velocity changes from V_pre to V_post\r when colliding with the boundary.  The pressure exerted on the\r boundary is the sum over all contributing p_delta dotted into the\r normal N of the boundary which is directed into the box, normalized by\r A = the area of the boundary face and dt = the timestep and fnum = the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command.\r \r The {shx}, {shy}, {shz} values calculate the shear pressure components\r Sx, Sy, Sz extered on the boundary in the tangential direction to its\r normal by particles in the group, with respect to the x, y, z\r coordinate axes.  These are computed as\r \r p_delta = mass * (V_post - V_pre)\r p_delta_t = p_delta - (p_delta dot N) N\r Sx = - Sum_i (p_delta_t_x) / (A * dt / fnum)\r Sy = - Sum_i (p_delta_t_y) / (A * dt / fnum)\r Sz = - Sum_i (p_delta_t_z) / (A * dt / fnum) :pre\r \r where p_delta, V_pre, V_post, N, A, dt, and fnum are defined as\r before.  P_delta_t is the tangential component of the change in\r momentum vector p_delta of a particle.  P_delta_t_x (and y,z) are its\r x, y, z components.\r \r The {ke} value calculates the kinetic energy flux {Eflux} imparted to\r the boundary by particles in the group, such that energy lost by a\r particle is a positive flux.  This is computed as\r \r e_delta = 1/2 mass (V_post^2 - V_pre^2)\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the kinetic energy change in a particle, whose\r velocity changes from V_pre to V_post when colliding with the\r boundary.  The energy flux imparted to the boundary is the sum over\r all contributing e_delta, normalized by A = the area of the boundary\r face and dt = the timestep and fnum = the real/simulated particle\r ratio set by the \"global fnum\"_global.html command.\r \r The {erot} value calculates the rotational energy flux {Eflux}\r imparted to the boundary by particles in the group, such that energy\r lost by a particle is a positive flux.  This is computed as\r \r e_delta = Erot_post - Erot_pre\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the rotational energy change in a particle, whose\r internal rotational energy changes from Erot_pre to Erot_post when\r colliding with the boundary.  The flux equation is the same as for the\r {ke} value.\r \r The {evib} value calculates the vibrational energy flux {Eflux}\r imparted to the boundary by particles in the group, such that energy\r lost by a particle is a positive flux.  This is computed as\r \r e_delta = Evib_post - Evib_pre\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the vibrational energy change in a particle, whose\r internal vibrational energy changes from Evib_pre to Evib_post when\r colliding with the boundary.  The flux equation is the same as for the\r {ke} value.\r \r The {etot} value calculates the total energy flux imparted to the\r boundary by particles in the group, such that energy lost by a\r particle is a positive flux.  This is simply the sum of kinetic,\r rotational, and vibrational energies.  Thus the total energy flux is\r the sum of what is computed by the {ke}, {erot}, and {evib} values.\r \r :line\r \r [Output info:]\r \r This compute calculates a global array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {n} and {u} values were specified as keywords, then the first two\r columns would be {n} and {u} for the first group, the 3rd and 4th\r columns would be {n} and {u} for the second group, etc.  The number of\r rows is 4 for a 2d simulation for the 4 faces (xlo, xhi, ylo, yhi),\r and it is 6 for a 3d simulation (xlo, xhi, ylo, yhi, zlo, zhi).\r \r The array can be accessed by any command that uses global array values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The array values will be in the \"units\"_units.html appropriate to the\r individual values as described above.  {N} is unitless. {Press},\r {shx}, {shy}, {shz} are in pressure units.  {Ke}, {erot}, {evib}, and\r {etot} are in energy/area-time units for 3d simulations and\r energy/length-time units for 2d simulations.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_boundary.txt"
    },
    {
      "family": "compute",
      "style": "count",
      "description": "Define a computation that counts the number of particles currently in\r the simulation for various species or groups within mixtures.  Groups\r are collections of one or more species within a mixture.  See the\r \"mixture\" command for an explanation of how species are added to a\r mixture and how groups of species within the mixture are defined.\r \r Each of the listed {ids} (id1, id2, etc) can be in one of three\r formats.  Any of the ids can be in any of the formats.\r \r An {id} can be a species ID, in which case the count is for particles\r of that species.\r \r An {id} can be a mixture ID, in which case one count is performed for\r each of the groups within the mixture.  In the first example above,\r \"species\" is the name of a default mixture which assigns every species\r defined for the simulation to its own group.  If there are 10 species\r in the simulation, there will thus be 10 counts calculated, the same\r as if the command had been specified with explicit names for all 10\r species, e.g.\r \r compute 1 count O2 N2 O N NO O2+ N2+ O+ N+ NO+ :pre\r \r An {id} can also be of the form mix-ID/name where mix-ID is a mixture\r ID and name is the name of a group in that mixture.\r \r :line\r \r [Output info:]\r \r If there is a single count accumulated, this compute calculates a\r global scalar.  If there are multiple counts accumulated, it\r calculates a global vector with a length = number of counts.  These\r results can be used by any command that uses global scalar or vector\r values from a compute as input.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r The values will all be unitless counts.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_count.txt"
    },
    {
      "family": "compute",
      "style": "distsurf/grid",
      "description": "Define a computation that calculates the minimum distance from each\r grid cell in a grid cell group to any surface element in a surface\r element group.  This is useful for grid adaptation; the\r \"adapt_grid\"_adapt_grid.html command can use the compute as a\r criterion for refining or coarsening individual grid cells.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculation.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.  Only surface\r elements in the surface element group specified by {surf-ID} are\r included in the distance calculations.  See the \"group\r surf\"_group.html command for info on how surface elements can be\r assigned to surface element groups.\r \r If the {dir} keyword is specified it can exclude additional surface\r elements.  The Sx,Sy,Sz settings are components of a vector.  It's\r length does not matter, just its direction.  Only surface elements\r whose normal is opposed to the vector direction (in a dot product\r sense) are eligible surfaces for the distance calculations.  This can\r be useful to exclude surface elements that are not facing \"upwind\"\r with respect to the flow direction of the particles.  I.e.  by setting\r Sx,Sy,Sz to the flow direction.  If Sy,Sy,Sz = (0,0,0), which is the\r default, then no surface elements are excluded by this criterion.\r \r Each grid cell also only considers a subset of eligible surfaces in\r its distance calculations.  A vector from the grid cell center to the\r center of each surface element is calculated.  If that vector is\r opposed to the normal vector of the surface element (in a dot product\r sense), the distance from the grid cell to the surface is calculated.\r This means that for an individual grid cell, only surface elements\r that are \"facing\" the grid cell are considered.\r \r The \"distance\" between a grid cell and a surface element is the\r minimum distance between the two geometric entities.  If the surface\r element overlaps with the grid cell, the distance is 0.0.  Otherwise\r the distance is the minimum distance between the perimeter of the grid\r cell and the line segment (in 2d) or the perimeter of the triangle (in\r 3d).\r \r Here is an example of using this compute with the\r \"adapt_grid\"_adapt_grid.html command to adapt the grid around the\r upwind side of a circular object (flow is from the left boundary of\r the box).  The first adapt_grid command uses a threshold distance\r value of 0.5 to create refine grid cells once.  The second adapt_grid\r command uses a threshold distance value of 0.1 to create some of the\r grid cells closer to the surface a second time.\r \r NOTE: include pic\r \r Here is an example of how to use this compute with two successive\r \"adapt_grid\" commands.  The first refines once for grid cells within a\r distance of 0.3 from surface elements facing upwind.  The second\r refines again for grid cells within a distance of 0.1 from the surface\r elements.\r \r compute 5 distsurf/grid all all dir 1 0 0\r adapt_grid all refine value c_5 0.3 0.0 thresh less more\r adapt_grid all refine value c_5 0.1 0.0 thresh less more :pre\r \r For a 2d simulation of flow around a circle (flow from right to left),\r these commands produce this kind of adapted grid (click for a larger\r image):\r \r :c,image(JPG/adapt_surf_small.jpg,JPG/adapt_surf.jpg)\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid vector whose values are the\r distances of each grid cell from any of the surface elements.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The vector can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values for the vector will be in distance\r \"units\"_units.html.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.htmls#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_distsurf_grid.txt"
    },
    {
      "family": "compute",
      "style": "dt/grid",
      "description": "Calculate a current timestep for each grid cell in a grid cell group,\r based on the properties of particles currently in the cell and the\r grid cell size.  The per-grid cell timesteps can be output in a\r per-grid dump file for post analyses.  Or they can be used as input to\r the \"fix dt/reset\"_fix_dt_reset.html command to adjust the global\r timestep for a variable timestep simulation.  See this\r \"section\"_Section_howto.html#howto_18 of the manual for more\r information on variable timestep simulations.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The {tfraction} and {cfraction} arguments are both values from 0.0 to\r 1.0 which are applied to the transit term and collision term in the\r example formula for a candidate cell timestep below.\r \r :c,image(Eqs/cell_timestep.jpg)\r \r In practice, multiple transit-based timestep candidates are\r constructed based on the cell dimensions in each coordinate direction\r and the associated average particle velocity components in addition to\r the maximum most probable speed.  The selected cell timestep is the\r minumum of all candidate timesteps. The collision and transit\r fractions simply provide a user-defined safety margin for the\r collision time and transit time estimates. In \"(Bird2013)\"_#Bird2013,\r Bird recomnmends setting the collision fraction to 0.2, which is\r likely a good starting point for the selection of both of these\r fractions.\r \r The remaining 5 arguments specify either computes which calculate various per\r grid cell quantities or fixes which time average those\r per grid cell quantities.  The 5 quantities are per grid cell mean\r collision time (tau), temperature, and the xyz components of the\r velocity squared for particles in the grid cell. A mean collision time can be\r calculated with the \"compute lambda/grid\"_compute_lambda_grid.html command using the\r tau option. The \"compute grid\"_compute_grid.html command can calculate the other 4 quantities.\r The \"compute thermal/grid\"_compute_thermal_grid.html command can also\r compute a per grid cell temperature.\r \r This is done by specifying the tau, temperature, usq, vsq, wsq\r arguments like this:\r \r c_ID = compute with ID that calculates a per grid cell quantity as a vector output\r c_ID\\[m\\] = compute with ID that calculates a quantity as its Mth column of array output\r f_ID\\[m\\] = fix with ID that calculates a time-averaged quantity as a vector output\r f_ID\\[m\\] = fix with ID that calculates a time-averaged quantity as its Mth column of array output :ul\r \r See the Example section above for an example of how these arguments\r can be specified.\r \r IMPORTANT NOTE: If the IDs of one or more \"fix\r ave/grid\"_fix_ave_grid.html commands is used for these 5 arguments,\r they only produce output on timesteps that are multiples of their\r {Nfreq} argument.  Thus this compute can only be invoked on those\r timesteps.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid vector.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual for\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for the cell timestep.\r Likewise, split cells store no particles and will produce a zero result.\r This is because their sub-cells actually contain the particles that are\r geometrically inside the split cell.  Additionally, any cell that is able\r to store particles but does not have any particles when this compute is\r invoked produces a zero result.  Finally, a zero result is produced if any\r cell data to be used in the timestep calculation is\r zero (including temperature, speed, and mean collision time).\r \r The vector can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 4.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_dt_grid.txt"
    },
    {
      "family": "compute",
      "style": "eflux/grid",
      "description": "Define a computation that calculates components of the energy flux\r density vector for each grid cell in a grid cell group.  This is also\r called the heat flux density vector, and is based on the thermal\r velocity of the particles in each grid cell.  The values are tallied\r separately for each group of species in the specified mixture, as\r described in the Output section below.  See the mixture command for\r how a set of species can be partitioned into groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The values listed above rely on first computing and subtracting the\r center-of-mass (COM) velocity for all particles in the group and grid\r cell from each particle to yield a thermal velocity.  This thermal\r velocity is used to compute the components of the energy flux density\r vector, as described below.  This is in contrast to some of the values\r tallied by the \"compute grid temp\"_compute_grid.html command which\r simply uses the full velocity of each particle to compute a momentum\r or kinetic energy density.  For non-streaming simulations, the two\r results should be similar, but for streaming flows, they will be\r different.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set of particles to compute the formulas below.  \r \r Note that the center-of-mass (COM) velocity that is subtracted from\r each particle to yield a thermal velocity for each particle, as\r described below, is also computed over one large set of particles\r (across all timesteps), in contrast to using a COM velocity computed\r only for particles in the current timestep, which is what the \"compute\r sonine/grid\"_compute_sonine_grid.html command does.\r \r Note that this is a different form of averaging than taking the values\r produced by the formulas below for a single timestep, summing those\r values over the sampling timesteps, and then dividing by the number of\r sampling steps.\r \r :line\r \r Calculation of the energy flux density is done by first calcuating the\r center-of-mass (COM) velocity of particles for each group with a grid\r cell.  This is done as follows:\r \r COMx = Sum_i (mass_i Vx_i) / Sum_i (mass_i)\r COMy = Sum_i (mass_i Vy_i) / Sum_i (mass_i)\r COMz = Sum_i (mass_i Vz_i) / Sum_i (mass_i)\r Cx = Vx - COMx\r Cy = Vy - COMy\r Cz = Vz - COMz\r Csq = Cx*Cx + Cy*Cy + Cz*Cz :pre\r \r The COM velocity is (COMx,COMy,COMz).  The thermal velocity of each\r particle is (Cx,Cy,Cz), i.e. its velocity minus the COM velocity of\r particles in its group and cell.\r \r The {heatx}, {heaty}, {heatz} values compute the components of the\r energy flux density vector due to particles in the group as follows:\r \r heatx = 0.5 * fnum/volume Sum_i (mass_i Cx Csq)\r heaty = 0.5 * fnum/volume Sum_i (mass_i Cy Csq)\r heatz = 0.5 * fnum/volume Sum_i (mass_i Cz Csq) :pre\r \r Note that if particle weighting is enabled via the \"global\r weight\"_global.html command, then the volume used in the formula is\r divided by the weight assigned to the grid cell.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if\r {momxx} and {momxy} values were specified as keywords, then the first\r two columns would be {momxx} and {momxy} for the first group, the 3rd\r and 4th columns would be {momxx} and {momxy} for the second group, etc.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html of\r energy flux density = energy-velocity/volume units.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_eflux_grid.txt"
    },
    {
      "family": "compute",
      "style": "fft/grid",
      "description": "Define a computation that performs forward FFTs on per-grid values.  \r This can be useful, for example, in calculating the energy spectrum\r of a turbulent flow.\r \r The defined grid must be a regular one-level grid (not hierarchical)\r with an even number of grid cells in each dimension.  Depending on the\r \"dimension\"_dimension.html of the simulation, either 2d or 3d FFTs\r will be performed.  Because FFTs assume a periodic field, the\r simulation domain should be periodic in all dimensions, as set by the\r \"boundary\"_boundary.html command, though SPARTA does not check for\r that.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.  The values over many sampling\r timesteps can be averaged by the \"fix ave/grid\"_fix_ave_grid.html\r command.\r \r A forward FFT is perfomed on each input value independently.\r \r Each listed input can be the result of a \"compute\"_compute.html or\r \"fix\"_fix.html or the evaluation of a \"variable\"_variable.html, all of\r which must generate per-grid quantities.\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  The compute must generate a\r per-grid vector or array.  See the individual \"compute\"_compute.html\r doc page for details.  If no bracketed integer is appended, the vector\r calculated by the compute is used.  If a bracketed integer is\r appended, the Ith column of the array calculated by the compute is\r used.  Users can also write code for their own compute styles and \"add\r them to SPARTA\"_Section_modify.html.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  The fix must generate a\r per-grid vector or array.  See the individual \"fix\"_fix.html doc page\r for details.  Note that some fixes only produce their values on\r certain timesteps, which must be compatible with when this compute\r references the values, else an error results.  If no bracketed integer\r is appended, the vector calculated by the fix is used.  If a bracketed\r integer is appended, the Ith column of the array calculated by the fix\r is used.  Users can also write code for their own fix style and \"add\r them to SPARTA\"_Section_modify.html.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  It must be a \"grid-style\r variable\"_variable.html.  Such a variable defines a formula which can\r reference stats keywords or invoke other computes, fixes, or variables\r when they are evaluated.  So this is a very general means of creating\r a per-grid input to perform an FFT on.\r \r :line\r \r If the {sum} keyword is set to {yes}, the results of all FFTs\r will be summed together, grid value by grid value, to create\r a single output.\r \r The result of each FFT is scaled by the {sfactor} value of\r the {scale} keyword, whose default is 1.0.  Note that forward FFTs do\r not perform any scaling of their own; backward FFTs scale each output\r value by N = # of points in the FFT grid.\r \r If the {conjugate} keyword is set to {no}, the result of each FFT is 2\r values for each grid point, the real and imaginary parts of a complex\r number.  If the {conjugate} keyword is set to {yes}, the complex value\r for each grid point is multiplied by its complex conjugate to yield a\r single real-valued number for each grid point.  Note that this value\r is effectively the squared length of the complex 2-vector with real\r and imaginary components.\r \r If one or more of the {kx}, {ky}, {kz}, or {kmag} keywords are set to\r {yes}, then one or more extra columns of per-grid output is generated.\r For {kx} the x-component of the K-space wavevector is generated.\r Similarly for {ky} and {kz}.  For {kmag} the length of each K-space\r wavevector is generated.  These values can be useful, for example, for\r histogramming an energy spectrum computed from the FFT of a velocity\r field, as a function of wavelength or a component of the wavelength.\r \r Note that the wavevector for each grid cell is indexed as (Kx,Ky,Kz).\r Those indices are the x,y,z components output by the {kx}, {ky}, {kz}\r keywords.  The total wavelength, which is output by the {kmag}\r keyword, is sqrt(Kx^2 + Ky^2 + Kz^2) for 3d models and sqrt(Kx^2 +\r Ky^2) for 2d models.  For all keywords, the Kx,Ky,Kz represent\r distance from the origin in a periodic sense.  Thus for a grid that is\r NxMxP, the Kx values associated with the x-dimension and used in those\r formulas are not Kx = 0,1,2 ... N-2,N-1.  Rather they are Kx = 0,1,2,\r ... N/2-1, N/2, N/2-1, ... 2,1.  Similary for Ky in the y-dimension\r with a max index of M/2, and Kz in the z-dimension with a max index of\r P/2.\r \r :line\r :line\r \r [Output info:]\r \r The number of per-grid values ouptut by this compute depends on the\r optional keyword settings.  The number of FFTs is equal to the number\r of specified input values.\r \r There are 2 columns of output per FFT if {sum} = no and {conjugate} =\r no, with real and imaginary components for each FFT.  There is 1\r column of output per FFT if {sum} = no and {conjugate} = yes.  There\r are 2 columns of output if {sum} = yes and {conjugate} = no, with real\r and imaginary components for the sum of all the FFTs.  There is one\r column of output for {sum} = yes and {conjugate} = yes.  For all these\r cases, there is one extra column of output for each of the {kx}, {ky},\r {kz}, {kmag} keywords if they are set to {yes}.  The extra columns\r come before the FFT columns, in the order {kx}, {ky}, {kz}, {kmag}.\r Thus is only {ky} and {kmag} are set to yes, there will be 2 extra\r columns, the first for {ky} and the 2nd for {kmag}.\r \r If the total number of output columns = 1, then this compute produces\r a per-grid vector as output.  Otherwise it produces a per-grid array.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid vector or array values will be in the \"units\"_units.html\r appropriate to the FFT operations as described above.  The K-space\r wavevector magnitudes are effectively unitless, e.g. sqrt(Kx^2 + Ky^2\r + Kz^2) where Kx,Ky,Kz are integers.  The FFT values can be real or\r imaginary or squared values in K-space resulting from FFTs of per-grid\r quantities in whatever units the specified input values represent.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_fft_grid.txt"
    },
    {
      "family": "compute",
      "style": "gas/collision/grid",
      "description": "Count the number of gas-phase collisions bewteen pairs of particles\r which occur in each grid cell during the current timestep.  Only gas\r collisions which do not result in chemical reactions are counted by\r this command.  See the related \"compute\r gas/reaction/grid\"_compute_gas_reaction_grid.html command to count\r collisions which induce reactions.\r \r Only collisions within grid cells in the grid group specified by\r {group-ID} and pairs of particles with both species in the mixture\r specified by {mix-ID} are included.  See the \"group grid\"_group.html\r command for info on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command or used as inputs to the \"compute\r reduce\"_compute_reduce.html command.  The values can also be time\r averaged by the \"fix ave/grid\"_fix_ave_grid.html command.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid vector with the count of collisions\r for each grid cell.\r \r The vector can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r :line",
      "docFile": "compute_gas_collision_grid.txt"
    },
    {
      "family": "compute",
      "style": "gas/collision/tally",
      "description": "Tally various values for each gas-phase collision of two particles\r during the current timestep.  Only gas collisions which do not result\r in chemical reactions are tallied by this command.  See the related\r \"compute gas/reaction/tally\"_compute_gas_reaction_tally.html command\r to tally collisions which induce reactions.  It provides different\r tally value options than this compute.\r \r Only collisions within grid cells in the grid group specified by\r {group-ID} and pairs of particles with both species in the mixture\r specified by {mix-ID} are included.  See the \"group grid\"_group.html\r command for info on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump tally\"_dump.html command.  Currently this is the only option for\r accessing the tallied values.\r \r :line\r \r The {id/cell} value is the ID of the grid cell in which the collision\r takes place.\r \r The {id1} and {id2} values are the IDs of the colliding particles.\r Note that particle IDs are generated randomly.  Thus multiple\r particles in the system can potentially have the same ID.  See\r \"Section 6.17\"_Section_howto_17 for more details on particle IDs.\r \r The {type1} and {type2} values are the integer index for the particle\r species of the colliding particles.  They are values from 1 to\r Nspecies. The values corresponds to the order in which species were\r defined via the \"species\"_species.html command.  See \"Section\r 6.17\"_Section_howto_17 for more details on particle types.\r \r The {vx1/pre}, {vy1/pre}, {vz1/pre}, {vx2/pre}, {vy2/pre}, {vz2/pre}\r values are the velocity components of the two particles before the\r collision.\r \r The {vx1/post}, {vy1/post}, {vz1/post}, {vx2/post}, {vy2/post},\r {vz2/post} values are the velocity components of the two particles\r after the collision.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-tally array, with the number of columns\r equal to the number of values.\r \r The array can be accessed by any command that uses per-tally values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-tally array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  All the\r velocity keywords are in velocity units.\r \r :line",
      "docFile": "compute_gas_collision_tally.txt"
    },
    {
      "family": "compute",
      "style": "gas/reaction/grid",
      "description": "Count the number of gas-phase reactions bewteen pairs of particles\r which occur in each grid cell during the current timestep.  Only gas\r collisions which result in chemical reactions are counted by this\r command, as defined by the \"react\"_react.html command and the list of\r reactions it reads from a file.  See the related \"compute\r gas/collision/grid\"_compute_gas_reaction_grid.html command to count\r collisions which do not induce reactions.\r \r Only reactions within grid cells in the grid group specified by\r {group-ID} and pairs of reactant particles with both species in the\r mixture specified by {mix-ID} are included.  See the \"group\r grid\"_group.html command for info on how grid cells can be assigned to\r grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command or used as inputs to the \"compute\r reduce\"_compute_reduce.html command.  The values can also be time\r averaged by the \"fix ave/grid\"_fix_ave_grid.html command.\r \r :line\r \r The {mode} argument determines how the reactions in each grid cell are\r counted.\r \r If {mode} is specified as {all} then a single count of all reactions\r is produced for each grid cell.  The compute calculates this count as\r a per-grid vector.\r \r If {mode} is specified as {every} then an individual count for each\r reaction is produced for each grid cell.  The number of possible\r reactions M are those listed in the file read by the\r \"react\"_react.html commmand.  The compute calculates these counts as a\r per-grid array, even if M = 1.\r \r If {mode} is specified as {select} then a count for each reaction in\r the specified list of numeric indices is produced for each grid cell.\r Each index must be a number from 1 to M, where M is the number of\r reactions listed in the file read by the \"react\"_react.html commmand.\r The compute calculates these counts as a per-grid array, even if only\r a single index is specified.\r \r :line\r \r [Output info:]\r \r If no numeric keywords are used, then this compute calculates a\r per-grid vector with the count of all reactions for each grid cell.\r \r If one or more numeric keywords are used, then this compute calculates\r a per-grid array.  The first column wlll be the count of all reactions.\r The remaining columns (one per numeric keyword) will be the counts of only\r the specific reactions listed.\r \r The vector or array can be accessed by any command that uses per-grid\r values from a compute as input.  See \"Section\r 6.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r :line",
      "docFile": "compute_gas_reaction_grid.txt"
    },
    {
      "family": "compute",
      "style": "gas/reaction/tally",
      "description": "Tally various values for each gas-phase reaction of two particles\r during the current timestep.  Only gas collisions which result in a\r chemical reactions are tallied by this command, as enabled by the\r \"react\"_react.html command.  See the related \"compute\r gas/collide/tally\"_compute_gas_collide_tally.html command to tally\r collisions which do not induce reactions.  It provides different tally\r value options than this compute.\r \r Only collisions within grid cells in the grid group specified by\r {group-ID} and pairs of particles with both species in the mixture\r specified by {mix-ID} are included.  See the \"group grid\"_group.html\r command for info on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump tally\"_dump.html command.  Currently this is the only option for\r accessing the tallied values.\r \r Note that if a gas-phase reaction takes place then a collision of two\r particles can result in 1, 2, or 3 particles being produced.  The\r species of the 1, 2, or product particles may be different than the\r species of the reactants.  All of these attributes can be tallied\r using the values explained next.\r \r :line\r \r The {reaction} value is the index of which reaction occurred.  It is a\r number from 1 to N, where N is the number of reactions defined by the\r \"react\"_react.html command.\r \r The {id1/pre} and {id2/pre} values are the IDs of the reacting\r particles before the reaction.  Note that particle IDs are generated\r randomly.  Thus multiple particles in the system can potentially have\r the same ID.  See \"Section 6.17\"_Section_howto_17 for more details on\r particle IDs.\r \r The {id1/post}, {id2/post}, and {id3/post} values are the\r post-reaction IDs of the 1, 2, or 3 resulting particles.  If one of\r the particles vanished in the reaction, then {id2/post} will be 0.  If\r {id3/post} is 0, no 3rd particle was created by the reaction,\r otherwise it is the random ID assigned to the newly created particle.\r \r The {type1/pre} and {type2/pre} value are the integer indices for the\r species of the reacting particles before the reaction.  They are\r values from 1 to Nspecies.  The values correspond to the order in\r which species were defined via the \"species\"_species.html command.\r See \"Section 6.17\"_Section_howto_17 for more details on particle\r types.\r \r The {type1/post}, {type/post2}, and {type/post3} values are the\r post-collision integer indices for the particle species of the 0, 1,\r or 2 resulting particles.  If one of the particles vanished in the\r reaction, then {type2/post} will be 0.  If {type/post3} is 0, no 3rd\r particle was created by the reaction, otherwise it is the species\r index of the newly created particle.\r \r The values starting with {v} for particles 1 and 2 with a {pre} suffix\r are the velocity components of the two reacting particles before the\r reaction.\r \r The values starting with {v} for particles 1 and 2 and 3 with a {post}\r suffix are the velocity components of the 1, 2, or 3 resulting\r particles after the reaction.  Velocity components will be zero if the\r 2nd or 3rd particle does not exist due to the reaction.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-tally array, with the number of columns\r equal to the number of values.\r \r The array can be accessed by any command that uses per-tally values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-tally array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  All the\r velocity components are in velocity units.\r \r :line",
      "docFile": "compute_gas_reaction_tally.txt"
    },
    {
      "family": "compute",
      "style": "grid",
      "description": "Define a computation that calculates one or more values for each grid\r cell in a grid cell group, based on the particles in the cell.  The\r values are tallied separately for each group of species in the\r specified mixture, as described in the Ouput section below.  See the\r \"mixture\"_mixture.html command for how a set of species can be\r partitioned into groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set of particles to compute the formulas below.\r \r Note that for most of the values, this is a different form of\r averaging than taking the values produced by the formulas below for a\r single timestep, summing those values over the sampling timesteps, and\r then dividing by the number of sampling steps.\r \r :line\r :line\r \r The {n} value counts the number of particles in each group.  When\r accumulated over multiple sampling steps, this value is normalized by\r the number of sampling steps.\r \r The {nrho} value computes the number density for the grid cell volume\r due to particles in each group:\r \r Nrho = fnum/volume * N :pre\r \r N is the number of particles (same as the {n} keyword), fnum is the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command, and volume is the flow volume of the grid cell.  When\r accumulated over multiple sampling steps, this value is normalized by\r the number of sampling steps.  Note that if particle weighting is\r enabled via the \"global weight\"_global.html command, then the volume\r used in the formula is divided by the weight assigned to the grid\r cell.\r \r The {nfrac} value computes the number fraction of particles in each\r group:\r \r Nfrac = Ngroup / Ntotal\r \r Ngroup is the count of particles in the group and Ntotal is the total\r number of particles in all groups in the mixture.  Note that this\r total is not (necessarily) all particles in the cell.\r \r :line\r \r The {mass} value computes the average mass of particles in each group:\r \r Mass = Sum_i (mass_i) / N :pre\r \r where Sum_i is a sum over particles in the group.\r \r The {massrho} value computes the mass density for the grid cell volume\r due to particles in each group:\r \r Massrho = fnum/volume * Sum_i (mass_i) :pre\r \r where Sum_i is a sum over particles in the group, fnum is the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command, and volume is the flow volume of the grid cell.  When\r accumulated over multiple sampling steps, this value is normalized by\r the number of sampling steps.  Note that if particle weighting is\r enabled via the \"global weight\"_global.html command, then the volume\r used in the formula is divided by the weight assigned to the grid\r cell.\r \r The {massfrac} value computes the mass fraction of particles in each\r group:\r \r Massfrac = Sum_i (mass_i) / Masstotal :pre\r \r where Sum_i is a sum over particles in the group and Masstotal is the\r total mass of particles in all groups in the mixture.  Note that this\r total is not (necessarily) the mass of all particles in the cell.\r \r :line\r \r The {u}, {v}, {w} values compute the components of the mass-weighted\r average velocity of particles in each group:\r \r U = Sum_i (mass_i Vx_i) / Sum_i (mass_i)\r V = Sum_i (mass_i Vy_i) / Sum_i (mass_i)\r W = Sum_i (mass_i Vz_i) / Sum_i (mass_i) :pre\r \r This is the same as the center-of-mass velocity of particles in each\r group.\r \r The {usq}, {vsq}, {wsq} values compute the average mass-weighted\r squared components of the velocity of particles in each group:\r \r Usq = Sum_i (mass_i Vx_i Vx_i) / Sum_i (mass_i)\r Vsq = Sum_i (mass_i Vy_i Vy_i) / Sum_i (mass_i)\r Wsq = Sum_i (mass_i Vz_i Vz_i) / Sum_i (mass_i) :pre\r \r :line\r \r The {ke} value computes the average kinetic energy of particles in\r each group:\r \r Vsq = Vx*Vx + Vy*Vy + Vz*Vz\r KE = Sum_i (1/2 mass_i Vsq_i) / N :pre\r \r Note that this is different than the group's contribution to the\r average kinetic energy of entire grid cells.  That can be calculated\r by multiplying the {ke} quantity by the {n} quantity.\r \r The {temp} value first computes the average kinetic energy of\r particles in each group, as for the {ke} value.  This is then\r converted to a temperature {T} by the following formula where {kB} is\r the Boltzmann factor:\r \r Vsq = Vx*Vx + Vy*Vy + Vz*Vz\r KE = Sum_i (1/2 mass_i Vsq_i) / N\r T = KE / (3/2 kB) :pre\r \r Note that this definition of temperature does not subtract out a net\r streaming velocity for particles in the grid cell, so it is not a\r thermal temperature when the particles have a non-zero streaming\r velocity.  See the \"compute thermal/grid\"_compute_thermal_grid.html\r command to calculate thermal temperatures after subtracting out\r streaming components of velocity.\r \r :line\r \r The {erot} value computes the average rotational energy of particles\r in each group:\r \r Erot = Sum_i (erot_i) / N :pre\r \r Note that this is different than the group's contribution to the\r average rotational energy of entire grid cells.  That can be\r calculated by multiplying the {erot} quantity by the {n} quantity.\r \r The {trot} value computes a rotational temperature by the following\r formula where {kB} is the Boltzmann factor:\r \r Trot = (2/kB) Sum_i (erot_i) / Sum_i (dof_i) :pre\r \r Dof_i is the number of rotational degrees of freedom for particle i.\r \r :line\r \r The {evib} value computes the average vibrational energy of particles\r in each group:\r \r Evib = Sum_i (evib_i) / N :pre\r \r Note that this is different than the group's contribution to the\r average vibrational energy of entire grid cells.  That can be\r calculated by multiplying the {evib} quantity by the {n} quantity.\r \r The {tvib} value computes a classical definition of vibrational\r temperature, valid for continous distributions of vibrational energy,\r by the following formula where {kB} is the Boltzmann factor:\r \r Tvib = (2/kB) Sum_i (evib_i) / Sum_i (dof_i) :pre\r \r Dof_i is the number of vibrational degrees of freedom for particle i.\r \r :line\r \r The {pxrho}, {pyrho}, {pzrho} values compute components of momentum\r density for the grid cell volume due to particles in each group:\r \r Pxrho = fnum/volume * Sum_i (mass_i * Vx_i)\r Pyrho = fnum/volume * Sum_i (mass_i * Vy_i)\r Pzrho = fnum/volume * Sum_i (mass_i * Vz_i) :pre\r \r where Sum_i is a sum over particles in the group, fnum is the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command, and volume is the flow volume of the grid cell.  When\r accumulated over multiple sampling steps, this value is normalized by\r the number of sampling steps.  Note that if particle weighting is\r enabled via the \"global weight\"_global.html command, then the volume\r used in the formula is divided by the weight assigned to the grid\r cell.\r \r The {kerho} value computes the kinetic energy density for the grid\r cell volume due to particles in each group:\r \r Vsq = Vx*Vx + Vy*Vy + Vz*Vz\r KErho = fnum/volume * Sum_i (mass_i * Vsq_i) :pre\r \r where Sum_i is a sum over particles in the group, fnum is the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command, and volume is the flow volume of the grid cell.  When\r accumulated over multiple sampling steps, this value is normalized by\r the number of sampling steps.  Note that if particle weighting is\r enabled via the \"global weight\"_global.html command, then the volume\r used in the formula is divided by the weight assigned to the grid\r cell.\r \r :line\r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {n} and {u} values were specified as keywords, then the first two\r columns would be {n} and {u} for the first group, the 3rd and 4th\r columns would be {n} and {u} for the second group, etc.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  {N} is\r unitless.  {Nrho} is in 1/distance^3 units for 3d simulations and\r 1/distance^2 units for 2d simulations.  {Mass} is in mass units.\r {Massrho} is in is in mass/distance^3 units for 3d simulations and\r mass/distance^2 units for 2d simulations.  {U}, {v}, and {w} are in\r velocity units.  {Usq}, {vsq}, and {wsq} are in velocity squared\r units.  {Ke}, {erot}, and {evib} are in energy units.  {Temp} and\r {trot} and {tvib} are in temperature units.  {Pxrho}, {pyrho}, {pzrho}\r are in momentum/distance^3 units for 3d simulations and\r momentum/distance^2 units for 2d simulations, where momentum is in\r units of mass*velocity.  {Kerho} is in units of energy/distance^3\r units for 3d simulations and energy/distance^2 units for 2d\r simulations.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_grid.txt"
    },
    {
      "family": "compute",
      "style": "isurf/grid",
      "description": "Define a computation that calculates one or more values for each grid\r cell in a grid cell group, based on the particles that collide with\r the implicit surfaces in that grid cell.  The values are summed for\r each group of species in the specified mixture.  See the\r \"mixture\"_mixture.html command for how a set of species can be\r partitioned into groups.  Only grid cells in the grid group specified\r by {group-ID} are included in the calculations.  See the \"group\r grid\"_group.html command for info on how grid cells can be assigned to\r grid groups.\r \r Implicit surface elements are triangles for 3d simulations and line\r segments for 2d simulations.  Unlike explicit surface elements, each\r triangle or line segment is wholly contained within a single grid\r cell.  See the \"read_isurf\"_read_isurf.html command for details.\r \r This command can only be used for simulations with implicit surface\r elements.  See the similar \"compute surf\"_compute_surf.html command\r for use with simulations with explicit surface elements.\r \r Note that when a particle collides with a surface element, it can\r bounce off (possibly as a different species), be captured by the\r surface (vanish), or a 2nd particle can also be emitted.  The formulas\r below account for all the possible outcomes.  For example, the kinetic\r energy flux {ke} onto a suface element for a single collision includes\r a positive contribution from the incoming particle and negative\r contributions from 0, 1, or 2 outgoing particles.  The exception is\r the {n} and {nwt} values which simply tally counts of particles\r colliding with the surface element.\r \r Also note that all values for a collision are tallied based on the\r species group of the incident particle.  Quantities associated with\r outgoing particles are part of the same tally, even if they are in\r different species groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles striking the surface elements within the grid cell at each\r sampling timestep were combined together into one large set to compute\r the formulas below.  The answer is then divided by the number of\r sampling timesteps if it is not otherwise normalized by the number of\r particles.  Note that in general this is a different normalization\r than taking the values produced by the formulas below for a single\r timestep, summing them over the sampling timesteps, and then dividing\r by the number of sampling steps.  However for the current values\r listed below, the two normalization methods are the same.\r \r NOTE: If particle weighting is enabled via the \"global\r weight\"_global.html command, then all of the values below are scaled\r by the weight assigned to the grid cell in which the particle\r collision with the surface element occurs.  The only exception is the\r the {n} value, which is NOT scaled by the weight; it is a simple count\r of particle collisions with surface elements in the grid cell.\r \r :line\r \r The meaning of all the value keywords and the formulas for calculating\r these quantities is exactly the same as described by the \"compute\r surf\"_compute_surf.html command.\r \r The only difference is that the quantities are calculated on a per\r grid cell basis, summing over all the surface elements in that grid\r cell.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {n} and {u} values were specified as keywords, then the first two\r columns would be {n} and {u} for the first group, the 3rd and 4th\r columns would be {n} and {u} for the second group, etc.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html\r appropriate to the individual values as described above. {N} is\r unitless.  {Press}, {px}, {py}, {pz}, {shx}, {shy}, {shz} are in in\r pressure units.  {Ke}, {erot}, {evib}, and {etot} are in\r energy/area-time units for 3d simulations and energy/length-time units\r for 2d simulations.\r \r :line",
      "docFile": "compute_isurf_grid.txt"
    },
    {
      "family": "compute",
      "style": "ke/particle",
      "description": "Define a computation that calculates the per-atom translational\r kinetic energy for each particle.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump particle\"_dump.html command.\r \r The kinetic energy is\r \r Vsq = Vx*Vx + Vy*Vy + Vz*Vz\r KE = 1/2 m Vsq :pre\r \r where m is the mass and (Vx,Vy,Vz) are the velocity components of the\r particle.\r \r [Output info:]\r \r This compute calculates a per-particle vector, which can be accessed\r by any command that uses per-particle values from a compute as input.\r \r The vector can be accessed by any command that uses per-particle\r values from a compute as input.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r The per-particle vector values will be in energy \"units\"_units.html.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_ke_particle.txt"
    },
    {
      "family": "compute",
      "style": "lambda/grid",
      "description": "This command calculates properties related to the gas collision mean\r free path for each grid cell based on the number density {nrho} and\r thermal tempearture {temp} of particles in the cell.\r \r If the {lambda} value is specified, the mean free path (MFP) of\r particles between collisions will be computed. If the {tau} value is\r specified, the mean collision time (MCT) between molecular collisions\r will be computed.\r \r If one or more of the {knall} or {knx} or {kny} or {knz} values are\r specified, the dimensionless Knudsen number will be calculated, which\r is the ratio of the MFP to the cell size.  For {knall}, the cell size\r is the average of the three grid cell side lengths (or two cell\r lengths for a 2d simulation).  For {knx}, {kny}, or {knz}, the cell\r size is the single cell side length in the corresponding x,y,z\r dimension.\r \r The Knudsen number can be useful for estimating the optimal grid cell\r size when adapting the grid, e.g. via the \"adapt_grid\"_adapt_grid.html\r or \"fix adapt/grid\"_fix_adapt_grid.html commands, as well as for\r estimating the optimal timestep size.\r \r Note that unlike other computes which calculate per-grid values, this\r compute does not take a \"group-ID\" for a grid cell group or a particle\r \"mixture\"_mixture.html ID as an argument.  This is because it uses the\r number density and thermal temperature calculated by other computes or\r fixes as input, and those computes or fixes use grid group IDs or\r mixture IDs as part of their computations.\r \r The results of this compute can be used by different commands in\r different ways.  For example, the values can be output by the \"dump\r grid\"_dump.html command.  Or as indicated above the compute can be\r used as an argument for the \"adapt_grid\"_adapt_grid.html or \"fix\r adapt\"_fix_adapt.html commands, e.g. to trigger grid cell refinement\r or coarsening based on a mean free path metric.\r \r :line\r \r The formula used to calculate the mean free path (lambda) is given in\r \"(Bird94)\"_#Bird94 as equation 4.77:\r \r :c,image(Eqs/lambda.png)\r \r IMPORTANT NOTE: Prior to October 2024, a simpler formula was used for\r the mean free path, also from \"(Bird94)\"_#Bird94, equation 4.65:\r \r :c,image(Eqs/lambda_old.jpg)\r \r The new formula is more accurate as it uses the number densities and\r VSS parameters of all gas species (sum over {s}) in the system, as\r opposed to using the total number density and the VSS parameters of\r only one species.  This can make a significant difference for gas\r mixtures, in particular for reacting flow problems where the\r composition of the mixture changes significantly over time.\r \r The formula used to calculate the mean collision time (tau) is given in\r \"(Bird94)\"_#Bird94 as equation 1.38 combined with 4.75:\r \r :c,image(Eqs/tau.png)\r \r These two formulas are the exact mean free path (MFP) and mean\r collision time (MCT) for a multi-species mixture, suitable for\r estimating optimal grid cell sizes and timestep as explained above.\r \r The {dref} and {Tref} and {omega} values in the two formulas are\r collision properties for a pair of species in the flow, subscripted as\r *pq*.  Specifically, {dref} is the diameter of molecules of the\r species pair, {Tref} is the reference temperature, and {omega} is the\r viscosity temperature-dependence for the species pair.\r \r In the two formulas, {n_q} is the number density of species {q} within\r a grid cell.  {T} is the thermal temperature of all particles (all\r species) in a grid cell.  These per-grid values are specified for use\r by this command as the {nrho} and {temp} arguments in one of these\r forms:\r \r c_ID\\[*\\] = compute with ID that calculates nrho for each species as an array output\r f_ID\\[*\\] = fix with ID that calculates a time-averaged nrho for each species as an array output :pre\r \r c_ID = compute with ID that calculates temp for all particles a vector output\r c_ID\\[m\\] = compute with ID that calculates temp for all particles as its Mth column of array output\r f_ID = fix with ID that calculates a time-averaged temp for all particles as a vector output\r f_ID\\[m\\] = fix with ID that calculates a time-averaged temp for all particles as its Mth column of array output :pre\r \r The {temp} argument can also be specified as NULL, which drops the\r (Tref/T) and (T/Tref) ratios from the two formulas above.\r \r Note that because the MFP and MFT formulas require a number density\r nrho for all species in the system, you should use a \"compute\r grid\"_compute_grid.html command like this:\r \r compute 1 grid all species nrho :pre\r \r which calculates per-species nrho values for each gird cell.  It can\r then be reference by this command as c_ID\\[*\\].  Or similarly for a\r \"fix ave/grid\"_fix_ave_grid.html command that takes c_ID\\[*\\] as an\r argument, which is then referenced by this command as f_ID\\[*\\].\r \r For the compute or fix referenced by the {temp} argument, this is\r meant in the two formulas to be the aggregate thermal temperature of\r all the particles in a grid cell.\r \r For systems with a zero of small streaming velocity, this can be\r calculated using a command like this:\r \r compute 2 grid all all temp :pre\r \r which uses the velocity of each particle to calculate a temperature.\r \r For systems with a streaming flow, an appropriate therml temperature\r can be calculated by the \"compute\r thermal/grid\"_compute_thermal_grid.html thermal/grid command.  Its doc\r page explain that the the center-of-mass velocity for the particles in\r each grid cell is subtracted from each particle's individual velocity\r to yield its thermal velocity, from which a thermal temperature is\r calculated.  The corresponding command to use with this compute would\r be:\r \r compute 2 thermal/grid all all temp :pre\r \r Note that using fixes, rather than computes, for the {nrho} and {temp}\r arguments will typically result in less noisy MFP and MCT values, due\r to the time averaging which the \"fix ave/grid\"_fix_ave_grid.html\r command can perform.\r \r IMPORTANT NOTE: If the IDs of one or more \"fix\r ave/grid\"_fix_ave_grid.html commands is used for the {nrho} or {temp}\r arguments, they only produce output on timesteps that are multiples of\r their {Nfreq} argument.  Thus this compute can only be invoked on\r those timesteps.\r \r :line\r \r [Output info:]\r \r If only one output value is specified, this compute outputs a per-grid\r vector. Otherwise it outputs a per-grid array with two or more\r columns, in the order the output values were specified.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all the\r individual values.  Likewise, split cells store no particles and will\r produce a zero result.  This is because their sub-cells actually\r contain the particles that are geometrically inside the split cell.\r \r The vector or array can be accessed by any command that uses per-grid\r values from a compute as input.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r The per-grid values for a column of output for {lambda} will be in\r \"distance units\"_units.html.  A column of output for {tau} will be\r \"time units\"_units.html.  Columns of output for {knall} or {knx} or\r {kny} or {knz} will be dimensionless.\r \r If the value of {nrho} for a grid cell is 0.0 (no particles), its MFP\r and MCT will be set to 1.0e20 (infinite length and time).  This\r \"infinite\" MFP value will also be used for the calculation of Knudsen\r numbers.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_lambda_grid.txt"
    },
    {
      "family": "compute",
      "style": "pflux/grid",
      "description": "Define a computation that calculates components of the momemtum flux\r density tensor for each grid cell in a grid cell group.  This is\r equivalent to the kinetic energy density tensor, and is based on the\r thermal velocity of the particles in each grid cell.  The values are\r tallied separately for each group of species in the specified mixture,\r as described in the Output section below.  See the mixture command for\r how a set of species can be partitioned into groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The values listed above rely on first computing and subtracting the\r center-of-mass (COM) velocity for all particles in the group and grid\r cell from each particle to yield a thermal velocity.  This thermal\r velocity is used to compute the components of the momentum flux\r density tensor, as described below.  This is in contrast to some of\r the values tallied by the \"compute grid temp\"_compute_grid.html\r command which simply uses the full velocity of each particle to\r compute a momentum or kinetic energy density.  For non-streaming\r simulations, the two results should be similar, but for streaming\r flows, they will be different.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set of particles to compute the formulas below.  \r \r Note that the center-of-mass (COM) velocity that is subtracted from\r each particle to yield a thermal velocity for each particle, as\r described below, is also computed over one large set of particles\r (across all timesteps), in contrast to using a COM velocity computed\r only for particles in the current timestep, which is what the \"compute\r sonine/grid\"_compute_sonine_grid.html command does.\r \r Note that this is a different form of averaging than taking the values\r produced by the formulas below for a single timestep, summing those\r values over the sampling timesteps, and then dividing by the number of\r sampling steps.\r \r :line\r \r Calculation of the momentum flux density is done by first calcuating\r the center-of-mass (COM) velocity of particles for each group within a\r grid cell.  This is done as follows:\r \r COMx = Sum_i (mass_i Vx_i) / Sum_i (mass_i)\r COMy = Sum_i (mass_i Vy_i) / Sum_i (mass_i)\r COMz = Sum_i (mass_i Vz_i) / Sum_i (mass_i)\r Cx = Vx - COMx\r Cy = Vy - COMy\r Cz = Vz - COMz :pre\r \r The COM velocity is (COMx,COMy,COMz).  The thermal velocity of each\r particle is (Cx,Cy,Cz), i.e. its velocity minus the COM velocity of\r particles in its group and cell.\r \r The {momxx}, {momyy}, {momzz} values compute the diagonal components\r of the momentum flux density tensor due to particles in the group as\r follows:\r \r momxx = fnum/volume Sum_i (mass_i Cx^2)\r momyy = fnum/volume Sum_i (mass_i Cy^2)\r momzz = fnum/volume Sum_i (mass_i Cz^2) :pre\r \r The {momxy}, {momyz}, {momxz} values compute the off-diagonal\r components of the momentum flux density tensor due to particles in the\r group as follows:\r \r momxy = fnum/volume Sum_i (mass_i Cx Cy)\r momyz = fnum/volume Sum_i (mass_i Cy Cz)\r momxz = fnum/volume Sum_i (mass_i Cx Cz) :pre\r \r Note that if particle weighting is enabled via the \"global\r weight\"_global.html command, then the volume used in the formula is\r divided by the weight assigned to the grid cell.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if\r {momxx} and {momxy} values were specified as keywords, then the first\r two columns would be {momxx} and {momxy} for the first group, the 3rd\r and 4th columns would be {momxx} and {momxy} for the second group, etc.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html of\r momentum flux density = energy density = energy/volume units.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_pflux_grid.txt"
    },
    {
      "family": "compute",
      "style": "property/grid",
      "description": "Define a computation that simply stores grid attributes for each grid\r cell in a grid cell group.  This is useful for values which can be\r used by other \"output commands\"_Section_howto.html#howto_4 that take\r computes as inputs.  See for example, the \"compute\r reduce\"_compute_reduce.html, \"fix ave/grid\"_fix_ave_grid.html, \"dump\r grid\"_dump.html, and \"grid-style variable\"_variable.html commands.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculation.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r :line\r \r {Id} is the grid cell ID.  In SPARTA each grid cell is assigned a\r unique ID which represents its logical location within the\r hierarchical grid.  This ID is stored as an integer such as 5774983,\r but can also be decoded into a string such as 33-4-6, which makes it\r easier to understand the grid hierarchy.  In this case it means the\r grid cell is at the 3rd level of the hierarchy.  Its grandparent cell\r was 33 at the 1st level, its parent was cell 4 (at level 2) within\r cell 33, and the cell itself is cell 6 (at level 3) within cell 4\r within cell 33.  If you specify {id}, the ID is printed directly as an\r integer.  The ID in string format can be accessed by the \"dump\r grid\"_dump.html command and its {idstr} argument.\r \r {Proc} is the ID of the processor which currently owns the grid cell.\r \r The {xlo}, {ylo}, {zlo} attributes are the coordinates of the\r lower-left corner of the grid cell in the appropriate distance\r \"units\"_units.html.  The {xhi}, {yhi}, {zhi} are the coordinates of\r the upper-right corner of the grid cell.  The {xc}, {yc}, {zc}\r attributes are the coordinates of the center point of the grid cell.\r The {zlo}, {zhi}, {zc} attributes cannot be used for a 2d simulation.\r \r The {vol} attribute is the flow volume of the grid cell (or area in\r 2d).  Flow volume is the portion of the grid cell that is accessible\r to particles, i.e. outside any closed surface that may intersect the\r cell.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid vector or per-grid array depending\r on the number of input values.  If a single input is specified, a\r per-grid vector is produced.  If two or more inputs are specified, a\r per-grid array is produced where the number of columns = the number of\r inputs.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r The {id} and {xlo,ylo,zlo} and {xhi,yhi,zhi} values for a split cell\r and its sub cells are all the same.  The {vol} of a cut cell is the\r portion of the cell in the flow.  The {vol} of a split cell is the\r same as if it were unsplit.  The {vol} of each sub cell within a split\r cell is its portion of the flow volume.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The vector or array can be accessed by any command that uses per-atom\r values from a compute as input.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r The vector or array values will be in whatever \"units\"_units.html the\r corresponding attribute is in, e.g. distance units for xlo or xc.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line\r \r [Restrictions:}\r \r For 2d simulations, none of the attributes which refer to the 3rd\r dimension may be used.",
      "docFile": "compute_property_grid.txt"
    },
    {
      "family": "compute",
      "style": "property/surf",
      "description": "Define a computation that simply stores surface element attributes for\r each explicit surface element in a surface group.  This is useful for\r values which can be used by other \"output\r commands\"_Section_howto.html#howto_4 that take computes as inputs.\r See for example, the \"compute reduce\"_compute_reduce.html, \"fix\r ave/surf\"_fix_ave_surf.html, \"dump surf\"_dump.html, and \"surf-style\r variable\"_variable.html commands.\r \r Only surface elements in the surface group specified by {group-ID} are\r included in the calculation.  See the \"group surf\"_group.html command\r for info on how surface elements can be assigned to surface groups.\r \r This command can only be used for simulations with explicit surface\r elements.  Explicit surface elements are triangles for 3d simulations\r and line segments for 2d simulations.  Unlike implicit surface\r elements, each explicit triangle or line segment may span multiple\r grid cells.  See \"Section 4.9\"_Section_howto.html#howto_9 of the\r manual for details.\r \r :line\r \r {Id} is the surface element ID, as defined in the surface data file\r read by the \"read_surf\"_read_surf.html comand.\r \r The {v1x}, {v1y}, {v1z} attributes are the coordinates of the first\r end point of a line segment (2d) or first corner point of a triangle\r (3d).  Likewise, the {v2x}, {v2y}, {v2z} attributes are the\r coordinates of the second end point of a line segment (2d) or second\r corner point of a triangle (3d).  The {v3x}, {v3y}, {v23z} attributes\r are the coordinates of the third corner point of a triangle (3d).\r \r The {xc}, {yc}, {zc} attributes are the coordinates of the center\r point of a line segment or tringle.\r \r The {area} attribute is the length of a line segment (distance units\r in 2d), or area of a triangle (area units in 3d).\r \r The {normx}, {normy}, {normz} attributes are components of a unit\r normal perpendicular to the line segment or face of the trangle. It\r points into the flow volume of the simulation.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-surf vector or per-surf array depending\r on the number of input values.  If a single input is specified, a\r per-surf vector is produced.  If two or more inputs are specified, a\r per-surf array is produced where the number of columns = the number of\r inputs.\r \r This compute performs calculations for each explicit surface element\r in the simulation.\r \r Surface elements not in the specified {group-ID} will output zeroes\r for all their values.\r \r The vector or array can be accessed by any command that uses per-surf\r values from a compute as input.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r The vector or array values will be in whatever \"units\"_units.html the\r corresponding attribute is in, e.g. distance units for {v1x} or {xc},\r length units for {area} in 2d, area units for {area} in 3d.\r \r :line",
      "docFile": "compute_property_surf.txt"
    },
    {
      "family": "compute",
      "style": "react/boundary",
      "description": "Define a computation that tallies counts of reactions for each\r boundary (i.e. face) of the simulation box, based on the particles\r that collide with the boundary.  Only faces assigned to the surface\r reaction model specified by {reaction-ID} are included in the\r tallying.\r \r Note that when a particle collides with a face, it can bounce off\r (possibly as a different species), be captured by the surface\r (vanish), or a 2nd particle can also be emitted.\r \r The doc page for the \"surf_react\"_surf_react.html command explains the\r different reactions that can occur for each specified style.\r \r If no values are specified each reaction specified by the\r \"surf_react\"_surf_react.html style is tallied individually for each\r boundary.\r \r If M values are specified, then M tallies are made for each face, one\r per value.  If the value starts with \"r:\" then any reaction which\r occurs with one (or more) of the listed species as a reactant is\r counted as part of that tally.  If the value starts with \"p:\" then any\r reaction which occurs with one (or more) of the listed species as a\r product is counted as part of that tally.  Note that these rules mean\r that a single reaction may be tallied multiple times depending on\r which values it matches.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"stats_style\"_stats_style.html command.  The values over many sampling\r timesteps can be averaged by the \"fix ave/time\"_fix_ave_time.html\r command.\r \r :line\r \r [Output info:]\r \r This compute calculates a global array, with the number of columns\r either equal to the number of reactions defined by the\r \"surf_react\"_surf_react style (if no values are specified) or equal to\r M = the # of values specified.  The number of rows is 4 for a 2d\r simulation for the 4 faces (xlo, xhi, ylo, yhi), and it is 6 for a 3d\r simulation (xlo, xhi, ylo, yhi, zlo, zhi).\r \r The array can be accessed by any command that uses global array values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The array values are counts of the number of reactions that occurred\r on each face.\r \r :line",
      "docFile": "compute_react_boundary.txt"
    },
    {
      "family": "compute",
      "style": "react/surf",
      "description": "Define a computation that tallies counts of reactions for each\r explicit surface element in a surface element group, based on the\r particles that collide with that element.  Only surface elements in\r the surface group specified by {group-ID} are included in the\r tallying.  See the \"group surf\"_group.html command for info on how\r surface elements can be assigned to surface groups.  Likewise only\r surface elements assigned to the surface reaction model specified by\r {reaction-ID} are included in the tallying.\r \r Explicit surface elements are triangles for 3d simulations and line\r segments for 2d simulations.  Unlike implicit surface elements, each\r explicit triangle or line segment may span multiple grid cells.  See\r the \"read_surf\"_read_surf.html command for details.\r \r This command can only be used for simulations with explicit surface\r elements.  See the similar \"compute\r react/isurf/grid\"_compute_react_isurf_grid.html command for use with\r simulations with implicit surface elements.\r \r Note that when a particle collides with a surface element, it can\r bounce off (possibly as a different species), be captured by the\r surface (vanish), or a 2nd particle can also be emitted.\r \r The doc page for the \"surf_react\"_surf_react.html command explains the\r different reactions that can occur for each specified style.\r \r If no values are specified each reaction specified by the\r \"surf_react\"_surf_react.html style is tallied individually for each\r surface element.\r \r If M values are specified, then M tallies are made for each surface\r element, one per value.  If the value starts with \"r:\" then any\r reaction which occurs with one (or more) of the listed species as a\r reactant is counted as part of that tally.  If the value starts with\r \"p:\" then any reaction which occurs with one (or more) of the listed\r species as a product is counted as part of that tally.  Note that\r these rules mean that a single reaction may be tallied multiple times\r depending on which values it matches.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump surf\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/surf\"_fix_ave_surf.html command.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-surf array, with the number of columns\r either equal to the number of reactions defined by the\r \"surf_react\"_surf_react style (if no values are specified) or equal to\r M = the # of values specified.\r \r Surface elements not in the specified {group-ID} or not assigned to\r the specified {reaction-ID} will output zeroes for all their values.\r \r The array can be accessed by any command that uses per-surf values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-surf array values are counts of the number of reactions that\r occurred.\r \r :line",
      "docFile": "compute_react_surf.txt"
    },
    {
      "family": "compute",
      "style": "react/isurf/grid",
      "description": "Define a computation that tallies counts of reactions for each grid\r cell containing implicit surface elements, based on the particles that\r collide with those elements.  Only grid cells in the grid group\r specified by {group-ID} are included in the tallying.  See the \"group\r grid\"_group.html command for info on how grid cells can be assigned to\r grid groups.  Likewise only grid cells with surface elements assigned\r to the surface reaction model specified by {reaction-ID} are included\r in the tallying.  This assignment is done via the\r \"surf_modify\"_surf_modify.html command.\r \r Implicit surface elements are triangles for 3d simulations and line\r segments for 2d simulations.  Unlike explicit surface elements, each\r triangle or line segment is wholly contained within a single grid\r cell.  See the \"read_isurf\"_read_isurf.html command for details.\r \r This command can only be used for simulations with implicit surface\r elements.  See the similar \"compute\r react/surf\"_compute_react_surf.html command for use with simulations\r with explicit surface elements.\r \r Note that when a particle collides with a surface element, it can\r bounce off (possibly as a different species), be captured by the\r surface (vanish), or a 2nd particle can also be emitted.\r \r The doc page for the \"surf_react\"_surf_react.html command explains the\r different reactions that can occur for each specified style.\r \r If no values are specified each reaction specified by the\r \"surf_react\"_surf_react.html style is tallied individually for each\r grid cell.\r \r If M values are specified, then M tallies are made for each grid cell,\r one per value.  If the value starts with \"r:\" then any reaction which\r occurs with one (or more) of the listed species as a reactant is\r counted as part of that tally.  If the value starts with \"p:\" then any\r reaction which occurs with one (or more) of the listed species as a\r product is counted as part of that tally.  Note that these rules mean\r that a single reaction may be tallied multiple times depending on\r which values it matches.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  \r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r either equal to the number of reactions defined by the\r \"surf_react\"_surf_react style (if no values are specified) or equal to\r M = the # of values specified.\r \r Grid cells not in the specified {group-ID} or whose implicit surfaces\r are not assigned to the specified {reaction-ID} will output zeroes for\r all their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values are counts of the number of reactions that\r occurred on surface elements in that grid cell.\r \r :line",
      "docFile": "compute_react_isurf_grid.txt"
    },
    {
      "family": "compute",
      "style": "reduce",
      "description": "Define a calculation that \"reduces\" one or more vector inputs into\r scalar values, one per listed input.  The inputs can be per-particle\r or per-grid or per-surf quantities; they cannot be global quantities.\r Particle attributes are per-particle quantities.\r \"Computes\"_compute.html and \"fixes\"_fix.html may generate any of the\r three kinds of quantities.  \"Particle-style, grid-style, and\r surf-style variables\"_variable.html generate per-particle, per-grid,\r or per-surf quantities respectively.  Custom attributes can be\r per-particle, per-grid, or per-surf quantities.  See the\r \"variable\"_variable command and its special functions which can\r perform the same operations as the compute reduce command on global\r vectors.\r \r IMPORTANT NOTE: All inputs to a compute reduce command must be the\r same type: per-particle, per-grid, or per-surf.  You can use the\r command multiple times if you need to reduce values of different\r types.\r \r The reduction operation is specified by the {mode} setting.  The {sum}\r option adds the values in the vector into a global total.  The {min}\r or {max} operations find the minimum or maximum value across all\r vector values.  The {ave} operation adds the vector values into a\r global total, then divides by the number of values in the vector.  The\r {sumsq} operation sums the square of the values in the vector into a\r global total.  The {avesq} oepration does the same as {sumsq}, then\r divdes the sum of squares by the number of values.  These two\r operations can be useful for calculating the variance of some\r quantity, e.g. variance = sumsq - ave^2.\r \r The {sum-area} or {ave-area} options can only be used for per-surf\r inputs.  Both options multiply each per-surf value by the area of the\r surface element (triangle in 3d, line segment in 2d) and sum the\r resulting values over all surface elements.  That is the output for\r the {sum-area} option.  For the {ave-area} option the summed value is\r divided by the summed area of all elements.  Note that both of these\r options are designed to work with flux values (e.g. mass per area per\r time) produced by the \"compute surf\"_compute_surf.html command with\r its default {norm} = yes option.\r \r :line\r \r Each listed input vector is operated on independently.\r \r Each listed input vector can be a particle attribute or can be the\r result of a \"compute\"_compute.html or \"fix\"_fix.html or the evaluation\r of a \"variable\"_variable.html.  Or it can be a custom attribute of a\r particle, grid cell, or surface element.\r \r Note that for values from a compute or fix or custom attribute, the\r bracketed index I can be specified using a wildcard asterisk with the\r index to effectively specify multiple values.  This takes the form \"*\"\r or \"*n\" or \"n*\" or \"m*n\".  If N = the size of the vector (for {mode} =\r scalar) or the number of columns in the array (for {mode} = vector),\r then an asterisk with no numeric values means all indices from 1 to N.\r A leading asterisk means all indices from 1 to n (inclusive).  A\r trailing asterisk means all indices from n to N (inclusive).  A middle\r asterisk means all indices from m to n (inclusive).\r \r Using a wildcard is the same as if the individual columns of the array\r had been listed one by one.  E.g. these 2 compute reduce commands are\r equivalent, since the \"compute grid\"_compute_grid.html command creates\r a per-grid array with 3 columns:\r \r compute myGrid grid all all u v w\r compute 2 all reduce min c_myGrid\\[*\\]\r compute 2 all reduce min c_myGrid\\[1\\] c_myGrid\\[2\\] c_myGrid\\[3\\] :pre\r \r :line\r \r The particle attributes x,y,z,vx,vy,vz are position and velocity\r components.  The ke,erot,evib attributes are for kinetic, rotational,\r and vibrational energy of particles.\r \r If a value begins with \"c_\", a compute ID must follow which has been\r previously defined in the input script.  Computes can generate\r per-particle or per-grid quantities.  See the individual\r \"compute\"_compute.html doc page for details.  If no bracketed integer\r is appended, the vector calculated by the compute is used.  If a\r bracketed integer is appended, the Nth column of the array calculated\r by the compute is used.  Users can also write code for their own\r compute styles and \"add them to SPARTA\"_Section_modify.html.  See the\r discussion above for how N can be specified with a wildcard asterisk\r to effectively specify multiple values.\r \r If a value begins with \"f_\", a fix ID must follow which has been\r previously defined in the input script.  Fixes can generate\r per-particle or per-grid or per-surf quantities.  See the individual\r \"fix\"_fix.html doc page for details.  Note that some fixes only\r produce their values on certain timesteps, which must be compatible\r with when this compute references the values, else an error results.\r If no bracketed integer is appended, the vector calculated by the fix\r is used.  If a bracketed integer is appended, the Nth column of the\r array calculated by the fix is used.  Users can also write code for\r their own fix style and \"add them to SPARTA\"_Section_modify.html.  See\r the discussion above for how N can be specified with a wildcard\r asterisk to effectively specify multiple values.\r \r If a value begins with \"v_\", a variable name must follow which has\r been previously defined in the input script.  It must be a\r \"particle-style or grid-style or surf-style variable\"_variable.html.\r These styles define formulas which can reference stats keywords or\r invoke other computes, fixes, or variables when they are evaluated.\r Particle-style variables can also reference various per-particle\r attributes (position, velocity, etc).  So these variables are a very\r general means of creating per-particle or per-grid or per-surf\r quantities to reduce.\r \r If a value begins with \"p_\" or \"g_\" or \"s_\", then a custom\r per-particle, per-grid, or per-surf attribute with the specified name\r is used.  Particles, grid cells, and surface elements can have custom\r attributes which store either single or multiple values per particle,\r per grid cell, or per surface element.  They can be defined and\r initialized in data files, e.g. via the \"read_surf\"_read_surf.html\r command.  Or they can be defined and used by specific commands,\r e.g. \"fix ambipolar\"_fix_ambipolar.html or \"fix\r surf/temp\"_fix_surf_temp.html or \"surf_react\r adsorb\"_surf_react_adsorb.html.  The name of each attribute is set by\r the user or defined by the command.  See \"Section\r 6.17\"_Section_howto.html#howto_17 for more discussion of custom\r attributes.\r \r If no bracketed integer is appended, the custom attribute must be a\r per-particle, per-grid, or per-surf vector (single value).  If a\r bracketed integer is appended, the custom attribute must be a\r per-particle, per-grid, or per-surf arayy (multiple values) and the\r Nth column of the custom array is used.  See the discussion above for\r how N can be specified with a wildcard asterisk to effectively specify\r multiple values.\r \r :line\r \r If the {replace} keyword is used, two indices {vec1} and {vec2} are\r specified, where each index ranges from 1 to the # of input values.\r The replace keyword can only be used if the {mode} is {min} or {max}.\r It works as follows.  A min/max is computed as usual on the {vec2}\r input vector.  The index N of that value within {vec2} is also stored.\r Then, instead of performing a min/max on the {vec1} input vector, the\r stored index is used to select the Nth element of the {vec1} vector.\r \r Here is an example which prints out both the grid cell ID and number\r of particles for the grid cell with the maximum number of particles:\r \r compute 1 property/grid id\r compute\t2 grid all n\r compute\t3 reduce max c_1 c_2\\[1\\] replace 1 2\r stats_style step c_temp c_3\\[1\\] c_3\\[2\\] :pre\r \r The first two input values in the compute reduce command are vectors\r with the ID and particle count of each grid cell.  Instead of taking\r the max of the ID vector, which does not yield useful information in\r this context, the {replace} keyword will extract the ID for the grid\r cell which has the maximum number of particles.  This ID and the\r cell's particle count will be printed with the statistical output.\r \r Note that the {replace} keyword can be used multiple times with\r different pairs of indices.\r \r :line\r \r The {subset} keyword allows selection of a subset of each input\r vectors quantities to be used for the reduce operation.  This may\r affect all of the reduction operations.  E.g. the ave and avesq\r operations will become averages for only a subset of numerical values.\r \r If inputs are per-particle values, then a mixture ID should be\r specified.  Only particle species belonging to the mixture will be\r included in the calculations.  See the \"mixture\"_mixture.html command\r for how a set of species is included in a mixture.\r \r If inputs are per-grid values, then a grid group ID should be\r specified.  Only grid cells in the grid group will be included in the\r calculations.  See the \"group grid\"_group.html command for info on how\r grid cells can be assigned to grid groups.\r \r If inputs are per-surf values, then a surface group ID should be\r specified.  Only surface elements in the surface group will be\r included in the calculations.  See the \"group surf\"_group.html command\r for info on how surface elements can be assigned to surface groups.\r \r IMPORTANT NOTE: If computes or fixes are used as inputs to compute\r reduce, they may define their own subsets of particles, grid cells, or\r surface elements which contribute to their output.  Typically output\r from those computes or fixes will be zero for grid cells or surface\r elements not in the grid or surface group specified for those\r commands.  Thus you may want to use an argument for the {subset}\r keyword which is consistent with the inputs, but that is not required.\r \r :line\r \r If a single input is specified this compute produces a global scalar\r value.  If multiple inputs are specified, this compute produces a\r global vector of values, the length of which is equal to the number of\r inputs specified.\r \r :line\r \r [Output info:]\r \r This compute calculates a global scalar if a single input value is\r specified or a global vector of length N where N is the number of\r inputs, and which can be accessed by indices 1 to N.  These values can\r be used by any command that uses global scalar or vector values from a\r compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4 for an\r overview of SPARTA output options.\r \r The scalar or vector values will be in whatever \"units\"_units.html the\r quantities being reduced are in.",
      "docFile": "compute_reduce.txt"
    },
    {
      "family": "compute",
      "style": "sonine/grid",
      "description": "Define a computation that calculates the sonine moments of the\r velocity distribution of the particles in each grid cell in a grid\r cell group.  The values are tallied separately for each group of\r species in the specified mixture, as described in the Output section\r below.  See the mixture command for how a set of species can be\r partitioned into groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set of particles to compute the A,B formulas below.\r \r Note however that the center-of-mass (COM) velocity that is subtracted\r from each particle to yield a squared thermal velocity Csq for each\r particle, as described below, is the COM velocity for only the\r particles in the current timestep.  When time-averaging it is NOT the\r COM velocity for all particles across all timesteps.\r \r Note that this is a different form of averaging than taking the values\r produced by the formulas below for a single timestep, summing those\r values over the sampling timesteps, and then dividing by the number of\r sampling steps.\r \r :line\r \r Calculation of both the A and B sonine moments is done by first\r calcuating the center-of-mass (COM) velocity of particles for each\r group within a grid cell.  This is done as follows:\r \r COMx = Sum_i (mass_i Vx_i) / Sum_i (mass_i)\r COMy = Sum_i (mass_i Vy_i) / Sum_i (mass_i)\r COMz = Sum_i (mass_i Vz_i) / Sum_i (mass_i)\r Cx = Vx - COMx\r Cy = Vy - COMy\r Cz = Vz - COMz\r Csq = Cx*Cx + Cy*Cy + Cz*Cz :pre\r \r The COM velocity is (COMx,COMy,COMz).  The thermal velocity of each\r particle is (Cx,Cy,Cz), i.e. its velocity minus the COM velocity of\r particles in its group and cell.  This allows computation of Csq for\r each particle which is used in the formulas below to calculate the\r sonine moments.\r \r :line\r \r The {a} keyword calculates the average of one or more sonine A moments\r for all particles in each group:\r \r A1 = Sum_i (mass_i * Vdim * pow(Csq,1)) / Sum_i (mass_i)\r A2 = Sum_i (mass_i * Vdim * pow(Csq,2)) / Sum_i (mass_i)\r A3 = Sum_i (mass_i * Vdim * pow(Csq,3)) / Sum_i (mass_i)\r A4 = Sum_i (mass_i * Vdim * pow(Csq,4)) / Sum_i (mass_i)\r A5 = Sum_i (mass_i * Vdim * pow(Csq,5)) / Sum_i (mass_i) :pre\r \r Vdim is Vx or Vy or Vz as specified by the {dim} value.  {Csq} is the\r squared thermal velocity of the particle, as in the COM equations\r above.  The number of moments computed is specified by the {order}\r value, e.g. for order = 3, the first 3 moments are computed, which\r leads to 3 columns of output as explained below.\r \r The {b} keyword calculates the average of one or more sonine B moments\r for all particles in each group:\r \r B1 = Sum_i (mass_i * Vdim1 * Vdim2 * pow(Csq,1)) / Sum_i (mass_i)\r B2 = Sum_i (mass_i * Vdim1 * Vdim2 * pow(Csq,2)) / Sum_i (mass_i)\r B3 = Sum_i (mass_i * Vdim1 * Vdim2 * pow(Csq,3)) / Sum_i (mass_i)\r B4 = Sum_i (mass_i * Vdim1 * Vdim2 * pow(Csq,4)) / Sum_i (mass_i)\r B5 = Sum_i (mass_i * Vdim1 * Vdim2 * pow(Csq,5)) / Sum_i (mass_i) :pre\r \r Vdim is Vx or Vy or Vz as specified by the {dim} value.  {Csq} is the\r squared thermal velocity of the particle, as in the COM equations\r above.  The number of moments computed is specified by the {order}\r value, e.g. for order = 2, the first 2 moments are computed, which\r leads to 2 columns of output as explained below.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {a z 3} and {b xy 2} moments were specified as keywords, then the 1st\r thru 3rd columns would be the A1, A2, A3 moments of the first group,\r the 4th and 5th columns would be the B1 and B2 moments of the first\r group, the 6th thru 8th columns would be the A1, A2, A3 moments of the\r 2nd group, etc.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will have zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  These are\r units like velocity cubed or velocity to the 6th power.\r \r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_sonine_grid.txt"
    },
    {
      "family": "compute",
      "style": "surf",
      "description": "Define a computation that calculates one or more values for each\r explicit surface element in a surface element group, based on the\r particles that collide with that element or are emitted from it.  The\r values are summed for each group of species in the specified mixture.\r See the \"mixture\"_mixture.html command for how a set of species can be\r partitioned into groups.  Only surface elements in the surface group\r specified by {group-ID} are included in the calculations.  See the\r \"group surf\"_group.html command for info on how surface elements can\r be assigned to surface groups.\r \r This command can only be used for simulations with explicit surface\r elements.  See the similar \"compute\r isurf/grid\"_compute_isurf_grid.html command for use with simulations\r with implicit surface elements.\r \r Explicit surface elements are triangles for 3d simulations and line\r segments for 2d simulations.  Unlike implicit surface elements, each\r explicit triangle or line segment may span multiple grid cells.  See\r \"Section 4.9\"_Section_howto.html#howto_9 of the manual for details.\r \r Note that when a particle collides with a surface element, it can\r bounce off (possibly as a different species), be captured by the\r surface (vanish), or a 2nd particle can also be emitted.\r Additionally, surface elements can emit particles directly -- see the\r \"fix_emit_surf\"_fix_emit_surf.html command doc page.  The formulas\r below account for all these possible outcomes.  For example, the\r kinetic energy flux {ke} onto a suface element for a single collision\r includes a positive contribution from the incoming particle and\r negative contributions from 0, 1, or 2 outgoing particles.  The\r exception is the {n} and {nwt} values which simply tally counts of\r particles colliding with the surface element.\r \r If the explicit surface element is transparent, the particle will pass\r through the surface unaltered.  See the transparent keyword for the\r \"read_surf\"_read_surf.html command.  The count of particles going\r through the surfacce as well as their mass or energy fluxes can still\r be tallied by this compute.  See details on transparent surface\r elements below.\r \r Also note that all values for a collision are tallied based on the\r species group of the incident particle.  Quantities associated with\r outgoing particles are part of the same tally, even if they are in\r different species groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump surf\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/surf\"_fix_ave_surf.html command.  It does its averaging as if the\r particles striking the surface element at each sampling timestep were\r combined together into one large set to compute the formulas below.\r The answer is then divided by the number of sampling timesteps if it\r is not otherwise normalized by the number of particles.  Note that in\r general this is a different normalization than taking the values\r produced by the formulas below for a single timestep, summing them\r over the sampling timesteps, and then dividing by the number of\r sampling steps.  However for the current values listed below, the two\r normalization methods are the same.\r \r NOTE: If particle weighting is enabled via the \"global\r weight\"_global.html command, then all of the values below are scaled\r by the weight assigned to the grid cell in which the particle\r collision with the surface element occurs.  The only exception is the\r the {n} value, which is NOT scaled by the weight; it is a simple count\r of particle collisions with the surface element.\r \r :line\r \r The {n} value counts the number of particles in the group striking the\r surface element.\r \r The {nwt} value counts the number of particles in the group striking\r the surface element and weights the count by the weight assigned to\r the grid cell in which the particle collision with the surface element\r occurs.  The {nwt} quantity will only be different than {n} if\r particle weighting is enabled via the \"global weight\"_global.html\r command.\r \r The {nflux} and {nflux_incident} values calculate the net and incident\r number flux imparted to the surface element by particles in the group\r respectively. Incident flux sums over all the impacting particles,\r while net flux subtracts out reflected particles and includes effects\r from surface chemistry such as particle deletion. These are computed as\r \r Nflux = N / (A * dt / fnum) :pre\r \r where N is the number of all contributing particles, normalized by\r A = the area of the surface element, dt = the timestep, and fnum = the\r real/simulated particle ratio set by the \"global fnum\"_global.html\r command.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the Nflux formula.  The Nflux quantity becomes effectively\r a particle flow rate (count per time).  See discussion of the {norm}\r keyword below.\r \r The {mflux} and {mflux_incident} values calculate the net and incident\r mass flux imparted to the surface element by particles in the group\r respectively.  These are computed as\r \r Mflux = Sum_i (mass_i) / (A * dt / fnum) :pre\r \r where the sum is over all contributing particle masses, normalized by\r the area of the surface element, dt and fnum as defined before.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the Nflux formula.  Then Mflux quantity becomes\r effectively a mass flow rate (mass per time).  See discussion of the\r {norm} keyword below.\r \r The {fx}, {fy}, {fz} values calculate the xyz components of force\r exerted on the surface element by particles in the group.  These are\r computed as\r \r p_delta = mass * (V_post - V_pre)\r Fx = - Sum_i (p_delta_x) / (dt / fnum)\r Fy = - Sum_i (p_delta_y) / (dt / fnum)\r Fz = - Sum_i (p_delta_z) / (dt / fnum) :pre\r \r where p_delta is the change in momentum of a particle, whose velocity\r changes from V_pre to V_post when colliding with the surface element.\r The force exerted on the surface element is the sum over all\r contributing p_delta, normalized by dt and fnum as defined before.\r \r The {tx}, {ty}, {tz} values calculate the xyz components of torque Tq\r exerted on the entire body by particles in the group colliding with\r this surface element.  Use of these values requires the the\r center-of-mass (COM) of the body be specified so the torque can be\r calculated around the COM.  The COM can be the geometric center of a\r triangulated object, or it can reflect an uneven distribution of mass\r within the body.  The torque components are computed as\r \r p_delta = mass * (V_post - V_pre)\r force = p_delta / (dt / fnum)\r R = Xcollide - Xcom\r Tq = R x force :pre\r \r where p_delta is the change in momentum of a particle, whose velocity\r changes from V_pre to V_post when colliding with the surface element.\r The vector R is from the COM specified by the {com} keyword Xcollide =\r the collision point on the surface.  The force exerted on then the\r surface element is the sum over all contributing p_delta, normalized\r by dt and fnum as defined before.\r \r Note that if the surfaces defined in a simulation represent multiple\r objects each with their own COM, then you should use this command\r multiple times with different surface groups, if you want to\r calculate the torque on each object.\r \r The {press} value calculates the pressure {P} exerted on the surface\r element in the normal direction by particles in the group, such that\r outward pressure is positive.  This is computed as\r \r p_delta = mass * (V_post - V_pre)\r P = Sum_i (p_delta_i dot N) / (A * dt / fnum) :pre\r \r where p_delta, V_pre, V_post, dt, fnum are defined as before.  The\r pressure exerted on the surface element is the sum over all\r contributing p_delta dotted into the outward normal N of the surface\r element, also normalized by A = the area of the surface element.\r \r The {px}, {py}, {pz} values calculate the normal pressure Px, Py, Pz\r extered on the surface element in the direction of its normal by\r particles in the group, with respect to the x, y, z coordinate axes.\r These are computed as\r \r p_delta = mass * (V_post - V_pre)\r p_delta_n = (p_delta dot N) N\r Px = - Sum_i (p_delta_n_x) / (A * dt / fnum)\r Py = - Sum_i (p_delta_n_y) / (A * dt / fnum)\r Pz = - Sum_i (p_delta_n_z) / (A * dt / fnum) :pre\r \r where p_delta, V_pre, V_post, N, A, and dt are defined as before.\r P_delta_n is the normal component of the change in momentum vector\r p_delta of a particle.  P_delta_n_x (and y,z) are its x, y, z\r components.\r \r The {shx}, {shy}, {shz} values calculate the shear pressure Sx, Sy, Sz\r extered on the surface element in the tangential direction to its\r normal by particles in the group, with respect to the x, y, z\r coordinate axes.  These are computed as\r \r p_delta = mass * (V_post - V_pre)\r p_delta_t = p_delta - (p_delta dot N) N\r Sx = - Sum_i (p_delta_t_x) / (A * dt / fnum)\r Sy = - Sum_i (p_delta_t_y) / (A * dt / fnum)\r Sz = - Sum_i (p_delta_t_z) / (A * dt / fnum) :pre\r \r where p_delta, V_pre, V_post, N, A, and dt are defined as before.\r P_delta_t is the tangential component of the change in momentum vector\r p_delta of a particle.  P_delta_t_x (and y,z) are its x, y, z\r components.\r \r The {ke} value calculates the kinetic energy flux {Eflux} imparted to\r the surface element by particles in the group, such that energy lost\r by a particle is a positive flux.  This is computed as\r \r e_delta = 1/2 mass (V_post^2 - V_pre^2)\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the kinetic energy change in a particle, whose\r velocity changes from V_pre to V_post when colliding with the surface\r element.  The energy flux imparted to the surface element is the sum\r over all contributing e_delta, normalized by A = the area of the\r surface element and dt = the timestep and fnum = the real/simulated\r particle ratio set by the \"global fnum\"_global.html command.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the Eflux formula.  Then Eflux quantity becomes\r effectively an energy flow rate (energy per time).  See discussion of\r the {norm} keyword below.\r \r The {erot} value calculates the rotational energy flux {Eflux}\r imparted to the surface element by particles in the group, such that\r energy lost by a particle is a positive flux.  This is computed as\r \r e_delta = Erot_post - Erot_pre\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the rotational energy change in a particle, whose\r internal rotational energy changes from Erot_pre to Erot_post when\r colliding with the surface element.  The flux equation is the same as\r for the {ke} value.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the Eflux formula.  Then Eflux quantity becomes\r effectively an energy flow rate (energy per time).  See discussion of\r the {norm} keyword below.\r \r The {evib} value calculates the vibrational energy flux {Eflux}\r imparted to the surface element by particles in the group, such that\r energy lost by a particle is a positive flux.  This is computed as\r \r e_delta = Evib_post - Evib_pre\r Eflux = - Sum_i (e_delta) / (A * dt / fnum) :pre\r \r where e_delta is the vibrational energy change in a particle, whose\r internal vibrational energy changes from Evib_pre to Evib_post when\r colliding with the surface element.  The flux equation is the same as\r for the {ke} value.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the Eflux formula.  Then Eflux quantity becomes\r effectively an energy flow rate (energy per time).  See discussion of\r the {norm} keyword below.\r \r The {echem} value calculates the chemical catalytic energy flux {Eflux}\r imparted to the surface element by particles in the group, such that\r energy lost by a particles recombining is a positive flux.  This is computed as\r \r Eflux = - Sum_i (e_recomb) / (A * dt / fnum)\r \r where e_recomb is the catalytic chemical energy of a particle pair\r (positive for an exothermic recombination reaction). The flux equation\r is the same as for the {ke} value. This option applies only to the\r {prob} style of surface reations. A value of 0.0 will be returned\r for other styles of surface reactions, e.g. {global} and {adsorb}.\r \r The {etot} value calculates the total energy flux imparted to the\r surface element by particles in the group, such that energy lost by a\r particle is a positive flux.  This is simply the sum of kinetic,\r rotational, and vibrational energies.  Thus the total energy flux is\r the sum of what is computed by the {ke}, {erot}, and {evib} values.\r \r If the optional {norm} key is set to {flow}, then the area A is not\r included in the {etot} formula.  Then {etot} quantity becomes\r effectively an energy flow rate (energy per time).  See discussion of\r the {norm} keyword below.\r \r :line\r \r [Transparent surface elements:]\r \r This compute will tally information on particles that pass through\r transparent surface elements.  The \"Section\r 6.15\"_Section_howto.html#howto_15 doc page provides an overview of\r transparent surfaces and how to create them.\r \r The {n} and {nwt} value are calculated the same for transparent\r surfaces as for non-transparent.  I.e. they are the count and weighted\r count of particles passing through the surface.\r \r The {nflux}, {mflux}, {ke}, {erot}. {evib}, {echem}, and {etot} values are\r fluxes.  For transparent surfaces, they are calculated only for the\r incident particle as if it had struck the surface.  The outgoing\r particle is ignored.  This means the tally quantity is the flux of\r particles onto the outward face of the surface.  No tallying is done\r for particles hitting the inward face of the transparent surface.  See\r \"Section 6.15\"_Section_howto.html#howto_15 for how to do tallying in\r both directions.\r \r All the other values are calculated as described above.  This means\r they will be zero, since the incident and outgoing particle have the\r same mass and velocity.\r \r IMPORTANT NOTE:\r \r Transparent surface elements can intersect standard non-transparent\r surface elements.  For example, to model flow around a spherical\r object, the sphere would be defined by the usual non-transparent\r triangles which interact with flow particles.  A plane of transparent\r surface elements normal to the flow direction could be defined which\r cut through the sphere.  In this case some or all of the transparent\r triangles will be partially or wholly inside the sphere.  SPARTA does\r not attempt to calculate the portion of a tranparent triangle (or line\r segment in 2d) which is inside the flow volume.  The \"area\" specified\r in all the formulas above will be the area of the entire transparent\r triangle (or line segment in 2d), which may or may not be what you\r want.\r \r See the optional norm keyword (below) to calculate flux values\r un-normalized by the surface element area.  Also see the \"sum-area\"\r and \"ave-area\" modes of the \"compute reduce\"_compute_reduce.html\r command for additional ways to sum or average either normalized or\r un-normalized flux values produced by this compute.\r \r :line\r \r [Optional keywords]\r \r If the {norm} keyword is used with a setting of {flow}, then the\r formulas above for all flux values will not use the surface element\r area A in the denominator.  Specifically these values are nflux,\r mflux, ke, erot, evib, etot.\r \r The formulas thus compute the aggregate mass or energy flow to the\r surface (e.g. mass per time), not the flux (e.g. mass per area per\r time).\r \r If the {norm} keyword setting is {flux} (the default), then the flux\r formulas will be calculated as shown with the area A in the\r denominator.\r \r The {com} keyword is only used if torque is being computed by any of\r the {tx}, {ty}, {tz} values.  The {Cx}, {Cy}, {Cz} settings are the\r coordinates of the center-of-mass of the body around which the torque\r will be calculated.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-surf array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {n} and {u} values were specified as keywords, then the first two\r columns would be {n} and {u} for the first group, the 3rd and 4th\r columns would be {n} and {u} for the second group, etc.\r \r Surface elements not in the specified {group-ID} will output zeroes\r for all their values.\r \r The array can be accessed by any command that uses per-surf values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-surf array values will be in the \"units\"_units.html\r appropriate to the individual values as described above. {N} is\r unitless.  {Press}, {px}, {py}, {pz}, {shx}, {shy}, {shz} are in in\r pressure units.  {Ke}, {erot}, {evib}, {echem}, and {etot} are in\r energy/area-time units for 3d simulations and energy/length-time units\r for 2d simulations.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_surf.txt"
    },
    {
      "family": "compute",
      "style": "surf/collision/tally",
      "description": "Tally various values for each collision of a particle with a surface\r element during the current timestep.  Only surface collisions which do\r not result in chemical reactions are tallied by this command.  See the\r related \"compute surf/reaction/tally\"_compute_reaction_tally.html\r command to tally collisions which induce reactions.  It provides\r different tally value options than this compute.\r \r Only surface elements in the surface group specified by {group-ID} and\r particles with species in the mixture specified by {mix-ID} are\r included.  See the \"group grid\"_group.html command for info on how\r surface elements can be assigned to surface groups.\r \r This compute can be used for simulations with either explicit or\r implicit surface elements.  See \"Section 6.13\"_Section_howto_13 for\r more discussion of explicit and implicit surfaces.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump tally\"_dump.html command.  Currently this is the only option for\r accessing the tallied values.\r \r :line\r \r The {id/surf} value is the ID of the surface element.  For explicit\r surfaces, surface element IDs are unique.  For implicit surfaces, the\r surface ID of all the surface elements within a grid cell are the grid\r cell ID.  See \"Section 6.13\"_Section_howto_13 for more discussion of\r explicit and implicit surfaces.\r \r The {id} value is the ID of the particle.  Note that particle IDs are\r generated randomly.  Thus multiple particles in the system can\r potentially have the same ID.  See \"Section 6.17\"_Section_howto_17 for\r more details on particle IDs.\r \r The {type} value is an integer index for the particle species.  It is\r a value from 1 to Nspecies. The value corresponds to the order in\r which species were defined via the \"species\"_species.html command.\r See \"Section 6.17\"_Section_howto_17 for more details on particle\r types.\r \r The {time} value is the point in time within the timestep when the\r collision occured.  Thus if the timestep size is DT, as set by the\r \"timestep\"_timestep.html command, the {time} values will all be within\r the range of 0.0 to DT, for all collisions tallied by this compute on\r each timestep it is invoked.\r \r The {xc}, {yc}, and {zc} are the coordinates of the collision point\r between the particle and the surface element.\r \r The {vx/pre}, {vy/pre}, {vz/pre} values are the velocity components of\r the incident particle before it collides with the surface element.\r \r The {vx/post}, {vy/post}, {vz/post} values are the velocity components\r of the particle after it collides with the surface element.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-tally array, with the number of columns\r equal to the number of values.\r \r The array can be accessed by any command that uses per-tally values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-tally array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  {Time} is in\r time units; see above for details.  {Xc}, {yc}, and {zc} are in\r distance units.  {Vx/pre}, {vy/pre}, {vz/pre} are in velocity units, as\r are {vx/post}, {vy/post}, {vz/post}.\r \r :line",
      "docFile": "compute_surf_collision_tally.txt"
    },
    {
      "family": "compute",
      "style": "surf/reaction/tally",
      "description": "Tally various values for each reaction of a particle with a surface\r element during the current timestep.  Only surface collisions which\r result in a chemical reaction are tallied by this command, as enabled\r by the \"surf_react\"_surf_react.html command.  See the related \"compute\r surf/collision/tally\"_compute_surf_collision_tally.html command to\r tally collisions which do not induce reactions.  It provides different\r tally value options than this compute.\r \r Only surface elements in the surface group specified by {group-ID} and\r particles with species in the mixture specified by {mix-ID} are\r included.  See the \"group grid\"_group.html command for info on how\r surface elements can be assigned to surface groups.\r \r This compute can be used for simulations with either explicit or\r implicit surface elements.  See \"Section 6.13\"_Section_howto_13 for\r more discussion of explicit and implicit surfaces.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump tally\"_dump.html command.  Currently this is the only option for\r accessing the tallied values.\r \r Note that if a gas/surface reaction takes place, then a collision of a\r single particle with a surface can result in 0, 1, or 2 particles\r being produced.  The species of the product particles may be different\r than the species of the reactant.  All of these attributes can be\r tallied using the values explained next.\r \r :line\r \r The {reaction} value is the index of which reaction occurred.  It is a\r number from 1 to N, where N is the number of reactions defined by the\r \"surf_react\"_surf_react.html command.\r \r The {id/surf} value is the ID of the surface element.  For explicit\r surfaces, surface element IDs are unique.  For implicit surfaces, the\r surface ID of all the surface elements within a grid cell are the grid\r cell ID.  See \"Section 6.13\"_Section_howto_13 for more discussion of\r explicit and implicit surfaces.\r \r The {id/pre} value is the ID of the incident particle.  Note that\r particle IDs are generated randomly.  Thus multiple particles in the\r system can potentially have the same ID.  See \"Section\r 6.17\"_Section_howto_17 for more details on particle IDs.\r \r The {id1/post} and {id2/post} values are the post-collision IDs of the\r 0, 1, or 2 resulting particles.  If the incident particle vanished in\r the reaction, then {id1/post} will be 0.  Otherwise it will be the\r same as {id/pre}.  If {id2/post} is 0, no 2nd particle was created by\r the reaction, otherwise it is the random ID assigned to the newly\r created particle.\r \r The {type/pre} value is the integer index for the incident particle\r species.  It is a value from 1 to Nspecies. The value corresponds to\r the order in which species were defined via the \"species\"_species.html\r command.  See \"Section 6.17\"_Section_howto_17 for more details on\r particle types.\r \r The {type1/post} and {type2/post} values are the post-collision\r integer indices for the particle species of the 0, 1, or 2 resulting\r particles.  If the incident particle vanished in the reaction, then\r {type1/post} will be 0.  Otherwise {type1/post} is the new species\r index of the incident particle.  If {type2/post} is 0, no 2nd particle\r was created by the reaction, otherwise it is the species index of the\r newly created particle.\r \r The {time} value is the point in time within the timestep when the\r collision occured.  Thus if the timestep size is DT, as set by the\r \"timestep\"_timestep.html command, the {time} values will all be within\r the range of 0.0 to DT, for all collisions tallied by this compute on\r each timestep it is invoked.\r \r The {xc}, {yc}, and {zc} are the coordinates of the collision point\r between the particle and the surface element.\r \r The {vx/pre}, {vy/pre}, {vz/pre} values are the velocity components of\r the incident particle before it collides with the surface element.\r \r The {vx1/post}, {vy1/post}, {vz1/post} values are the velocity\r components of the incident particle after it collides with the surface\r element and has reacted.  These values will be zero if the paticle\r vanished in the reaction.\r \r If a 2nd particle was created by the reaction, the {vx2/post},\r {vy2/post}, {vz2/post} values are its velocity components.  If no 2nd\r particle was created, these values will be zero.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-tally array, with the number of columns\r equal to the number of values.\r \r The array can be accessed by any command that uses per-tally values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-tally array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  {Time} is in\r time units; see above for details.  {Xc}, {yc}, and {zc} are in\r distance units.  {Vx/pre}, {vy/pre}, {vz/pre} are in velocity units,\r as are {vx1/post}, {vy1/post}, {vz1/post} and {vx2/post}, {vy2/post},\r {vz2/post}.\r \r :line",
      "docFile": "compute_surf_reaction_tally.txt"
    },
    {
      "family": "compute",
      "style": "temp",
      "description": "Define a computation that calculates the temperature of all particles.\r \r The temperature is calculated by the formula KE = dim/2 N kB T, where\r KE = total kinetic energy of the particles (sum of 1/2 m v^2), dim =\r dimensionality of the simulation, N = number of particles, kB =\r Boltzmann constant, and T = temperature.\r \r Note that this definition of temperature does not subtract out a net\r streaming velocity for particles, so it is not a thermal temperature\r when the particles have a non-zero streaming velocity.  See the\r \"compute thermal/grid\"_compute_thermal_grid.html command for\r calculation of thermal temperatures on a per grid cell basis.\r \r :line\r \r [Output info:]\r \r This compute calculates a global scalar (the temperature).  This value\r can be used by any command that uses global scalar values from a\r compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4 for an\r overview of SPARTA output options.\r \r The scalar value will be in temperature \"units\"_units.html.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_temp.txt"
    },
    {
      "family": "compute",
      "style": "thermal/grid",
      "description": "Define a computation that calculates one or more values for each grid\r cell in a grid cell group, which are based on the thermal temperature\r of the particles in each grid cell.  The values are tallied separately\r for each group of species in the specified mixture, as described in\r the Output section below.  See the mixture command for how a set of\r species can be partitioned into groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculation.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The values listed above rely on first computing a thermal temperature\r which subtracts the center-of-mass (COM) velocity for all particles in\r the group and grid cell from each particle to yield a thermal\r velocity.  This thermal velocity is used to compute the temperature,\r as described below.  This is in contrast to some of the values tallied\r by the \"compute grid temp\"_compute_grid.html command which simply uses\r the full velocity of each particle to compute a temperature.  For\r non-streaming simulations, the two results should be similar, but for\r streaming flows, they will be different.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set of particles to compute the formulas below.  \r \r Note that this is a different form of averaging than taking the values\r produced by the formulas below for a single timestep, summing those\r values over the sampling timesteps, and then dividing by the number of\r sampling steps.\r \r Also note that the center-of-mass (COM) velocity that is subtracted\r from each particle to yield a squared thermal velocity Csq for each\r particle, as described below, is also computed over one large set of\r particles (across all timesteps).  This is in contrast to using a COM\r velocity computed only for particles in the current timestep, which is\r what the \"compute sonine/grid\"_compute_sonine_grid.html command does.\r \r :line\r \r Calculation of the thermal temperature is done by first calcuating the\r center-of-mass (COM) velocity of particles for each group within a\r grid cell.  This is done as follows:\r \r COMx = Sum_i (mass_i Vx_i) / Sum_i (mass_i)\r COMy = Sum_i (mass_i Vy_i) / Sum_i (mass_i)\r COMz = Sum_i (mass_i Vz_i) / Sum_i (mass_i)\r Cx = Vx - COMx\r Cy = Vy - COMy\r Cz = Vz - COMz\r Csq = Cx*Cx + Cy*Cy + Cz*Cz :pre\r \r The COM velocity is (COMx,COMy,COMz).  The thermal velocity of each\r particle is (Cx,Cy,Cz), i.e. its velocity minus the COM velocity of\r particles in its group and cell.  This allows computation of Csq for\r each particle which is used to calculate the total kinetic energy due\r to particles in the group as follows:\r \r thermal_KE = Sum_i (1/2 mass_i Csq_i) :pre\r \r The {temp} value computes the thermal temperature T, due to particles\r in each group:\r \r T = thermal_KE / (3/2 N kB) :pre\r \r The {press} value uses the thermal_KE to compute a pressure P for the\r grid cell due to particles in the group:\r \r P = 2/3 fnum/volume * thermal_KE :pre\r \r Note that if multiple groups are defined in the mixture, one group's\r value is effectively a partial pressure due to particles in the group.\r When accumulated over multiple sampling steps, this value is\r normalized by the number of sampling steps.  Note that if particle\r weighting is enabled via the \"global weight\"_global.html command, then\r the volume used in the formula is divided by the weight assigned to\r the grid cell.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array, with the number of columns\r equal to the number of values times the number of groups.  The\r ordering of columns is first by values, then by groups.  I.e. if the\r {temp} and {press} values were specified as keywords, then the first\r two columns would be {temp} and {press} for the first group, the 3rd\r and 4th columns would be {temp} and {press} for the second group, etc.\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 6.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 6.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in the \"units\"_units.html\r appropriate to the individual values as described above.  {Temp} is in\r temperature units.  {Press} is in prsesure units.\r \r :line\r \r Styles with a {kk} suffix are functionally the same as the\r corresponding style without the suffix.  They have been optimized to\r run faster, depending on your available hardware, as discussed in the\r \"Accelerating SPARTA\"_Section_accelerate.html section of the manual.\r The accelerated styles take the same arguments and should produce the\r same results, except for different random number, round-off and\r precision issues.\r \r These accelerated styles are part of the KOKKOS package. They are only\r enabled if SPARTA was built with that package.  See the \"Making\r SPARTA\"_Section_start.html#start_3 section for more info.\r \r You can specify the accelerated styles explicitly in your input script\r by including their suffix, or you can use the \"-suffix command-line\r switch\"_Section_start.html#start_7 when you invoke SPARTA, or you can\r use the \"suffix\"_suffix.html command in your input script.\r \r See the \"Accelerating SPARTA\"_Section_accelerate.html section of the\r manual for more instructions on how to use the accelerated styles\r effectively.\r \r :line",
      "docFile": "compute_thermal_grid.txt"
    },
    {
      "family": "compute",
      "style": "tvib/grid",
      "description": "Define a computation that calculates the vibrational temperature for\r each grid cell in a grid cell group, based on the particles in the\r cell.  How the vibrational temperature is computed is explained below.\r The temperature is calculated separately for each group of species in\r the specified mixture, as described in the Output section below.  See\r the mixture command for how a set of species can be partitioned into\r groups.\r \r Only grid cells in the grid group specified by {group-ID} are included\r in the calculations.  See the \"group grid\"_group.html command for info\r on how grid cells can be assigned to grid groups.\r \r The results of this compute can be used by different commands in\r different ways.  The values for a single timestep can be output by the\r \"dump grid\"_dump.html command.\r \r The values over many sampling timesteps can be averaged by the \"fix\r ave/grid\"_fix_ave_grid.html command.  It does its averaging as if the\r particles in the cell at each sampling timestep were combined together\r into one large set to compute the formulas below.  Note that this is a\r different normalization than taking the values produced by the\r formulas below for a single timestep, summing them over the sampling\r timesteps, and then dividing by the number of sampling steps.\r \r If the {mode} keyword is specified, then temperatures for each\r vibrational mode of each polyatomic species are calculated and output\r as explained below.  To use this option, the \"collide_modify vibrate\r discrete\"_collide_modify.html option must be set, and the \"fix\r vibmode\" command must be used to store info about individual\r vibrational modes with each particle.\r \r :line\r \r The vibrational temperature in a grid cell for a group of particles\r comprised of different species and (optionally) different vibrational\r modes is defined as a weighted average as follows:\r \r T_group = (T1*N1 + T2*N2 + ...) / (N1 + N2 + ...) :pre\r \r What is summed over in the numerator and denominator depends on\r several settings.\r \r If the \"collide_modify vibrate\"_collide_modify.html setting is {no},\r then no vibrational energy is assigned to particles.  All the\r output temperatures will be 0.0.\r \r If the \"collide_modify vibrate\"_collide_modify.html setting is\r {smooth}, then the sums in the numerator and denominator are over the\r different species in the group.  T1, T2, ... are the vibrational\r temperatures of each species.  N1, N2, ... are the counts of particles\r of each species.\r \r The vibrational temperature Tsp for particles of a single species is\r defined as follows:\r \r Ibar = Sum_i (e_vib_i) / (N kB Theta)\r Tsp = Theta / ln(1 + 1/Ibar)) :pre\r \r where e_vib is the continuous (smooth) vibrational energy of a single\r particle I, N is the total # of particles of that species, and {kB} is\r the Boltzmann factor.  Theta is the characteristic vibrational\r temperature for the species, as defined in the file read by the\r \"species\"_species.html command.\r \r If the \"collide_modify vibrate\"_collide_modify.html setting is\r {discrete}, but no species has a vibrational DOF setting that implies\r multiple vibrational modes (vibdof = 4,6,8), then the calulation of\r vibrational temeperatures is the same as for \"collide_modify vibrate\r smooth\"_collide_modify.html.  See the \"species\"_species.html command\r and its description of the per-species \"vibdof\" setting in the species\r file.\r \r If the \"collide_modify vibrate\"_collide_modify.html setting is\r {discrete}, and one or more species have vibrational DOF settings that\r imply multiple vibrational modes (vibdof = 4,6,8), as defined by the\r \"species\"_species.html command, then the sums in the numerator and\r denominator are over the different species in the group and the modes\r for each species.  For example if species CO2 has vibdof=6, then it\r has 3 modes.  Three terms in the numerator and demoninator are\r included when CO2 is a species in the group.\r \r The vibrational temperature Tsp_m for particles of a single species\r and single mode M is defined as follows:\r \r Ibar_m = Sum_i (level_im) / (N)\r Tsp_m = Theta_m / ln(1 + 1/Ibar_m)) :pre\r \r where level_im is the integer level for mode M of a single particle I,\r and N is the total # of particles of that species.  Theta_m is the\r characteristic vibrational temperature for the species and its mode M,\r as defined in the vibfile read by the \"species\"_species.html command.\r \r Finally, if the {mode} keyword is used, then the output of this\r compute is not Ngroup vibrational temperatures, but rather\r Ngroup*Nmode vibrational temperatures, where Nmode is the maximum # of\r vibrational modes associated with any species in the system (not just\r in the mixture).  Thus the sums in the numerator and denominator are\r over the different species in the group but for only a single modes of\r each of those species.  If the species does not define that mode, then\r its contribution is zero.  For example if species CO2 has vibdof=6,\r then it has 3 modes.  For the group it is in, it will contribute to 3\r output temperature values, one for mode 1, another for mode 2, another\r for mode 3.\r \r The vibrational temperature Tsp_m for particles of a single species\r and single mode M is calculated the same as explained above.\r \r :line\r \r [Output info:]\r \r This compute calculates a per-grid array.  If the {mode} keyword is\r not specified, the number of columns is equal to the number of groups\r in the specified mixture.  If is is specified, the number of columns\r is equal to the number of groups in the specified mixture times the\r maximum number of vibrational modes defined for any species in the\r system (not just in the mixture).  The ordering of the columns is as\r follows: T11, T12, T13, T21, T22, T23, T31, ... TN1, TN2, TN3.  Where\r the first index is the group from 1 to N, and the second index is the\r vibrational mode (1 to 3 in this example).\r \r This compute performs calculations for all flavors of child grid cells\r in the simulation, which includes unsplit, cut, split, and sub cells.\r See \"Section 4.8\"_Section_howto.html#howto_8 of the manual gives\r details of how SPARTA defines child, unsplit, split, and sub cells.\r Note that cells inside closed surfaces contain no particles.  These\r could be unsplit or cut cells (if they have zero flow volume).  Both\r of these kinds of cells will compute a zero result for all their\r values.  Likewise, split cells store no particles and will produce a\r zero result.  This is because their sub-cells actually contain the\r particles that are geometrically inside the split cell.\r \r Grid cells not in the specified {group-ID} will output zeroes for all\r their values.\r \r The array can be accessed by any command that uses per-grid values\r from a compute as input.  See \"Section 4.4\"_Section_howto.html#howto_4\r for an overview of SPARTA output options.\r \r The per-grid array values will be in temperature \"units\"_units.html.",
      "docFile": "compute_tvib_grid.txt"
    }
  ],
  "collide": [
    {
      "family": "collide",
      "style": "vss",
      "docFile": ""
    }
  ],
  "surf_collide": [
    {
      "family": "surf_collide",
      "style": "adiabatic",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "cll",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "diffuse",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "impulsive",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "piston",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "specular",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "td",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "transparent",
      "docFile": ""
    },
    {
      "family": "surf_collide",
      "style": "vanish",
      "docFile": ""
    }
  ],
  "surf_react": [
    {
      "family": "surf_react",
      "style": "adsorb",
      "description": "Define a model for surface chemistry reactions to perform when\r particles collide with surface elements or the global boundaries\r (faces) of the simulation box, which enables the particles to adsorb\r to the surface.  This model also has an option to encode chemical\r reactions that can occur on the surface itself.\r \r One or more models can be defined and assigned to different surfaces\r or simulation box boundaries via the \"surf_modify\"_surf_modify.html or\r \"bound_modify\"_bound_modify.html commands.  See \"Section\r 6.9\"_Section_howto.html#howto_9 for more details of how SPARTA defines\r surfaces as collections of geometric elements, triangles in 3d and\r line segments in 2d.  Also see the \"react\"_react.html command for\r specification of a gas-phase chemistry reaction model.\r \r The per-surface element species state computed and updated by this\r command can be output via the \"dump surf\"_dump.html command, using the\r {s_name} syntax to output any of the 5 custom surface state variables\r created by this surface reaction model.  They are as follows:\r \r nstick_species = per-surf array with per-species counts\r nstick_total = per-surf vector with total count of all species\r area = per-surf area vector\r weight = per-surf weight vector\r tau = per-surf time-counter vector (see below) :ul\r \r See the examples/surf_react_adsorb dir for scripts that use this\r surface reaction model.\r \r The ID for a surface reaction model is used to identify it in other\r commands.  Each surface reaction model ID must be unique.  The ID can\r only contain alphanumeric characters and underscores.\r \r In this {adsorb} style, gas particles can adsorb on the surface.\r Adsorbed particles can then undergo reactions with other adsorbed\r particles as well as with new gas-phase particles that strike the\r surface.  Each surface element stores its \"state\" for the counts of\r different particle species currently adsorbed on the element, which\r alters the probablity for future reactions to take place.\r \r When a particle adsorbs on the surface, a gas particle is deleted and\r the number of adsorbed particles of that particular species is\r incremented.  When a particle desorbs from the surface, a new gas\r particle is created and the surface count is decremented.  The exact\r location of the adsorbed particles is not stored; they are assumed to\r uniformly distributed across the surface element.\r \r One of three options is specified by the {model} keyword: {gs} or {ps}\r or {gs/ps}.  This specifies one of two types of reactions to perform,\r or both in the {gs/ps} case.\r \r Gas-surface (GS) reactions involve both gas-phase and adsorbed/bulk\r species as reactants.  Hence GS reactions occur when gas particles\r collide with the surface.  Pure-surface (PS) reactions involve only\r adsorbed/bulk reactants and do not include any gas-phase atoms or\r molecules.  They are performed once every {Nsync} timesteps based on\r the current surface coverage of the various adsorbed species.\r \r For GS reactions, when a particle collides with a surface element or\r boundary, the list of all reactions possible with that species as the\r reactant is looped over.  A probability for each reaction is\r calculated, using the formulas discussed below, and a random number is\r used to decide which reaction (if any) takes place.  A check is made\r that the sum of probabilities for all possible reactions is <= 1.0,\r which should normally be the case if reasonable reaction coefficients\r are defined.\r \r For PS reactions, a time counter algorithm is used.  Each reaction has\r a time counter {tau} that is increased by a value of {nsync*timestep}\r if all the reactants are available.  The rate for each reaction {R} is\r computed based on the specified reaction rate constants as well as the\r surface coverage of the reactants. The product of the rate {R} and\r time counter {tau} is normalized to obtain the probability for each of\r reaction; and a random number is used to decide which reaction occurs.\r The chosen reaction is perforemd and its time counter is modified as\r follows:\r \r :c,image(Eqs/time_counter.png)\r \r Next the probabilities of each reaction are recomputed and a new\r reaction is chosen based on these probabilties and performed.  This\r process is repeated till the probability of all the reactions reaches\r zero. A detailed description can be found in Swaminathan Gopalan {et\r al.} \"(SG18)\"_#SG18.\r \r :line\r \r The infile argument(s) specify one or two filenames which contain a\r list of GS or PS reactions and their associated parameters.  Two files\r must be specified for the {gs/ps} model: a GS file first and a PS file\r second.\r \r The reactions are read into SPARTA and stored as a list.  Each time a\r simulation is run the list is scanned, and only active reactions are\r flagged.  In order for a reaction to be active, all the reactants and\r all the products must be currently defined as species-IDs; and also\r all the surface reactants and products must be present within the list\r of species that are allowed to adsorb on the surface.  The list of\r surface species is provided at the end of the surf_react adsorb\r command via the adsp1, adsp2, etc arguments.\r \r Note that this means the reaction file(s) can contain more reactions\r than are used in a particular simulation.  \r \r As explained below each reaction has a specified probability between\r 0.0 and 1.0.  That probability is used to choose which reaction (if\r any) is performed.\r \r The format of either a GS or PS reaction file is as follows.  Comments\r or blank lines are allowed in the file.  Comment lines start with a\r \"#\" character.  All other entries must come in 2 or more lines with\r values separated by whitespace in the following format\r \r R1(g) + R2(s) + R3(b) --> P1(g) + P2(s) + P3(b)\r reaction-type reaction-style C1 C2 ... \r scattering-model args :pre\r \r The first line is a text-based description of a single reaction. R1,\r R2, and R3 are the reactants; while P1, P2, and P3 are the products\r listed as a \"species\"_species.html IDs.  The phase of the reactants\r and products are species in the brackets immediately following the\r species ID (no space).  It can be one of the following\r \r (g) - gas phase = gas particle striking or scattering from the surface\r (s) - surface phase = adsorbed particle present on the surface\r (b) - bulk phase = material which the surface is made of :pre\r \r For a GS reaction, R1 must be a gas-phase reactant.  I.e. the particle\r species that collides with the surface/boundary.  For a PS reaction,\r R1 must be an adsorbed or bulk-phase species.\r \r Individual reactants and products must be separated by whitespace and\r a \"+\" sign. The left-hand and right-hand sides of the equation must be\r separated by whitespace and \"-->\".  The {type} of each reaction is a\r string of characters (upper or lower case).  The different types of GS\r and PS reactions are described below.\r \r :line\r \r The allowed types for GS reactions is as follows:\r \r AA = Associative Adsorption\r DA = Dissociative Adsorption\r LH1 = Langmuir-Hinshelwood mechanism of type 1\r LH3 = Langmuir-Hinshelwood mechanism of type 3 \r CD = Condensation reaction\r ER = Eley-Rideal mechanism \r CI = Collision-induced reaction :ul\r \r An associative adsorption (AA) reaction means that R1(g) adsorbs on the\r surface to form P1(s) when it collides with the surface/boundary.\r \r A dissociative adsorption (DA) reaction means that R1(g) dissociates\r when it collides with the surface/boundary. One or more of the\r dissociated products can adsorb on the surface and the rest of them\r scatter as gas-phase products.\r \r A Langmuir-Hinshelwood reaction of type 1 (LH1) means that R1(g)\r adsorbs on the surface when it collides with the surface/boundary, and\r reacts quickly with the adsorbed species R2(s) or bulk-phase species\r R2(b) on the surface to form gas-phase product P1(g).\r \r A Langmuir-Hinshelwood reaction of type 3 (LH3) means that R1(g)\r adsorbs on the surface when it collides with the surface/boundary, and\r reacts quickly with the adsorbed species R2(s) or bulk-phase species\r R2(b) on the surface to form adsorbed product P1(s).\r \r A condensation reaction (CR) means that R1(g) coalesces with the bulk\r material with which the surface is made of, when it collides with the\r surface/boundary to form bulk-phase product P1(b).\r \r An Eley-Rideal (ER) reaction means that R1(g) reacts quickly with\r adsorbed species R2(s) or bulk species R2(b) when it collides with the\r surface/boundary to form gas-phase product P1(g).\r \r A collision-induced (CI) reaction means that R1(g) causes the\r desorption of adsorbed species R2(s) when it collides with the\r surface/boundary to form gas-phase products P1(g) (same species as\r R2). The incident gas-phase particle can either adsorb P2(s) or\r scatter from the surface P2(g) post collision.\r \r A detailed description of the various types of reactions can be found\r in Swaminathan Gopalan {et al.} \"(SG18)\"_#SG18.  Here is a table of\r examples for each type of GS reaction.\r \r :c,image(Eqs/GS_list.png)\r \r :line\r \r The allowed types for  PS reactions is as follows:\r \r DS = Desorption reaction\r LH2 = Langmuir-Hinshelwood mechanism of type 2\r LH4 = Langmuir-Hinshelwood mechanism of type 4 \r SB = Sublimation reaction :ul\r \r A desorption reaction (DR) means that R1(s) desorbs from the surface\r to form P1(g) whose final velocities are determined by the provided\r scattering model.\r \r A Langmuir-Hinshelwood reaction of type 2 (LH2) means that R1(s)\r adsorbed on the surface reacts with another adsorbed species R2(s) or\r bulk-phase species R2(b) on the surface to form gas-phase product\r P1(g).\r \r A Langmuir-Hinshelwood reaction of type 4 (LH4) means that R1(s)\r adsorbed on the surface reacts with another adsorbed species R2(s) or\r bulk-phase species R2(b) on the surface to form adsorbed product\r P1(s).\r \r A sublimation reaction (SR) means that R1(b), the bulk material\r undergoes a transformation from solid to gas, to form gas-phase\r product P1(g).\r \r A detailed description of the various types of reactions can be found\r in Swaminathan Gopalan {et al.} \"(SG18)\"_#SG18.  Here is a table of\r examples for each type of PS reaction.\r \r :c,image(Eqs/PS_list.png)\r \r :line \r \r The {style} of each reaction is a single character (upper or lower\r case) with the following meaning:\r \r S = Simple\r A = Arrhenius :ul\r \r IMPORTANT: The style of the reaction determines how many reaction\r coefficients are listed as C1, C2, etc, and how they are interpreted\r by SPARTA.\r \r For S = Simple style, there is a single coefficient:\r \r C1 = direct value for the reaction rate constant :pre\r \r For A = Arrhenius style, there are three coefficients:\r \r   A = pre-expoential factor \r   b = temperature exponent \r   Ea = activation energy for the reaction :pre\r \r The reaction rate constant is calculated in the following manner: \r \r :c,image(Eqs/arrhenius_2.png)\r \r For all the reactions types which includes adsorption - AA, DA, LH1,\r LH3, and CD; the user must specify the number of species that adsorb\r on the surface for the reaction as a argument after the reaction rate\r coefficients\r \r Additional optional keywords for GS reactions can be used to define\r the reaction rate constant.  These are {kisliuk}: proposed by Kisliuk\r \"(Kisliuk57)\"_#Kisliuk57; and {energy}: proposed by Beckerle {et al.}\r \"(Beckerle89)\"_#Beckerle89.\r \r {kisliuk} args = A_k B_k Ea_k (only for reactions which includes adsorption - AA, DA, LH1, LH3, and CD) :b\r   {A_k} = pre-expoential factor\r   {B_k} = temperature exponent\r   {Ea_k} = activation energy for the adsorption :pre\r \r :c,image(Eqs/k_kisliuk.png)\r \r {energy} args = m n (only for CI) :b\r   {m} = energy exponent\r   {n} = polar angle exponent :pre\r \r :c,image(Eqs/k_energy.png)\r \r The final rate of the reaction is computed by the product of the\r reaction rate constant and the surface coverage of all the adsorbed\r reactants. The reaction probability is obtained by normalized all the\r reaction rates. A detailed description can be found in Swaminathan\r Gopalan {et al.} \"(SG18)\"_#SG18.\r \r :line\r \r If there are gas-phase products, there is an option to specify the\r model used to compute how the particle(s) scatter from the surface for\r this reaction.  This will override the surface collision model\r assigned to the surface element using the\r \"surf_collide\"_surf_collide.html command.  If no reaction-specific\r scattering model is desired, specify a {NULL} value.\r \r Any of the following surface collision models can be used: {specular},\r {diffuse}, {adiabatic}, {cll}, {impulsive}, {td}.  The scattering\r model style and its corresponding arguments are specified in the line\r following the reaction-style.  If there are two gas-phase products,\r two lines (for the first and second particle) can be specified.  The\r arguments for the different surface scattering models are the same as\r specified in the \"surf_collide\"_surf_collide.html command.\r \r :line\r \r [Output info:]\r \r All the surface reaction models calculate a global vector of values.\r The values can be used by the \"stats_style\"_stats_style.html command\r and by \"variables\"_variable.html that define formulas.  The latter\r means they can be used by any command that uses a variable as input,\r e.g. the \"fix ave/time\"_fix_ave_time.html command.  See \"Section\r 4.4\"_Section_howto.html#howto_4 for an overview of SPARTA output\r options.\r \r This {adsorb} style compute a vector of length 2 + 2*nlist.  Nlist is\r the sum of both the GS and PS reactions listed in the file(s) read as\r input.  Note that this count includes all reactions in the files, not\r just the ones flagged as active for a particular simulation.\r \r The first element of the vector is the count of particles that\r performed surface reactions for surface elements assigned to this\r reaction model during the current timestep. The second element is the\r cummulative count of particles that have performed reactions since the\r beginning of the current run.  The next nlist elements are the count\r of each individual reaction that occurred during the current timestep.\r The final nlist elements are the cummulative count of each individual\r reaction since the beginning of the current run.\r \r :line",
      "docFile": "surf_react_adsorb.txt"
    },
    {
      "family": "surf_react",
      "style": "global",
      "docFile": ""
    },
    {
      "family": "surf_react",
      "style": "prob",
      "docFile": ""
    }
  ],
  "region": [
    {
      "family": "region",
      "style": "block",
      "docFile": ""
    },
    {
      "family": "region",
      "style": "cylinder",
      "docFile": ""
    },
    {
      "family": "region",
      "style": "plane",
      "docFile": ""
    },
    {
      "family": "region",
      "style": "sphere",
      "docFile": ""
    },
    {
      "family": "region",
      "style": "union",
      "docFile": ""
    },
    {
      "family": "region",
      "style": "intersect",
      "docFile": ""
    }
  ],
  "react": [
    {
      "family": "react",
      "style": "tce",
      "docFile": ""
    },
    {
      "family": "react",
      "style": "tce/qk",
      "docFile": ""
    }
  ],
  "dump": [
    {
      "family": "dump",
      "style": "image",
      "description": "Dump a high-quality ray-traced image of the simulation every N\r timesteps and save the images either as a sequence of JPEG or PNG or\r PPM files, or as a single movie file.  The options for this command as\r well as the \"dump_modify\"_dump_modify.html command control what is\r included in the image and how it appears.\r \r Any or all of these entities can be included in the images:\r \r particles (all in mixture or limited to a \"region\"_region.html)\r grid cells (all or limited to a \"region\"_region.html)\r x,y,z planes cutting through the grid\r surface elements :ul\r \r Particles can be colored by any attribute allowed by the \"dump\r particle\"_dump.html command.  Grid cells and the x,y,z cutting planes\r can be colored by any per-grid attribute calculated by a\r \"compute\"_compute.html or \"fix\"_fix.html.  Surface elements can be\r colored by any per-surf attribute calculated by a\r \"compute\"_compute.html or \"fix\"_fix.html.\r \r A series of images can easily be converted into an animated movie of\r your simulation (see further details below), or the process can be\r automated without writing the intermediate files using the dump movie\r command.  Other dump styles store snapshots of numerical data\r asociated with particles, grid cells, and surfaces in various formats,\r as discussed on the \"dump\"_dump.html doc page.\r \r Here are two sample images, rendered as JPG files.  \r Click to see the full-size images.\r \r <DIV ALIGN=center>\r \r :image(JPG/sphere_image_small.jpg,JPG/sphere_image.jpg)\r :image(JPG/mix_sine_small.jpg,JPG/mix_sine.jpg)\r \r </DIV>\r \r The left image is flow around a sphere with visualization of\r triangular surface elements on the sphere surface (colored by surface\r presssure), a vertical plane of grid cells (colored by particle\r density), and a horizontal plane of particles (colored by chemical\r species).  The right image is the initial condition for a 2d\r simulation of Rayleigh-Taylor mixing as a relatively dense heavy gas\r (red) mixes with a light gas (green), driven by gravity in the\r downward direction.\r \r :line\r \r The filename suffix determines whether a JPEG, PNG, or PPM file is\r created with the {image} dump style.  If the suffix is \".jpg\" or\r \".jpeg\", then a JPEG format file is created, if the suffix is \".png\",\r then a PNG format is created, else a PPM (aka NETPBM) format file is\r created.  The JPEG and PNG files are binary; PPM has a text mode\r header followed by binary data.  JPEG images have lossy compression;\r PNG has lossless compression; and PPM files are uncompressed but can\r be compressed with gzip, if SPARTA has been compiled with\r -DSPARTA_GZIP and a \".gz\" suffix is used.\r \r Similarly, the format of the resulting movie is chosen with the\r {movie} dump style.  This is handled by the underlying FFmpeg\r converter program, which must be available on your machine, and thus\r details have to be looked up in the FFmpeg documentation.  Typical\r examples are: .avi, .mpg, .m4v, .mp4, .mkv, .flv, .mov, .gif\r Additional settings of the movie compression like bitrate and\r framerate can be set using the \"dump_modify\"_dump_modify.html command.\r \r To write out JPEG and PNG format files, you must build SPARTA with\r support for the corresponding JPEG or PNG library.  To convert images\r into movies, SPARTA has to be compiled with the -DSPARTA_FFMPEG flag.\r See \"Section 2.2\"_Section_start.html#start_2 of the manual for\r instructions on how to do this.\r \r :line\r \r Dumps are performed on timesteps that are a multiple of N, including\r timestep 0.  Note that this means a dump will not be performed on the\r initial timestep after the dump command is invoked, if the current\r timestep is not a multiple of N.  This behavior can be changed via the\r \"dump_modify first\"_dump_modify.html command.  N can be changed\r between runs by using the \"dump_modify every\"_dump_modify.html\r command.\r \r Dump {image} filenames must contain a wildcard character \"*\", so that\r one image file per snapshot is written.  The \"*\" character is replaced\r with the timestep value.  For example, tmp.dump.*.jpg becomes\r tmp.dump.0.jpg, tmp.dump.10000.jpg, tmp.dump.20000.jpg, etc.  Note\r that the \"dump_modify pad\"_dump_modify.html command can be used to\r insure all timestep numbers are the same length (e.g. 00010), which\r can make it easier to convert a series of images into a movie in the\r correct ordering.\r \r Dump {movie} filenames on the other hand, must not have any wildcard\r character since only one file combining all images into a single movie\r will be written by the movie encoder.\r \r :line\r \r Several of the keywords determine what objects are rendered in the\r image, namely particles, grid cells, or surface elements.  There are\r additional optional kewords which control how the image is rendered.\r As listed below, all of the keywords have defaults, most of which you\r will likely not need to change.  The \"dump modify\"_dump_modify.html\r also has options specific to the dump image style, particularly for\r assigning colors to particles and other image features.\r \r :line\r \r Rendering of particles :h4\r \r Particles are drawn by default using the {color} and {diameter}\r settings.  The {particle} keyword allow you to turn off the drawing of\r all particles, if the specified value is {no}.\r \r Only particles in the specified mixture ID (mix-ID) are drawn.  Only\r particles in a geometric region can be drawn using the \"dump_modify\r region\"_dump_modify.html command.\r \r The {color} and {diameter} settings determine the color and size of\r particles rendered in the image.  They can be any particle attribute\r defined for the \"dump particle\"_dump.html command, including {type}.\r \r The {diameter} setting can be overridden with a numeric value by the\r optional {pdiam} keyword, in which case you can specify the {diameter}\r setting with any valid particle attribute.  The {pdiam} keyword\r overrides the {diameter} setting with a specified numeric value.  All\r particles will be drawn with that diameter, e.g. 1.5, which is in\r whatever distance \"units\"_units.html the input script defines.\r \r If {type} is specified for the {color} setting, then the color of each\r particle is determined by its type = species index.  By default the\r mapping of types to colors is as follows:\r \r type 1 = red\r type 2 = green\r type 3 = blue\r type 4 = yellow\r type 5 = aqua\r type 6 = purple :ul\r \r and repeats itself for types > 6.  This mapping can be changed by the\r \"dump_modify pcolor\"_dump_modify.html command.\r \r If {proc} is specified for the {color} setting, then the color of each\r particle is determined by the ID of the owning processor.  The default\r mapping of proc IDs to colors is that same as in the list above,\r except that proc P corresponds to type P+1.\r \r If {type} is specified for the {diameter} setting then the diameter of\r each particle is determined by its type = species index.  By default\r all types have diameter 1.0.  This mapping can be changed by the\r \"dump_modify adiam\"_dump_modify.html command.\r \r If {proc} is specified for the {diameter} setting then the diameter of\r each particle will be the proc ID (0 up to Nprocs-1) in whatever\r \"units\"_units.html you are using, which is undoubtably not what you\r want.\r \r Any of the particle attributes listed in the \"dump custom\"_dump.html\r command can also be used for the {color} or {diameter} settings.  They\r are interpreted in the following way.\r \r If \"vx\", for example, is used as the {color} setting, then the color\r of the particle will depend on the x-component of its velocity.  The\r association of a per-particle value with a specific color is\r determined by a \"color map\", which can be specified via the\r \"dump_modify cmap\"_dump_modify.html command.  The basic idea is that\r the particle-attribute will be within a range of values, and every\r value within the range is mapped to a specific color.  Depending on\r how the color map is defined, that mapping can take place via\r interpolation so that a value of -3.2 is halfway between \"red\" and\r \"blue\", or discretely so that the value of -3.2 is \"orange\".\r \r If \"vx\", for example, is used as the {diameter} setting, then the\r particle will be rendered using the x-component of its velocity as the\r diameter.  If the per-particle value <= 0.0, them the particle will not be\r drawn. \r \r :line\r \r Rendering of grid cells :h4\r \r The {grid} keyword turns on the drawing of grid cells with the\r specified color attribute.  For 2d, the grid cell is shaded with an\r rectangle that is infinitely thin in the z dimension, which allows you\r to still see the particles in the grid cell.  For 3d, the grid cell is\r drawn as a solid brick, which will obscure the particles inside it.\r \r Only grid cells in a grid group can be drawn using the \"dump_modify\r gridgroup\"_dump_modify.html command.  Only grid cells in a geometric\r region can be drawn using the \"dump_modify region\"_dump_modify.html\r command.\r \r The {gridx} and {gridy} and {gridz} keywords turn on the drawing of of\r a 2d plane of grid cells at the specified coordinate.  This is a way\r to draw one or more slices through a 3d image.\r \r The \"dump_modify region\"_dump_modify.html command does not apply\r to the {gridx} and {gridy} and {gridz} plane drawing.\r \r If {proc} is specified for the {color} setting, then the color of each\r grid cell is determined by its owning processor ID.  This is useful\r for visualizing the result of a load balancing of the grid cells,\r e.g. by the \"balance_grid\"_balance_grid.html or \"fix\r balance\"_fix_balance.html commands.  By default the mapping of proc\r IDs to colors is as follows:\r \r proc ID 1 = red\r proc ID 2 = green\r proc ID 3 = blue\r proc ID 4 = yellow\r proc ID 5 = aqua\r proc ID 6 = purple :ul\r \r and repeats itself for IDs > 6.  Note that for this command, processor\r IDs range from 1 to Nprocs inclusive, instead of the more customary 0\r to Nprocs-1.  This mapping can be changed by the \"dump_modify\r gcolor\"_dump_modify.html command.\r \r The {color} setting can also be a per-grid compute or fix.  In this\r case, it is specified as {c_ID} or {c_ID\\[N\\]} for a compute and as\r {f_ID} and {f_ID\\[N\\]} for a fix.\r \r This allows per grid cell values in a vector or array to be used to\r color the grid cells.  The ID in the attribute should be replaced by\r the actual ID of the compute or fix that has been defined previously\r in the input script.  See the \"compute\"_compute.html or \"fix\"_fix.html\r command for details.\r \r If {c_ID} is used as a attribute, then the per-grid vector calculated\r by the compute is used.  If {c_ID\\[N\\]} is used, then N must be in the\r range from 1-M, which will use the Nth column of the per-grid array\r calculated by the compute.\r \r If {f_ID} is used as a attribute, then the per-grid vector calculated\r by the fix is used.  If {f_ID\\[N\\]} is used, then N must be in the\r range from 1-M, which will use the Nth column of the per-grid array\r calculated by the fix.\r \r The manner in which values in the vector or array are mapped to color\r is determined by the \"dump_modify cmap\"_dump_modify.html command.\r \r :line\r \r Rendering of surface elements :h4\r \r The {surf} keyword turns on the drawing of surface elements with the\r specified color attribute.  For 2d, the surface element is a line\r whose diameter is specified by the {diam} setting as a fraction of the\r minimum simulation box length.  For 3d it is a triangle and the {diam}\r setting is ignored.  The entire surface is rendered, which in 3d will\r hide any grid cells (or fractions of a grid cell) that are inside the\r surface.\r \r Only surface elements in a surface group can be drawn using the\r \"dump_modify surfgroup\"_dump_modify.html command.  The \"dump_modify\r region\"_dump_modify.html command does not apply to surface element\r drawing.\r \r If {one} is specified for the {color} setting, then the color of every\r surface element is drawn with the color specified by the \"dump_modify\r scolor\"_dump_modify.html keyword, which is gray by default.\r \r If {proc} is specified for the {color} setting, then the color of each\r surface element is determined by its owning processor ID.  Surface\r elements are assigned to owning processors in a round-robin fashion.\r By default the mapping of proc IDs to colors is as follows:\r \r proc ID 1 = red\r proc ID 2 = green\r proc ID 3 = blue\r proc ID 4 = yellow\r proc ID 5 = aqua\r proc ID 6 = purple :ul\r \r and repeats itself for IDs > 6.  Note that for this command, processor\r IDs range from 1 to Nprocs inclusive, instead of the more customary 0\r to Nprocs-1.  This mapping can be changed by the \"dump_modify\r scolor\"_dump_modify.html command, which has not yet been added to\r SPARTA.\r \r The {color} setting can also be a per-surf compute or fix.  In this\r case, it is specified as {c_ID} or {c_ID\\[N\\]} for a compute and as\r {f_ID} and {f_ID\\[N\\]} for a fix.\r \r This allows per-surf values in a vector or array to be used to color\r the surface elemtns.  The ID in the attribute should be replaced by\r the actual ID of the compute or fix that has been defined previously\r in the input script.  See the \"compute\"_compute.html or \"fix\"_fix.html\r command for details.\r \r If {c_ID} is used as a attribute, then the per-surf vector calculated\r by the compute is used.  If {c_ID\\[N\\]} is used, then N must be in the\r range from 1-M, which will use the Nth column of the per-surf array\r calculated by the compute.\r \r If {f_ID} is used as a attribute, then the per-surf vector calculated\r by the fix is used.  If {f_ID\\[N\\]} is used, then N must be in the\r range from 1-M, which will use the Nth column of the per-surf array\r calculated by the fix.\r \r The manner in which values in the vector or array are mapped to color\r is determined by the \"dump_modify cmap\"_dump_modify.html command.\r \r :line\r :line\r \r The {size} keyword sets the width and height of the created images,\r i.e. the number of pixels in each direction.\r \r :line\r \r The {view}, {center}, {up}, {zoom}, and {persp} values determine how\r 3d simulation space is mapped to the 2d plane of the image.  Basically\r they control how the simulation box appears in the image.\r \r All of the {view}, {center}, {up}, {zoom}, and {persp} values can be\r specified as numeric quantities, whose meaning is explained below.\r Any of them can also be specified as an \"equal-style\r variable\"_variable.html, by using v_name as the value, where \"name\" is\r the variable name.  In this case the variable will be evaluated on the\r timestep each image is created to create a new value.  If the\r equal-style variable is time-dependent, this is a means of changing\r the way the simulation box appears from image to image, effectively\r doing a pan or fly-by view of your simulation.\r \r The {view} keyword determines the viewpoint from which the simulation\r box is viewed, looking towards the {center} point.  The {theta} value\r is the vertical angle from the +z axis, and must be an angle from 0 to\r 180 degrees.  The {phi} value is an azimuthal angle around the z axis\r and can be positive or negative.  A value of 0.0 is a view along the\r +x axis, towards the {center} point.  If {theta} or {phi} are\r specified via variables, then the variable values should be in\r degrees.\r \r The {center} keyword determines the point in simulation space that\r will be at the center of the image.  {Cx}, {Cy}, and {Cz} are\r speficied as fractions of the box dimensions, so that (0.5,0.5,0.5) is\r the center of the simulation box.  These values do not have to be\r between 0.0 and 1.0, if you want the simulation box to be offset from\r the center of the image.  Note, however, that if you choose strange\r values for {Cx}, {Cy}, or {Cz} you may get a blank image.  Internally,\r {Cx}, {Cy}, and {Cz} are converted into a point in simulation space.\r If {flag} is set to \"s\" for static, then this conversion is done once,\r at the time the dump command is issued.  If {flag} is set to \"d\" for\r dynamic then the conversion is performed every time a new image is\r created.  If the box size or shape is changing, this will adjust the\r center point in simulation space.\r \r The {up} keyword determines what direction in simulation space will be\r \"up\" in the image.  Internally it is stored as a vector that is in the\r plane perpendicular to the view vector implied by the {theta} and\r {pni} values, and which is also in the plane defined by the view\r vector and user-specified up vector.  Thus this internal vector is\r computed from the user-specified {up} vector as\r \r up_internal = view cross (up cross view) :pre\r \r This means the only restriction on the specified {up} vector is that\r it cannot be parallel to the {view} vector, implied by the {theta} and\r {phi} values.\r \r The {zoom} keyword scales the size of the simulation box as it appears\r in the image.  The default {zfactor} value of 1 should display an\r image mostly filled by the particles in the simulation box.  A {zfactor} >\r 1 will make the simulation box larger; a {zfactor} < 1 will make it\r smaller.  {Zfactor} must be a value > 0.0.\r \r The {persp} keyword determines how much depth perspective is present\r in the image.  Depth perspective makes lines that are parallel in\r simulation space appear non-parallel in the image.  A {pfactor} value\r of 0.0 means that parallel lines will meet at infininty (1.0/pfactor),\r which is an orthographic rendering with no persepctive.  A {pfactor}\r value between 0.0 and 1.0 will introduce more perspective.  A {pfactor}\r value > 1 will create a highly skewed image with a large amount of\r perspective.\r \r IMPORTANT NOTE: The {persp} keyword is not yet supported as an option.\r \r :line\r \r The {box} keyword determines how the simulation box boundaries are\r rendered as thin cylinders in the image.  If {no} is set, then the box\r boundaries are not drawn and the {diam} setting is ignored.  If {yes}\r is set, the 12 edges of the box are drawn, with a diameter that is a\r fraction of the shortest box length in x,y,z (for 3d) or x,y (for 2d).\r The color of the box boundaries can be set with the \"dump_modify\r boxcolor\"_dump_modify.html command.\r \r The {gline} keyword determines how the outlines of grid cells are\r rendered as thin cylinders in the image.  If the {gridx} or {gridy} or\r {gridz} keywords are specified to draw a plane(s) of grid cells, then\r outlines of all cells in the plane(s) are drawn.  If the planar\r options are not used, then the outlines of all grid cells are drawn,\r whether the {grid} keyword is specified or not.  In this case, the\r \"dump_modify region\"_dump_modify.html command can be used to restrict\r which grid cells the outlines are drawn for.\r \r For the {gline} keywork, if {no} is set, then grid outlines are not\r drawn and the {diam} setting is ignored.  If {yes} is set, the 12\r edges of each grid cell are drawn, with a diameter that is a fraction\r of the shortest box length in x,y,z (for 3d) or x,y (for 2d).  The\r color of the grid cell outlines can be set with the \"dump_modify\r glinecolor\"_dump_modify.html command.\r \r The {sline} keyword determines how the outlines of surface elements\r are rendered as thin cylinders in the image.  If {no} is set, then the\r surface element outlines are not drawn and the {diam} setting is\r ignored.  If {yes} is set, a line is drawn for 2d and a triangle\r outline for 3d surface elements, with a diameter that is a fraction of\r the shortest box length in x,y,z (for 3d) or x,y (for 2d).  The color\r of the surface element outlines can be set with the \"dump_modify\r slinecolor\"_dump_modify.html command.\r \r The {axes} keyword determines how the coordinate axes are rendered as\r thin cylinders in the image.  If {no} is set, then the axes are not\r drawn and the {length} and {diam} settings are ignored.  If {yes} is\r set, 3 thin cylinders are drawn to represent the x,y,z axes in colors\r red,green,blue.  The origin of these cylinders will be offset from the\r lower left corner of the box by 10%.  The {length} setting determines\r how long the cylinders will be as a fraction of the respective box\r lengths.  The {diam} setting determines their thickness as a fraction\r of the shortest box length in x,y,z (for 3d) or x,y (for 2d).\r \r :line\r \r The {shiny} keyword determines how shiny the objects rendered in the\r image will appear.  The {sfactor} value must be a value 0.0 <=\r {sfactor} <= 1.0, where {sfactor} = 1 is a highly reflective surface\r and {sfactor} = 0 is a rough non-shiny surface.\r \r The {ssao} keyword turns on/off a screen space ambient occlusion\r (SSAO) model for depth shading.  If {yes} is set, then particles\r further away from the viewer are darkened via a randomized process,\r which is perceived as depth.  The calculation of this effect can\r increase the cost of computing the image by roughly 2x.  The strength\r of the effect can be scaled by the {dfactor} parameter.  If {no} is\r set, no depth shading is performed.\r \r :line\r \r A series of JPEG, PNG, or PPM images can be converted into a movie\r file and then played as a movie using commonly available tools.  Using\r dump style {movie} automates this step and avoids the intermediate\r step of writing (many) image snapshot file.\r \r To manually convert JPEG, PNG or PPM files into an animated GIF or\r MPEG or other movie file you can:\r \r a) Use the ImageMagick convert program. :ulb,l\r \r % convert *.jpg foo.gif\r % convert -loop 1 *.ppm foo.mpg :pre\r \r Animated GIF files from ImageMagick are unoptimized. You can use a\r program like gifsicle to optimize and massively shrink them.\r MPEG files created by ImageMagick are in MPEG-1 format with rather\r inefficient compression and low quality.\r \r b) Use QuickTime. :l\r \r Select \"Open Image Sequence\" under the File menu Load the images into\r QuickTime to animate them Select \"Export\" under the File menu Save the\r movie as a QuickTime movie (*.mov) or in another format.  QuickTime\r can generate very high quality and efficiently compressed movie\r files. Some of the supported formats require to buy a license and some\r are not readable on all platforms until specific runtime libraries are\r installed.\r \r c) Use FFmpeg  :l\r \r FFmpeg is a command line tool that is available on many platforms and\r allows extremely flexible encoding and decoding of movies.\r \r cat snap.*.jpg | ffmpeg -y -f image2pipe -c:v mjpeg -i - -b:v 2000k movie.m4v\r cat snap.*.ppm | ffmpeg -y -f image2pipe -c:v ppm -i - -b:v 2400k movie.avi :pre\r \r Frontends for FFmpeg exist for multiple platforms. For more\r information see the \"FFmpeg homepage\"_http://www.ffmpeg.org/\r \r :ule\r \r :line\r \r You can play a movie file as follows:\r \r a) Use your browser to view an animated GIF movie. :ulb,l\r \r Select \"Open File\" under the File menu\r Load the animated GIF file\r \r b) Use the freely available mplayer or ffplay tool to view a\r movie. Both are available for multiple OSes and support a large\r variety of file formats and decoders. :l\r \r % mplayer foo.mpg \r % ffplay bar.avi :pre\r \r c) Use the \"Pizza.py\"_https://lammps.github.io/pizza\r \"animate tool\"_https://lammps.gitbug.io/pizza/doc/animate.html,\r which works directly on a series of image files. :l\r \r a = animate(\"foo*.jpg\") :pre\r \r d) QuickTime and other Windows- or MacOS-based media players can\r obviously play movie files directly. Similarly for corresponding tools\r bundled with Linux desktop environments.  However, due to licensing\r issues with some file formats, the formats may require installing\r additional libraries, purchasing a license, or may not be\r supported. :ule,l\r \r :line",
      "docFile": "dump_image.txt"
    },
    {
      "family": "dump",
      "style": "movie",
      "docFile": ""
    }
  ]
}
